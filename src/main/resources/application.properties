spring.application.name=booking-healthy
server.port=${SERVER_PORT:8090}

app.base-url=${APP_BASE_URL:http://localhost:8090}

# === DATABASE ===
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/bookinghealthy}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:hoanganh176}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=${SHOW_SQL:true}

# Enable running SQL file manually
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true

spring.thymeleaf.cache=${THYMELEAF_CACHE:false}
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=never
spring.session.timeout=30m

server.servlet.session.persistent=false
server.servlet.session.cookie.secure=${COOKIE_SECURE:false}
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=lax
app.upload-dir=${UPLOAD_DIR:uploads}
app.private-dir=${PRIVATE_DIR:private}
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.forward-headers-strategy=${FORWARD_HEADERS:framework}

# === MAIL CONFIGURATION ===
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:vuhoanganh1706@gmail.com}
spring.mail.password=${MAIL_PASSWORD:jlgn duze xdjc yqbp}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:75450127050-ipf4cievvhp8rhisanne993lui6rmmt0.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=profile, email

spring.security.oauth2.client.registration.facebook.client-id=${FACEBOOK_CLIENT_ID:1541719893702600}
spring.security.oauth2.client.registration.facebook.client-secret=${FACEBOOK_CLIENT_SECRET:d8b9b08eb19d8f4f96adfaf3600243a1}
spring.security.oauth2.client.registration.facebook.scope=email, public_profile

# === AI Agent Configuration (OpenAI Standard) ===
ai.api.url=${AI_API_URL:https://openrouter.ai/api/v1/chat/completions}
ai.api.key=${OPENROUTER_API_KEY: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}
ai.api.referer=${APP_BASE_URL:http://localhost:8090}

# === VNPAY ===
vnpay.pay-url=${VNPAY_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
vnpay.api-url=${VNPAY_API_URL:https://sandbox.vnpayment.vn/merchant_webapi/api/transaction}
vnpay.tmn-code=${VNPAY_TMN_CODE:S9U6WCSW}
vnpay.hash-secret=${VNPAY_HASH_SECRET:9J6U7W5KKELXY9XY6GL7EACW161AGTVM}

vietqr.bank-id=${VIETQR_BANK_ID:MB}
vietqr.account-no=${VIETQR_ACCOUNT_NO:0965170604}
vietqr.account-name=${VIETQR_ACCOUNT_NAME:VU HUU HOANG ANH}
vietqr.memo-prefix=${VIETQR_MEMO_PREFIX:HEALCARE}

payment.webhook.secret=${PAYMENT_WEBHOOK_SECRET:}

seed.enabled=${SEED_ENABLED:true}
seed.admin.username=${SEED_ADMIN_USERNAME:admin}
seed.admin.password=${SEED_ADMIN_PASSWORD:admin123}
seed.demo-accounts.enabled=${SEED_DEMO_ACCOUNTS:true}
seed.doctor.default-password=${SEED_DOCTOR_PASSWORD:123456}

spring.task.scheduling.pool.size=${SCHEDULER_POOL_SIZE:4}

app.schema.strict=${SCHEMA_STRICT:false}

management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=when-authorized
management.endpoint.health.probes.enabled=true

news.fetch.enabled=${NEWS_FETCH_ENABLED:true}
news.fetch.cron=${NEWS_FETCH_CRON:0 0 6,18 * * ?}
news.fetch.max-per-run=${NEWS_FETCH_MAX_PER_RUN:2}
news.fetch.max-age-days=${NEWS_FETCH_MAX_AGE_DAYS:3}
