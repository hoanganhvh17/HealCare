# Progress Log & Documentation Upkeep (MANDATORY)

**Every time work is completed in this repo, the documentation must be updated in the same change.** Do not treat a task as finished until this rule has been applied — it is part of the task, not optional follow-up.

## What to update

1. **This file** — append an entry to the log below for every completed piece of work (new feature, bug fix, refactor, config change, removed code).
2. **The topic rule file(s) under `.claude/rules/`** — if the change alters how the app actually behaves, update the rule that owns that topic so it stays accurate:
   - booking / slots / cancel / reschedule / queue → [booking-flow.md](booking-flow.md)
   - roles, URL authorization, login flows → [authentication-and-roles.md](authentication-and-roles.md)
   - new controller/service/repository/entity layout, new package → [code-structure.md](code-structure.md)
   - AI prompt, JSON schema, models, memory → [ai-assistant.md](ai-assistant.md)
   - medical records cluster → [medical-records.md](medical-records.md)
   - wallet, recruitment, content, QR, email, dashboards → [supporting-subsystems.md](supporting-subsystems.md)
   - properties, DB, seed data, uploads, port → [environment-setup.md](environment-setup.md)
   - Maven, dependencies, run/build commands → [build-and-run.md](build-and-run.md)
   - conventions, gotchas, error handling, testing → [coding-conventions.md](coding-conventions.md)
3. **[CLAUDE.md](../../CLAUDE.md)** — only when a *new* rule file is created (add the `@` import) or when the one-line project description at the top no longer fits.

If a change spans several topics, update every rule file it touches. If a documented behaviour is deleted, delete the sentence describing it — stale rules are worse than missing ones.

## Entry format

Newest first. One line per completed unit of work:

```
- YYYY-MM-DD — `type:` short Vietnamese description — files/areas touched — rule files updated (or `—`)
```

`type` follows the commit convention: `feat:` / `fix:` / `refactor:` / `docs:` / `chore:`.

Keep entries short (one line). Details belong in the topic rule file, not here.

---

## Log
- 2026-08-25 — `feat:`/`fix:` **rà soát ngang toàn dự án: vá 6 lỗ hổng quyền + 10 lỗi sập trang + 6 lỗi tiền bạc, rồi thêm màn hình thu ngân tại quầy** — quét 36 controller, 28 service impl, 40 entity và toàn bộ templates; mọi phát hiện đều mở file đọc trực tiếp để xác minh, phần schema và số tiền truy vấn thẳng trên MySQL đang chạy — **(A) lỗ hổng nặng nhất là một dòng KHÔNG TỒN TẠI**: `@EnableMethodSecurity` không có ở bất kỳ đâu trong dự án, nên mọi `@PreAuthorize` chỉ là chú thích — không chặn gì, không cảnh báo lúc biên dịch lẫn lúc chạy; cộng với `/api/doctor/**` nằm trong danh sách `permitAll`, `DoctorAssistantController` (khai `@PreAuthorize("hasRole('DOCTOR')")`) thành **proxy LLM công khai cho cả Internet**, gọi OpenRouter bằng API key bệnh viện — một vòng lặp là cạn credit, kéo theo chatbot bệnh nhân và 4 trợ lý form khám chết theo; hoá ra cả nhánh đó là **mã chết** (`doctor-ai-chat.js` gọi nó không template nào nạp) nên **xoá hẳn** thay vì vá, cộng thu hẹp luật URL về `/api/doctor/{id:[0-9]+}` — **(B) ba đường bệnh án của bác sĩ đều hở, mỗi đường một kiểu**: `viewRecordForDoctor` bị **comment mất** phép kiểm (duyệt id là đọc bệnh án cả viện), `saveAdvancedMedicalRecord` **nhận `Authentication` rồi không dùng** — bác sĩ A ghi được bệnh án + đơn thuốc vào ca của B, đẩy ca sang COMPLETED rồi `deliver()` **gửi email kèm PDF đơn thuốc giả tới bệnh nhân của B** và ca thật của B kẹt cứng; `viewPatientHistoryRecords` chỉ kiểm "có phải bác sĩ không" nên trả trọn bệnh sử + hồ sơ ngoại viện của bệnh nhân bất kỳ, **đi vòng qua đúng `whyCannotView`** mà `ExternalMedicalRecordService` đã dựng sẵn; nghịch lý: 3 endpoint KHÁC trong chính file đó đều kiểm đầy đủ — **(C) `@GetMapping` ghi dữ liệu cuối cùng còn sót**: `/doctor/schedule/unblock/{id}` không nhận cả `Authentication`, service là `deleteById()` mù, mà CSRF **không xét GET** nên một thẻ `<img>` trên trang lạ là gỡ được; nặng hơn vẻ ngoài vì các bản ghi đó do `LeaveServiceImpl.applyBlockTimes` sinh ra khi **đơn nghỉ phép được duyệt** — xoá đi là bệnh nhân đặt được lịch vào ngày bác sĩ đang nghỉ; nay là POST + kiểm quyền, **và từ chối gỡ khối giờ mang `leaveRequestId`** (thuộc hệ thống nghỉ phép, không phải của bác sĩ) — **(D) trưởng khoa duyệt được đơn nghỉ/ca trực của KHOA KHÁC**: hàng đợi trên màn hình đã lọc theo khoa nên giao diện chưa từng mời bấm nhầm, nhưng `approve`/`reject`/`approveShift`/`rejectShift` nhận thẳng `@PathVariable id`; thêm `whyCannotDecideAs` **tách khỏi `whyCannotDecide`** có chủ ý — hàm kia nói về tính hợp lệ của bản thân lá đơn và được dùng để tách khối "đã hết hiệu lực", khoa của người duyệt không phải thuộc tính của lá đơn — **(E) 10 lỗi HTTP 500**, nặng nhất là `ScheduleInfoController` deref `getDoctor().getDepartment().getId()` trên **trang công khai** `/doctor-schedule` (một bác sĩ chưa gán khoa = 500 cho mọi khách), `BookingStatus.valueOf` và `LocalDate.parse` không bọc trên endpoint `permitAll`, và `getBookingPrice()` null **ngay giữa luồng đối soát thanh toán** — **(F) tiền bạc**: `cancelWithRefund` **thiếu `@Transactional`** trong khi `reserve`/`reassign`/`rescheduleByUser` đều có, mà `refundToWallet` mang transaction riêng nên commit ngay — `save()` hỏng sau đó là tiền đã ra khỏi két mà lịch vẫn `PAID`, bấm Hủy lần nữa hoàn tiếp; `ProfileController` tự viết lại logic hoàn tiền nên gộp về hàm chung; ví bị **lost update** (đọc-tính-ghi, hai request song song cùng lọt) → chuyển sang `UPDATE ... WHERE balance >= :amount` nguyên tử, **chọn cách này thay vì `@Version`** vì optimistic locking trên `User` sẽ bắt mọi đường ghi User khác phải xử lý xung đột; nhánh WALLET của `processAppointment` gộp 3 transaction rời thành một `reserveAndPayWithWallet` — **(G) báo cáo tài chính cho admin sai 100 lần**: `DecimalFormat` với mẫu `#,##0.0'%'` có dấu nháy nên `%` là **ký tự literal**, không nhân 100, mà `revenueTrend` đã là điểm phần trăm sẵn — chia thêm là tăng trưởng 15,2% in ra "0,2%", tỷ lệ hủy 30% thành "0,3%"; kiểm chứng sau khi sửa: báo cáo in **-2,1%**, khớp đúng SQL đối chứng theo định nghĩa `('PAID','REFUNDED')` của service — **(H) ảnh đại diện bác sĩ mới chưa từng được lưu**: `userService.save(user)` chạy trong transaction riêng của repository và **detach** entity khi trả về (controller không có `@Transactional`), nên `setAvatar` sau đó rơi vào hư không — tệp đã ghi ra đĩa (rác tích luỹ) còn DB trống; `updateDoctor` làm đúng thứ tự, chỉ đường tạo mới sai — **(I) giao diện**: `/department-details/{id}` là trang CÔNG KHAI mà **tự viết footer riêng** nên còn nguyên nội dung mẫu của theme — "Web Design / Web Development / Product Management / Marketing / Graphic Design" và hai cột lorem ipsum ("Hic solutasetp", "Molestiae accusamus iure") cùng 4 icon `href=""` bấm vào tải lại chính trang; trang 404 là **ngõ cụt** (logo và nút "Về trang chủ" đều `href="index.html"`) **và mất CSS** (tài nguyên dùng đường dẫn tương đối, lỗi ở `/admin/xyz` khiến trình duyệt tìm `/admin/assets/css/main.css`) → viết lại 283 dòng còn 68 theo khuôn `error/403.html`; 4 link chết trong footer dùng chung (nhân lên 22 trang), nút "Nạp thêm tiền" và "Quên mật khẩu?" là nút giả, badge "Tài khoản đã xác thực" hardcode hiện cho mọi người dù dự án **không có luồng xác thực email nào**, mã bệnh nhân hardcode "HC-849201" giống hệt nhau ở mọi tài khoản — **(J) CHỨC NĂNG MỚI — thu tiền tại quầy + đánh dấu vắng khám**: trước đó `setPaymentStatus("PAID")` chỉ tồn tại ở ba nơi (ví, webhook VietQR, VNPay) nên **không màn hình nào ghi được tiền mặt** — đo trên DB dev: **2.800.000đ tiền thật vô hình** với cả 4 truy vấn doanh thu; hai hàm gác `whyCannotCollectPayment`/`whyCannotMarkNoShow` **cố ý KHÔNG dùng lại `whyStaffCannotChange`** vì hàm đó từ chối mọi lịch đã qua giờ, mà thu tiền quầy xảy ra đúng SAU giờ hẹn (bệnh nhân tới ca 08:00, lễ tân thu lúc 08:05) — dùng lại là nút không bao giờ hiện; **`NO_SHOW` cố ý KHÔNG tự hoàn tiền** vì làm vậy là biến nó thành đường hủy miễn phí đi vòng qua luật 24 giờ; **4 truy vấn doanh thu không phải sửa một dòng** vì đã khoá theo `paymentStatus` — **(K) bẫy ENUM native tái hiện đúng như tài liệu cảnh báo, và nó hỏng CẢ TRÊN DEV**: sau khi khởi động lại với hằng số `NO_SHOW` đã có trong Java, `ddl-auto=update` tạo 3 cột mới nhưng **không** viết lại danh sách ENUM, lệnh ghi trả `ERROR 1265: Data truncated for column 'status'`; `db/manual/004_*.sql` viết **idempotent** (hỏi `INFORMATION_SCHEMA` rồi `PREPARE`, vì MySQL 8 không có `ADD COLUMN IF NOT EXISTS` còn trên dev ba cột đã tồn tại), kiểm chứng chạy lại lần hai: exit 0, không lỗi, khoá ngoại không nhân đôi — **(L) kiểm chứng end-to-end trên jar đã đóng gói với DB thật**: `clean package` **5/5 test xanh**; đăng nhập thật 5 vai trò, quét 39 URL đều 200 (riêng `/appointment` 302 cho khách chưa đăng nhập — đúng thiết kế); **phép kiểm âm bản** là thứ duy nhất chứng minh đã vá — bác sĩ A xem bệnh án của B → **302 chặn** trong khi xem của chính mình → **200**, ghi bệnh án vào ca của B → **chặn, lịch 23 vẫn CONFIRMED** trong khi ghi vào ca của mình → **thành công, đúng 1 bệnh án mới**, `GET unblock` → **405**, `/careers` chưa đăng nhập → **200** (trước 302), `?status=abc` → **200 bỏ lọc** (trước 500), `?date=abc` → **400** (trước 500), body thiếu `prompt` → **400 kèm câu tiếng Việt** (trước 500); **đối chiếu tiền từng đồng**: thu 500.000đ một lịch → "đã thu" **+500.000**, "còn phải thu" **−500.000**, thu lần hai bị chặn không cộng đôi; đánh dấu vắng khám lịch **tương lai bị chặn**, lịch **quá khứ thành công** và **số dư ví KHÔNG đổi** (không tự hoàn), NO_SHOW không bị đếm vào "còn phải thu"; 41 form POST / 41 token CSRF khớp hoàn toàn; **dữ liệu thử đã dọn sạch, DB về đúng nền** (63 lịch, 9 bệnh án, 12.100.000 đã thu, 3.300.000 còn phải thu, 0 dòng NO_SHOW sót) — **lỗi CÓ SẴN tìm thêm khi làm, đã sửa**: `error/403.html:31` trỏ `/assets/img/not-found.svg` **không tồn tại** nên hiện ảnh vỡ; form liên hệ gửi tới địa chỉ Gmail **hardcode trong mã** và gửi **đồng bộ** (khách phải chờ SMTP) → đưa ra `app.contact-email` + `EmailService` (`@Async`) + escape HTML; trang chủ gọi `departmentService.findAll()` **hai lần**, `/about` nạp 132 bác sĩ chỉ để `.size()`; `AuthService` **không đặt `authProvider`** nên mọi tài khoản đăng ký qua web mang `auth_provider = NULL` — **CHƯA sửa, đã báo lại**: footer dùng chung có link Zalo trỏ số `0965170604` nhưng chữ hiển thị `0987654321`, không tự đoán số nào đúng — **CỐ Ý để lại thành nợ** (refactor lớn, gộp vào sẽ làm thay đổi không kiểm chứng nổi): **phân trang** (grep `Pageable` toàn dự án = **0 kết quả**, `ReceptionistDashboardController` và `AdminBookingController` nạp trọn bảng bookings vào RAM rồi lọc bằng Java), **N+1** (nặng nhất `LeaveServiceImpl.countAffectedBookings` chạy một truy vấn cho MỖI NGÀY trong kỳ nghỉ — đơn thai sản 180 ngày × 20 đơn = **3.600 truy vấn** một lần mở `/head/leave-requests`), **chỉ mục DB** (không entity nào khai `@Index`; `bookings` không có chỉ mục trên `(doctor_id, appointment_date)`), và **quên mật khẩu / nạp ví / xác thực email** (nút đã ẩn thay vì để nút giả) — **CHƯA commit, CHƯA deploy**; **chưa mở bằng trình duyệt thật**: 2 nút mới của lễ tân mới kiểm ở tầng HTTP + HTML đã render — `SecurityConfig`, `DoctorMedicalRecordController`, `DoctorDashboardController`, `DoctorBlockTimeService(+Impl)`, `LeaveServiceImpl`, `StaffScheduleServiceImpl`, `BookingService(+Impl)`, `WalletServiceImpl`, `UserRepository`, `ReceptionService(+Impl)`, `ReceptionistBookingController`, `BookingController`, `ProfileController`, `AdminDoctorController`, `AdminAiController`, `BookingApi`, `ScheduleInfoController`, `DoctorController`, `DoctorBookingRequestController`, `StaffWorkScheduleController`, `UserMedicalDocumentController`, `VietQRController`, `PaymentController`, `HomeController`, `AuthService`, `Booking`, `BookingStatus`, `db/manual/004_cash_collection_and_no_show.sql` (mới), `application.properties`, 8 template; **xoá** `DoctorAssistantController`, `DoctorAssistantService`, `UserController`, `doctor-ai-chat.js` — authentication-and-roles.md, booking-flow.md, medical-records.md, environment-setup.md, supporting-subsystems.md, code-structure.md, coding-conventions.md, progress-log.md
- 2026-08-24 — `chore:` **deploy commit `feac4f7` (sidebar bác sĩ + logo mới) lên production** — **(A) phép kiểm chặn đường cho kết quả RỖNG, và đó là tin tốt**: `git diff --stat 7ae0722..feac4f7 -- '*/model/*.java'` không trả dòng nào, tức **không đụng `@Entity` nào** nên không cần `db/manual/*.sql` và không dính bẫy `DDL_AUTO=validate` từng làm sập production hôm 2026-08-21 — **(B) phải xác định production đang chạy commit nào TRƯỚC, và nhật ký không đủ tin**: mục deploy gần nhất ghi `2bb1bfc`, nhưng trên máy chủ có một lần đổi jar ngày 23/08 **không ai ghi lại**; nếu prod thật sự còn ở `2bb1bfc` thì lần đẩy này sẽ kéo theo trọn đợt bật CSRF (`7ae0722`) — 55 tệp, 943 dòng, **chưa từng mở bằng trình duyệt thật** — tức một deploy hoàn toàn khác về mức rủi ro; **đo bằng chính ứng dụng đang chạy chứ không đoán**: `curl localhost:8090/assets/js/csrf.js` → **200** và trang chủ có phát cookie **`XSRF-TOKEN`** → prod đã ở `7ae0722`, nên delta thật chỉ là 11 tệp của `feac4f7` — **(C) tên miền trong tài liệu đã CHẾT**: `nnlhospital.io.vn` không phân giải được từ cả máy dev lẫn máy chủ (trong khi `google.com` thì được), production nay chạy ở **`hoanganhvh17.online`** (chứng chỉ Let's Encrypt của nginx ghi đúng tên đó); `deploy/README.md` và `DEPLOY-ORACLE-FREE.md` vẫn ghi tên cũ — **(D) đóng gói bằng `git worktree` chứ không build tại chỗ**, và lần này lý do là cụ thể chứ không phải nguyên tắc suông: thư mục làm việc đang có `application.properties` chưa commit chứa **bí mật vừa xoay** (Gmail app password, Google OAuth client id + secret, OpenRouter key, mật khẩu MySQL) — build tại chỗ là nướng thẳng chúng vào jar, và commit chúng còn tệ hơn: đó đúng là cách bộ bí mật cũ đã bị đốt, xem mục 0 của `deploy/README.md`; worktree đã được kiểm là **không** chứa các giá trị đó — **(E) bẫy Windows MAX_PATH lặp lại**: `D:\nnlbuild` bị guard của môi trường chặn xoá nên đổi sang `D:\nnlb`; đường dẫn phải NGẮN, đặt worktree dưới thư mục scratchpad là `git` chết giữa chừng ở `assets-admin/vendor/` — **(F) hai con số "sai" khi soi jar hoá ra là chú thích của chính mình**: template trong jar vẫn còn 1 chuỗi `Emergency Call` và 2 chuỗi `/admin/dashboard`, nhưng cả ba đều nằm trong khối `<!--/* */-->` (Thymeleaf strip lúc render) — bài học: **kiểm trên HTML ĐÃ RENDER, không kiểm trên mã nguồn template**; đối chiếu `th:href` của thẻ logo cho đúng 4 giá trị `/doctor/dashboard`, `/receptionist/dashboard`, `/head/dashboard`, `/` và **0** chỗ trỏ `/admin/dashboard` — **(G) thứ tự hạ thời gian chết xuống đúng một lần khởi động lại**: tải jar lên `/tmp` trước (chưa dừng gì), đối chiếu `sha256` hai đầu (khớp `58ca8c4e…`), sao lưu DB + tệp **khi app cũ vẫn phục vụ** (52K + 15M), rồi mới `cp` jar cũ ra `.bak-20260824-022215` → `stop` → đổi → `chown/chmod 750` → `start`; `Started BookingHealthyApplication in 13.57 seconds`, `SchemaGuard` báo lược đồ đầy đủ, **0 dòng ERROR** — **(H) `cf-cache-status: BYPASS`, phép kiểm bắt buộc mỗi lần đổi tài nguyên tĩnh**: logo nằm trong jar nên một bản cache ở biên Cloudflare sẽ khiến khách vẫn thấy logo CŨ dù jar đã đổi; đo thật hai lượt liên tiếp đều `BYPASS` kèm `cache-control: no-cache, no-store, must-revalidate` → khách thấy ngay — **(I) kiểm chứng trên chính production qua HTTPS**: logo trả **200 / 628.668 byte** (bản cũ 738 byte), 5/5 trang công khai 200, **4/4 endpoint có bảo vệ trả 302** (`/doctor/dashboard`, `/admin/dashboard`, `/user/profile`, `/api/chat/my-bookings`) tức luật khối 0 vẫn chặn người lạ, `/actuator/health` 200 — **CHƯA kiểm bằng tài khoản thật trên production**: production đặt `SEED_DOCTOR_PASSWORD` khác mặc định dev nên không đăng nhập được, và **không đoán mật khẩu**; sidebar mới mới chỉ được kiểm end-to-end trên máy dev với DB thật (8 trang, huy hiệu, phân vai) cộng với việc jar đang chạy có `sha256` khớp đúng jar đã soi nội dung — **(J) món nợ hiệu năng do logo mới tạo ra**: 628 KB cho một ảnh hiển thị ~30px, mà Spring gắn `Cache-Control: no-store` cho tài nguyên tĩnh nên nó được tải lại ở **mọi lượt xem trang** của nhân viên; nên nén xuống vài chục KB — **(K) đường quay lui** `app.jar.bak-20260824-022215` còn nguyên, không cần gỡ gì vì commit này không thêm bảng nào; đĩa còn 42G/48G — không sửa mã nguồn — progress-log.md
- 2026-08-24 — `feat:`/`fix:` **thiết kế lại sidebar khu bác sĩ; nó vốn là mảnh sót của một bản mẫu chứ chưa từng được viết cho dự án này** — sidebar này đang được **8 trang thuộc 3 khu** nhúng (6 trang `doctor/*`, 3 trang `staff/*` qua `__${sidebarFragment}__`, và nhánh `role == 'DOCTOR'` của `user/medical-record-detail.html` — một trang **bệnh nhân cũng nhìn thấy**), và **cả 8 đều nằm dưới `/doctor/**`**, kể cả trang cuối vì bác sĩ tới đó qua `/doctor/medical-record/view/{id}` — **(A) khối hồ sơ trên cùng là ảnh stock Unsplash + chữ tiếng Anh "HealCare Family / Manage family health" hardcode**, trong khi header ngay cạnh xưa nay vẫn đọc đúng `#authentication.principal.fullName`/`.avatar`; nay chép nguyên biểu thức avatar của `doctor/include/header.html:94-97` (đã xử lý sẵn avatar rỗng) — **(B) nút đỏ "Emergency Call" KHÔNG có handler nào trong toàn dự án**, bấm không ra gì, mà một nút đỏ chết còn tệ hơn không có nút; thay bằng nút mở trợ lý AI (`window.toggleDoctorAI`, giữ guard `if (window...)` để một trang tương lai quên nạp widget thì nút chỉ im chứ không ném lỗi JS làm chết phần script còn lại) — **(C) `/doctor/booking-requests` chưa từng có mục nào trong sidebar** dù controller vẫn đặt `activePage = "requests"`: không `<li>` nào kiểm giá trị đó nên trang chỉ tới được bằng một link trên dashboard, và đứng ở đó thì cả sidebar tối thui — **(D) ba trang không đặt `activePage`** (`medical-record-form`, `patient-records`, `medical-record-detail`), sửa bằng **3 dòng trong đúng một controller** chứ không đẩy vào interceptor: 4 controller còn lại đã tự đặt và đang đúng, ghi đè lên chúng là đổi hành vi đang tốt để lấy sự "gọn" không ai cần — **(E) hai huy hiệu, và cái thứ hai phải ĐỔI so với yêu cầu ban đầu**: "bệnh án chưa hoàn tất" nghe hợp lý nhưng `BookingStatus.COMPLETED` chỉ được gán ở đúng hai chỗ (`MedicalRecordServiceImpl:71` và `:141`), **cả hai đều ngay sau `save(record)` trong cùng transaction**, nên trạng thái đó **không thể tồn tại** — con số luôn 0, huy hiệu không bao giờ render mà vẫn tốn một câu COUNT mỗi lượt tải trang; kể cả khác 0 vì dữ liệu cũ thì bác sĩ **cũng không sửa được** (`/doctor/examinations` chỉ hiện nút "Khám" khi `isToday`, `showCreateForm` cũng chặn ở server), tức vi phạm đúng luật "con số phải về 0 được bằng thao tác của chính người nhìn thấy nó" → thay bằng **"ca cần khám hôm nay"** (`CONFIRMED` + `appointment_date = CURDATE()`, dùng lại `countByDoctorIdAndStatusAndDateRange` có sẵn, khớp `countToday` của dashboard); "yêu cầu chờ duyệt" **cố ý không lọc ngày** để bằng đúng số dòng của trang nó dẫn tới, chấp nhận lệch với thẻ "Yêu cầu mới" trên dashboard vốn cắt theo kỳ 7/30 ngày — phần chênh đã được chính ô AI Insight dưới thẻ nói ra — **(F) `DoctorNavInterceptor` giữ nguyên cả 4 ràng buộc của `AdminNavInterceptor`** (`postHandle` chứ không `@ControllerAdvice`; chốt `mv == null`/`redirect:`; tiền tố `nav` vì `AbstractView` trộn FlashMap **trước** model của handler nên trùng tên là nuốt sạch thông báo lỗi — kênh báo lỗi duy nhất của ứng dụng; bọc `try/catch` để mất một con số chứ không mất trang), giải người dùng qua `CurrentUserService.find(auth)` chứ không `auth.getName()` thẳng vì principal có thể là `OAuth2User` — **(G) CSS ở lại trong `<style>` của fragment**, không tách tệp: khối đó nằm trong `<body>` nên thắng mọi `<link>` ở `<head>` ở cùng specificity, đúng thứ cần để đè `.sidebar` của `style.css` mà **không phải `!important`**; bỏ thuộc tính `style=` inline (nó còn mang `z-index: 999`, tức **đè lên header vốn 997**) và chuyển sang `#sidebar.doc-sidebar`; **không** gom sang `admin-theme.css` vì mọi luật ở đó bọc `body.admin-theme` mà thêm class đó vào các trang này là restyle luôn trang bệnh nhân — **(H) đổi tiền tố class sang `doc-*`**: `.emergency-btn` cũ **trùng tên** với một class có thật trong theme bệnh nhân (`assets/css/main.css:2070`, đang được bọc dưới `.call-to-action .emergency-alert` nên chưa nổ); `button.doc-nav-link` cố ý kèm tên thẻ (0,1,1) vì nút Đăng xuất là `<button>` không thừa kế font — đúng bẫy `.ai-chat-input-area button` đã ghi sẵn; ternary viết **trọn trong một cặp `${...}`** theo bẫy SpEL đã cắn dự án hai lần — **(I) khối hồ sơ chuyển từ dọc/avatar 64px sang ngang/avatar 40px** (~110px → ~64px) và ô mục siết còn `10px 14px`: sidebar admin đã từng tràn trên 1366×768 mà bản này còn thêm một mục — **(J) kiểm chứng end-to-end trên jar đã đóng gói với DB thật**, không phải chỉ biên dịch được: 6/6 trang 200 với **đúng 1 `<aside id="sidebar">`, đúng 1 `<header>`, đúng 1 nút AI, đúng một mục sáng**; 3 trang bệnh án trước đây tối thui nay sáng "Lịch khám bệnh"; huy hiệu khớp **chính xác** SQL đối chứng (3 và 2) rồi **giảm 3→2 sau khi xác nhận thật một yêu cầu qua POST**, và **biến mất hoàn toàn khi về 0**; trưởng khoa (`bs_nguyenductoan`) thấy "Phê duyệt của khoa" còn bác sĩ thường (`doctor_walter`) không; trang bệnh nhân `/user/medical-record/view/{id}` **0 `doc-sidebar`, 0 `doc-nav-link`**, header bệnh nhân nguyên vẹn; ảnh chụp thật ở 1440×900 / 1366×768 / 390×844 (mobile mở sidebar có nền mờ, nút AI ghim đáy); `./mvnw clean package` **5/5 test xanh**; **dữ liệu thử đã xoá sạch, DB về đúng 63 lịch và phân bố 9/24/30 như trước** — **lỗi CÓ SẴN tìm ra khi kiểm, không sửa vì ngoài phạm vi**: `DoctorBookingManagerController:48` sắp xếp theo `createdAt` **không guard null**, nên một dòng `bookings` có `created_at` NULL là HTTP 500 cả trang (lộ ra khi tôi chèn dữ liệu thử bằng SQL thô); và `doctor/include/header.html:46` — logo "HealCare" trỏ `@{/admin/dashboard}` nên bác sĩ bấm vào logo nhận **403** — `doctor/include/sidebar.html` (viết lại), `DoctorNavInterceptor` (mới), `WebConfig`, `BookingRepository`, `DoctorMedicalRecordController` — coding-conventions.md, code-structure.md, progress-log.md
- 2026-08-24 — `fix:` **vá hai lỗi điều hướng CÓ SẴN tìm ra khi kiểm chứng đợt sidebar ngay trên** — **(A) logo trên header nhân viên trả 403 cho MỌI người bấm vào**: `doctor/include/header.html:46` hardcode `@{/admin/dashboard}`, nhưng fragment `header-nav` đó **không phải của admin** (`admin/include/header :: header-nav` mới là, và nó trỏ đúng) — nó phục vụ **ba** vai trò khác: bác sĩ (6 trang `doctor/*` + nhánh `role=='DOCTOR'` của `user/medical-record-detail`), trưởng khoa (4 trang `head/*`), và **lễ tân** (3 trang `staff/*`, vì `StaffWorkScheduleController` dùng chung cho cả hai vai trò — chỗ dễ bỏ sót nhất vì tên thư mục `doctor/` không gợi ra điều đó); nay rẽ bằng `sec:authorize` với **DOCTOR đứng TRƯỚC HEAD_DOCTOR** y như `successHandler` của `SecurityConfig` (trưởng khoa do `DataInitializer` tạo vẫn giữ `ROLE_DOCTOR` nên nhà của họ là `/doctor/dashboard`; nhánh HEAD_DOCTOR chỉ dành cho tài khoản admin cấp mỗi quyền trưởng khoa), cộng một nhánh phủ định cả ba làm lưới an toàn — **mất hẳn logo hỏng nặng hơn một liên kết trỏ sai** — **(B) một dòng `created_at` NULL làm sập cả trang `/doctor/manage-bookings`**: `b2.getCreatedAt().compareTo(b1.getCreatedAt())` ném NPE nên là **HTTP 500 cho toàn bộ trang** chứ không phải mất một dòng; `createdAt` mang `@CreationTimestamp` nên mọi dòng qua Hibernate đều có, nhưng cột **nullable** nên dữ liệu cũ / import / một câu SQL sửa tay lúc vận hành đều gài được quả mìn đó — chính nó lộ ra khi tôi chèn dữ liệu thử bằng SQL thô ở đợt trên; đổi sang `Comparator.comparing(Booking::getCreatedAt, Comparator.nullsLast(Comparator.reverseOrder()))` — **kiểm chứng trên jar đã đóng gói với DB thật**: 3 vai trò × 3 trang đều render **đúng 1 logo** trỏ đúng nhà của mình và bấm vào trả **200** (trước là 403); chèn một dòng `created_at` NULL thật rồi mở `/doctor/manage-bookings` → **200**, render **đủ 12/12 dòng** (không mất dòng nào), dòng NULL nằm **cuối bảng** còn 11 dòng kia giữ nguyên thứ tự mới-nhất-trước; `./mvnw clean package` **5/5 test xanh**; dữ liệu thử đã xoá, DB về đúng 63 lịch và phân bố 9/24/30 — **lỗi có sẵn khác tìm ra, KHÔNG sửa**: `error/404.html:45` và `:185` vẫn là `href="index.html"` của theme gốc, tức link chết — `doctor/include/header.html`, `DoctorBookingManagerController` — coding-conventions.md, progress-log.md
- 2026-08-22 — `fix:` **bật lại CSRF cho toàn ứng dụng và đóng 19 endpoint đổi trạng thái bằng GET** — món nợ đã ghi sẵn ở authentication-and-roles.md là *"the first thing to do after deployment"*, cố ý để ngoài đợt hardening 15/08 vì làm cùng lúc với đổi kho lưu phiên là cách chắc chắn nhất để hỏng một lần ra mắt; production vừa lên hôm 21-22/08 nên giờ mới tới lượt — **(A) thứ nguy hiểm nhất KHÔNG phải chỗ ai cũng nghĩ**: bật CSRF chỉ bảo vệ POST/PUT/DELETE, mà `SameSite=Lax` **VẪN gửi cookie phiên khi điều hướng cấp cao bằng GET** — nên **19 `@GetMapping` có ghi dữ liệu** mới là chỗ khai thác được chỉ bằng cách gửi một đường link cho admin: `AdminController:220` xoá tài khoản, `AdminPostController:115` xuất bản bài viết **kèm bắn thông báo tới TOÀN BỘ bệnh nhân**, cộng xoá/huỷ/xác nhận lịch hẹn, duyệt–từ chối–xoá ứng viên, xoá khoa/bác sĩ/dịch vụ/tin tuyển dụng, và `/user/cancel-booking/{id}` của bệnh nhân; đã đọc mã xác minh chúng ghi thật chứ không phải trang xác nhận, rồi chuyển hết sang `@PostMapping` + viết lại 23 thẻ `<a>` sống thành form POST inline (2 hit còn lại của grep nằm trong khối comment chết ở `booking-requests.html`) — **kiểm trước khi sửa: cả 23 chỗ đều ở `depth=0`**, không chỗ nào lồng trong một `<form>` khác, vì form lồng nhau là HTML không hợp lệ — **(B) blocker suýt bỏ sót: 6 link đăng xuất**. Khi CSRF bật, `LogoutFilter` chỉ còn khớp **POST** `/logout`, nên mọi `<a th:href="@{/logout}">` **im lặng ngừng đăng xuất** — không lỗi, không log — và 6 chỗ đó nằm trong **fragment dùng chung** nên phủ toàn bộ khu nhân viên; khuôn đúng đã có sẵn ở `user/include/header.html:172` (trang bệnh nhân xưa nay vẫn làm đúng), chép nguyên nó; chú thích ở `admin/include/sidebar.html` ghi "8 liên kết" nay sửa lại còn 6 và ghi rõ nợ đã trả — **(C) hai bẫy Spring Security 6, cả hai đều im lặng và cả hai đều phải vá mới chạy**: *token nạp trễ* — Spring 6 chỉ ghi cookie khi token được ĐỌC, nên trang không render form nào (đúng trang chủ bệnh nhân, chỉ có khung chat) không bao giờ nhận được cookie và `fetch` POST đầu tiên ăn 403 → thêm `config/CsrfCookieFilter` chạm `getToken()` mỗi request; và *mặt nạ XOR* — handler mặc định che token mỗi lần render còn `CookieCsrfTokenRepository` ghi token **thô** vào cookie, nên JS đọc cookie gửi lên header sẽ bị đem đi giải mặt nạ và trượt → `config/SpaCsrfTokenRequestHandler` rẽ theo nguồn (header → thô, form param → giải mặt nạ), giữ nguyên chống BREACH cho 48 form thay vì gỡ bỏ nó để chiều 13 lời gọi fetch — **(D) miễn trừ đúng MỘT chỗ**: `/api/payment/webhook`, Casso/SePay gọi server-to-server không phiên không cookie; **`permitAll()` KHÔNG cứu được** vì `CsrfFilter` chạy TRƯỚC tầng phân quyền, quên dòng này là tiền chuyển khoản của khách ngừng được ghi nhận mà không ai biết — **(E) chọn cookie thay vì thẻ meta**, và bị chính lựa chọn đó cắn một lần: token ở cookie thì JS đọc được ở mọi trang bất kể trang đó nạp fragment nào (lý do: dự án đã bị "fragment A có ở 31 trang, fragment B chỉ 12" cắn nhiều lần), lại không đi vào `SPRING_SESSION_ATTRIBUTES` nên tránh xa bẫy `MEDIUMBLOB` bị cắt cụt — nhưng lúc kiểm mới lòi ra dự án có **BA** fragment tên `pagehead` (admin/doctor/receptionist) chứ không phải một, và khu lễ tân đã bị bỏ sót đúng vì chỉ nhìn thấy cái của admin; nay cả ba cộng `user/include/header :: header-nav` đều nạp `assets/js/csrf.js` — **(F) 13 lời gọi fetch POST** gắn `MediTrustCsrf.headers()`, gom trong 7 tệp; 3 trợ lý AI trên form khám đi chung helper `postJson` nên chỉ sửa một chỗ; hai chỗ multipart (`chat-upload`, `allergy/add`) **chỉ thêm header, không đặt `Content-Type`** kẻo mất boundary; `assets-admin/js/doctor-ai-chat.js` là mã chết (không template nào nạp) nên bỏ qua — **(G) hai form KHÔNG có `action`** là chỗ Thymeleaf không chèn token: `th:formaction` trên `<button>` **không** kích hoạt bộ xử lý (chỉ `th:action` trên chính thẻ `<form>` mới có), và form được JS gán `action` lúc chạy thì phải chèn tay — bản chèn tay kèm guard `${_csrf != null}` để tắt CSRF đi vẫn không vỡ template, tức đường quay lui còn dùng được — **(H) thứ tự làm là để giảm rủi ro chứ không tuỳ tiện**: Phần 2–4 (logout, fetch, GET→POST) chạy bình thường khi CSRF **vẫn tắt**, nên làm và thử trước, rồi mới lật một dòng cấu hình — lỗi phát sinh ở giai đoạn đầu chắc chắn không phải do CSRF — **(I) kiểm chứng trên jar đã đóng gói**: 22/22 ca HTTP, trong đó có **phép kiểm âm bản** là thứ duy nhất chứng minh CSRF thật sự bật (có cookie phiên hợp lệ nhưng không token → **403**; URL gõ tay bằng GET → **405**), 4 vai trò đăng nhập + đăng xuất được, webhook không bị `CsrfFilter` chặn, khách **chưa đăng nhập** vẫn chat được (nhánh dễ trượt nhất vì trang chủ không có form nào); **459 form POST render ra trên 21 trang, 0 form thiếu token**; độ phủ `csrf.js` 37/38 URL của 5 vai trò; và bằng chứng dứt điểm cho `SpaCsrfTokenRequestHandler`: token form 96 ký tự (XOR) **và** token cookie 36 ký tự (thô) đều được chấp nhận, còn đặt token thô vào form param thì **bị từ chối 403** — `./mvnw clean package` **5/5 test xanh** — **CHƯA commit, CHƯA deploy**; **chưa mở bằng trình duyệt thật**: 25 nút vừa chuyển sang form POST mới kiểm ở tầng HTML/HTTP, chưa bấm tay để xem bố cục nút (`d-inline`, `.btn-group`, badge bật/tắt tin tuyển dụng) có xô lệch không — **lỗi CÓ SẴN tìm ra khi kiểm, không sửa vì ngoài phạm vi**: `/careers` là trang tuyển dụng công khai nhưng không có trong danh sách `permitAll` của `SecurityConfig` nên khách vãng lai bị đá về `/login` (302), đăng nhập vào thì 200 — `SecurityConfig` + `CsrfCookieFilter` + `SpaCsrfTokenRequestHandler` (mới) + `assets/js/csrf.js` (mới), 11 controller, 4 fragment header, 12 template danh sách, `staff/work-schedule.html`, `receptionist/schedule-change.html`, 5 tệp JS — authentication-and-roles.md, coding-conventions.md, progress-log.md
- 2026-08-22 — `chore:` **deploy commit `2bb1bfc` (sửa chatbot trả lời bác sĩ toàn viện) lên production** — lần đầu dùng trọn mục **7b của deploy/README.md** (nâng cấp máy đang chạy) kể từ khi mục đó được viết, và nó chạy đúng như mô tả — **(A) phép kiểm chặn đường cho kết quả RỖNG, và đó là tin tốt**: `git diff --stat e6e4ba1..2bb1bfc -- '*/model/*.java'` không trả về dòng nào, tức commit này **không đụng `@Entity` nào** nên **không cần `db/manual/*.sql`** và không dính cái bẫy đã làm sập production hôm 2026-08-21 (`DDL_AUTO=validate` không tạo bảng, thiếu bảng là crash-loop 502); toàn bộ thay đổi chỉ là 2 tệp JS + 1 chuỗi prompt trong `AiService` + tài liệu — **(B) đóng gói bằng `git worktree` chứ không build tại chỗ**, đúng lý do runbook đã ghi: thư mục làm việc còn `application.properties` (khoá dev đã xoay) và một tệp `.docx` chưa commit, build tại chỗ là nướng thẳng chúng vào jar; **bẫy Windows gặp thêm**: đặt worktree dưới thư mục scratchpad làm đường dẫn vượt MAX_PATH và `git` chết giữa chừng ở `assets-admin/vendor/chart.js/...` (`Filename too long`) — phải dùng thư mục ngắn (`/d/nnlbuild`) — **(C) thứ tự hạ thời gian chết xuống đúng một lần khởi động lại**: tải jar lên `/tmp` **trước** (chưa dừng gì), đối chiếu `sha256` hai đầu (khớp `5754d30c…`), chạy sao lưu DB + tệp **khi app cũ vẫn đang phục vụ** (44K + 14M), rồi mới `cp` jar cũ ra `.bak-<stamp>` → `stop` → đổi → `chown/chmod` → `start`; app lên sau **~9 giây**, `Started ... in 13.399 seconds`, `SchemaGuard` báo lược đồ đầy đủ, không một dòng ERROR — **(D) `cf-cache-status: BYPASS`, thứ suýt bị bỏ sót**: site đứng sau Cloudflare nên một tệp `/assets/js/ai-chat.js` bị cache ở biên sẽ khiến khách vẫn nhận JS CŨ dù jar đã đổi — kiểm thật thì header trả `Cache-Control: no-cache, no-store, must-revalidate` và `BYPASS`, tức không dính; **phải kiểm lại mỗi lần deploy có đổi tài nguyên tĩnh**, đừng tin là mặc định luôn như vậy — **(E) jar build từ worktree ra JS xuống dòng CRLF** (`core.autocrlf=true` nên git checkout ra CRLF), 236.843 byte so với 220.540 byte của bản build tại chỗ; **vô hại với trình duyệt** nhưng làm mọi bộ kiểm tự động cắt mã theo mốc `\n` trượt sạch — chuẩn hoá CRLF trước khi so là xong, đừng tưởng jar hỏng — **(F) kiểm chứng trên chính production qua HTTPS**: 5/5 trang công khai 200; **4/4 endpoint có bảo vệ trả 302** (`/api/chat/my-bookings`, `/api/chat/my-documents`, `/user/profile`, `/admin/dashboard`) tức luật khối 0 của `SecurityConfig` vẫn chặn người lạ; `filter?departmentId=14` trả 6/6 dòng Răng hàm mặt còn bỏ tham số ra thì 6 dòng thuộc 5 khoa (đúng thiết kế cho khởi động nguội); và **kịch bản đúng như khách báo, chạy với model thật trên production**: "sâu răng đau nhức" → khoa 14 bung thẻ → "bác sĩ nào tốt nhất?" → model tự điền `booking_target.department_id = 14` với `booking_intent` vẫn false, JSON đủ 10 key, thẻ in *"Em gợi ý anh/chị điểm đánh giá thật cao nhất trong khoa Răng hàm mặt ạ"*, 100% kết quả đúng khoa — **(G) đường quay lui** `app.jar.bak-20260823-022636` (jar `e6e4ba1`) còn nguyên trên máy chủ, quay lui không cần gỡ gì vì commit này không thêm bảng nào; đĩa còn 42G/48G — **CHƯA kiểm bằng tài khoản thật trên production**: nút "Xem bác sĩ khoa X" và chế độ gọi vẫn mới chỉ kiểm ở tầng HTTP/JSON, chưa mở bằng trình duyệt có đăng nhập — `deploy/README.md` mục 7b (quy trình đã dùng), không sửa mã nguồn — progress-log.md
- 2026-08-21 — `fix:` **khách hỏi "bác sĩ nào tốt nhất?" thì được trả lời bác sĩ giỏi nhất CẢ BỆNH VIỆN, không phải của khoa đang nói dở** — kể triệu chứng (hoặc gửi ảnh triệu chứng) xong, trợ lý đề xuất đúng chuyên khoa và bung dải thẻ bác sĩ của khoa đó; hỏi tiếp một câu so sánh thì bảng xếp hạng lại nhảy ra toàn viện — **(A) gốc rễ nằm ở ĐÚNG MỘT DÒNG**: `const deptId = bookingTarget.department_id || deptIds[0] || null`, mà **cả hai toán hạng đều đọc JSON của RIÊNG lượt này**; ở một câu hỏi nối tiếp trống trơn thì hai prompt rule tự đẩy model vào chỗ trống — mục 5D ra lệnh `booking_intent = false` nên model xoá luôn `booking_target` đi kèm, còn `recommended_departments` chỉ bị ép phát lại "KHI KHÁCH YÊU CẦU ĐẶT LỊCH" mà một câu hỏi thì không phải; `deptId` rơi về null, tham số bị bỏ, và `PatientChatLookupApiController` rơi xuống `doctorService.findAll()` — đo trên DB thật: có `departmentId=1` thì 6/6 dòng đều Tim mạch, bỏ nó ra thì 6 dòng thuộc **6 khoa khác nhau** — **(B) ba thứ làm nó vô hình**: `ai-chat.js` **không có bộ nhớ khoa nào cả** (ba biến sống qua lượt là `lastHandoffDate`/`lastAvailabilityDoctor`/`lastChosenDoctor`, `resolveDoctorFilter` không đọc cái nào), khoa nuôi dải thẻ là `const deptId` cục bộ trong vòng `for` nên chết theo handler; **thẻ kết quả không in tên khoa** nên câu trả lời toàn viện trông y hệt câu trả lời đúng; và "tốt" không khớp regex tiêu chí nào nên `sortBy` rỗng, máy chủ lặng lẽ xếp theo `experienceYears` trong khi thẻ chat in **không** tiêu chí nào còn loa lại đọc "em xếp theo số năm kinh nghiệm" — hai mặt của cùng một câu trả lời nói khác nhau — **(C) `lastDepartmentContext`, ghi ở ĐÚNG MỘT NƠI** (vòng lặp dựng dải thẻ, tức khoảnh khắc khách thật sự được cho XEM bác sĩ của một khoa), và **chỉ ghi khoa bung được thẻ** — khoa kín lịch trả mảng rỗng, ghi nó vào là trả lời câu sau bằng một khoa khách chưa từng thấy bác sĩ nào; thứ tự ưu tiên là một THANG BẬC chứ không tuỳ tiện: khách vừa nói rõ > bác sĩ khách đã **CHỌN** > thứ vừa được **HIỆN** ra > thứ khách mới chỉ **HỎI THĂM**, đúng thang bậc file này đã dùng cho bác sĩ — **(D) CỐ Ý không xoá nó trong `parseCancelIntent`** dù `lastChosenDoctor` bị xoá ở đó: cái kia ghim một CON NGƯỜI, còn khoa là ký ức CHỦ ĐỀ, cùng loại với `patient_summary` mà prompt bắt giữ qua mọi lần đổi chủ đề; nó tự đúng nhờ luật ghi đè — **(E) thẻ BẮT BUỘC nói ra phạm vi** ("trong khoa Răng hàm mặt" / "trong toàn bệnh viện"), đây là luật "every superlative must name its scope" có sẵn, áp vào đúng cái thẻ đã lọt lưới; `departmentName` lấy từ chính response (`buildProfile` đã trả sẵn) nên **không phải sửa máy chủ**, và **chỉ đọc khi thật sự có gửi `departmentId`** — không thì đó là khoa của người đứng đầu bảng toàn viện, tức một cái nhãn SAI dán lên câu trả lời đúng; nhánh "không tìm được ai" cũng phải nói, vì "cả khoa này không có ai" và "cả bệnh viện không có ai" là hai câu khác hẳn — **(F) nhiều khoa thì trả lời theo khoa đầu + nút "Xem bác sĩ khoa X"**, `describeFilterScope` **bắt buộc lọc chính khoa đang trả lời ra** (cùng lý do đã ghi cho `pickOtherDoctors`) và danh sách rỗng thì **không in gì**; nút tra lại **không gọi model** — danh sách do hệ thống tra ra, model chưa từng nhìn thấy, cùng nguyên tắc `resolveAlternativeChoice` — và đi bằng `data-*` + **một listener uỷ quyền** vì tên khoa tiếng Việt có dấu nháy làm vỡ chuỗi JS, còn đoạn chat thì được khôi phục từ `sessionStorage` nên nút gắn handler riêng sẽ chết sau khi tải lại trang — **(G) "tốt nhất" -> `rating` chứ không phải `experience`** (dù "giỏi nhất" vẫn là experience): `rating` là nhánh tự phòng hộ — người chưa ai chấm xuống cuối chứ không bị loại, mỗi dòng in kèm SỐ LƯỢT, và cả khoa chưa ai được chấm thì thẻ tự nói ra rồi lùi về xếp theo kinh nghiệm; thêm `tốt|tot` vào `DOCTOR_SUPERLATIVE` để "đặt lịch với bác sĩ tốt nhất" cũng ra bảng so sánh — cửa thoát `looksLikeDoctorFilterQuestion` trong `resolveBookingHandoff` nhờ đó chặn luôn được soft-lock 3 phút + đếm ngược chuyển trang cho câu đó — **(H) sửa prompt 5D là lớp GIA CỐ, không phải bản vá chính** (buộc model suy khoa từ ngữ cảnh và nói rõ điền `booking_target.department_id` KHÔNG có nghĩa là đang đặt lịch): bản vá chính là bộ nhớ ở trình duyệt, đúng doctrine "ở chủ đề mà prompt CẤM model suy luận thì tín hiệu của model là tín hiệu yếu hơn"; **không đổi schema** — không thêm key, không thêm giá trị enum, vẫn đủ 10 key nên `/skills/ai-schema-change` không kích hoạt — **(I) kiểm chứng trên jar đã đóng gói với model thật**: 21/21 ca logic (cắt thẳng mã nguồn ra chạy, không chép tay) + 18/18 ca end-to-end với JS **tải từ máy chủ đang chạy**; kịch bản đúng như khách báo — "sâu răng đau nhức" -> khoa 14 Răng hàm mặt bung thẻ -> "bác sĩ nào tốt nhất?" -> `departmentId=14`, 100% kết quả Răng hàm mặt, tiêu đề in "trong khoa Răng hàm mặt", tiêu chí `rating`; model thật nay tự điền `booking_target.department_id` ở lượt nối tiếp (14, và 1 ở kịch bản tim mạch) trong khi `booking_intent` vẫn false, JSON vẫn đủ 10 key và `suggested_prompts` vẫn xưng "Tôi"; khởi động nguội vẫn trả toàn viện nhưng **tự khai báo**; `./mvnw clean package` **5/5 test xanh** — commit `2bb1bfc`, **đã deploy production 2026-08-22** (xem mục ngay trên); **chế độ gọi và nút đổi khoa mới kiểm bằng JSON + đọc mã + `node --check`, chưa mở bằng trình duyệt thật** — `ai-chat.js` (`lastDepartmentContext`, `describeFilterScope`, `showDoctorFilterForDepartment`, listener uỷ quyền, `buildDoctorFilterHtml`, `DOCTOR_SUPERLATIVE`, `resolveDoctorFilter`), `meditrust-voice-call.js` (`describeDoctorFilter`), `AiService.java` (mục 5D) — ai-assistant.md, progress-log.md
- 2026-08-21 — `chore:`/`fix:` **deploy commit `e6e4ba1` lên production, và sửa lỗi làm ứng dụng KHÔNG khởi động được sau khi đổi jar** — **(A) lỗi chặn đường, lộ ra đúng lúc jar mới đã nằm trên máy chủ**: production chạy `DDL_AUTO=validate` (bước 4 của runbook bảo lật sang như vậy sau lần boot đầu), mà `validate` **chỉ đối chiếu chứ không tạo bảng** — commit này thêm hai entity mới (`ExternalMedicalRecord`, `AiImageUsage`) nên Hibernate ném `SchemaManagementException: missing table [ai_image_usage]`, context bị huỷ, và `Restart=always` của systemd biến thành **crash-loop trả 502 cho mọi khách**; đây là mặt trái của mọi cái bẫy khác trong environment-setup.md — chúng hỏng *im lặng*, cái này hỏng *toàn phần và ồn ào*, nhưng **sau** khi đã thay jar — **(B) ba thứ làm nó dễ mắc**: máy dev chạy `ddl-auto=update` nên bảng có ngay lúc viết entity, tức khiếm khuyết chỉ tồn tại trên đúng cái máy không thử được; **`deploy/.env` trong repo KHÔNG phải thứ máy chủ đang chạy** (repo ghi `update`/`SEED_ENABLED=true`, `/etc/nnlhospital/.env` thật là `validate`/`false`/`SCHEMA_STRICT=true`); và **`SchemaGuard` không bắt được** vì nó chỉ kiểm 4 đối tượng của `001`/`002`, nên vẫn báo "Lược đồ đầy đủ" ngay trước lúc Hibernate từ chối khởi động — **(C) cách sửa: LẤY DDL TỪ HIBERNATE chứ không gõ tay** — `validate` so cả tên lẫn kiểu cột nên một bản gõ tay sẽ trượt đúng như cũ; database dev đã chứa sẵn thứ Hibernate muốn, nên `SHOW CREATE TABLE` trên dev **chính là** bản migration, chỉ bỏ `AUTO_INCREMENT=` và `COLLATE=` để bảng thừa kế mặc định của database (khớp `utf8mb4_unicode_ci` với mọi bảng production khác) → `db/manual/003_external_medical_records.sql`; **cố ý KHÔNG lật máy chủ về `update`** để chữa cháy: làm vậy là mở lại toàn bộ trang bẫy của `update` chỉ vì một cái bảng, và cờ đó sẽ nằm sai im lặng cho tới lần bất ngờ sau — **(D) đóng gói bằng `git worktree` chứ không build tại chỗ**, vì thư mục làm việc đang có thay đổi chưa commit (khoá dev đã xoay + tệp .docx) sẽ bị nướng vào jar, và `spring-boot:run` đang chạy ở cổng 8090 thì `mvn package` ghi đè `target/classes` ngay dưới chân nó — **(E) kiểm chứng end-to-end trên production qua HTTPS**: 5/5 test xanh, manifest có `Main-Class`, sha256 của jar khớp hai đầu, sao lưu DB+tệp trước khi đổi, khởi động lại sạch trong **14,095 giây**, `SchemaGuard` báo lược đồ đầy đủ, `/actuator/health` **200** (không phải 302), `/` và `/doctors` 200, `/api/chat/doctors/filter` 200, còn `/api/chat/my-documents` + `/api/chat/my-bookings` + `/user/medical-document/list` + `/user/profile` đều **302** — tức luật khối 0 của `SecurityConfig` thật sự đang chặn, hồ sơ sức khoẻ không lộ cho người lạ; nút kẹp giấy `ai-chat-attach-btn` có trong HTML đã phục vụ và `ai-chat.js` trên production chứa đủ `chat-upload`/`my_documents`/`buildMyDocumentsHtml`/`buildEmergencyCardHtml`/`skipNextDocumentsCard` — **CHƯA kiểm bằng tài khoản thật**: luồng tải hồ sơ lên, gửi ảnh triệu chứng và hạn mức 10 ảnh/ngày mới chỉ kiểm ở tầng phân quyền chứ chưa đăng nhập chạy thật trên production — `db/manual/003_external_medical_records.sql` (mới), `deploy/README.md` (thêm mục 7b — nâng cấp máy đang chạy) — environment-setup.md, progress-log.md
- 2026-08-21 — `feat:`/`fix:` **khách gửi ẢNH CHỖ ĐANG BỊ ĐAU vào khung chat để hỏi chuyên khoa; kèm sửa nhánh cấp cứu và thêm hạn mức AI đầu tiên của dự án** — **(A) lỗi phải sửa trước khi làm được tính năng**: nút kẹp giấy đang coi MỌI ảnh là giấy tờ y tế, nên ảnh mắt sưng chạy qua prompt "đọc hồ sơ bệnh án", model trả đúng như prompt dặn (*"đây không phải giấy tờ y tế"*), rồi `applyAiResult` chỉ kiểm `summary` rỗng hay không nên **vẫn lưu `aiStatus = DONE`** — dòng đó sau đó được tiêm vào system prompt **mọi lượt chat** dưới tiêu đề "HỒ SƠ BỆNH ÁN TỪ NƠI KHÁC" và hiện luôn trên form khám của bác sĩ; `applyAnalysis` nay từ chối đặt `DONE` cho bất cứ thứ gì không phải `DOCUMENT`, vì `DONE` chính là tấm vé đi vào tiền sử bệnh nhân — **(B) MỘT lượt gọi thị giác vừa phân loại vừa phân tích** (`kind` ∈ DOCUMENT|SYMPTOM|OTHER): tách làm hai lượt là nhân đôi chi phí và độ trễ cho mọi tấm ảnh trong khi model nhìn một lần là biết cả hai; `analyzeDocumentImage` đổi tên thành `analyzeImage` vì nó vốn không biết gì về loại nội dung — **(C) thứ tự PHÂN TÍCH TRƯỚC, LƯU SAU là ràng buộc chứ không phải tối ưu**: đó là thứ duy nhất làm cho lời hứa "ảnh triệu chứng không bao giờ được lưu" thành sự thật — bytes đi thẳng từ `MultipartFile` tới model qua overload `toImageDataUrl(byte[])` mới, và **chỉ nhánh DOCUMENT mới ghi ra đĩa**; ghi-rồi-xoá yếu hơn hẳn vì mọi nhánh lỗi ở giữa đều để lại một tệp mồ côi trong thư mục riêng tư — **(D) nhánh DOCUMENT không gọi AI lần hai**: kết quả đã có sẵn từ lượt phân loại, `applyAnalysis` chỉ ghi lại; gọi `analyze()` nữa là trả tiền hai lần cho đúng một tấm ảnh — **(E) `appendAssistantNote` thay cho khối tiêm ngữ cảnh**: ảnh không lưu ở đâu cả nên không có gì để tiêm, thiếu nó thì ngay lượt sau khách hỏi *"nó có nguy hiểm không?"* model đã không còn biết vừa nhìn thấy gì; cửa sổ phát lại 6 tin nhắn cuối làm ghi chú tự rụng, đúng ý — **(F) sửa nhánh cấp cứu của khung chat gõ chữ**, một lỗ hổng CÓ SẴN mà tính năng ảnh làm nguy hiểm hơn hẳn: nó chỉ prepend một dòng chữ đỏ rồi **chạy tiếp** — vẫn dựng thẻ bác sĩ có link đặt lịch, vẫn vào `resolveBookingHandoff`, vẫn bật đếm ngược **tự nhảy sang `/appointment` sau 5 giây**; nay in thẻ đỏ có `tel:115`, `cancelRedirect()` rồi **`return`** (chặn ở dòng 2763, trước vòng lặp khoa ở 2776 và chốt lịch ở 2857), dùng chung cho cả ảnh lẫn chữ; chế độ gọi (`enterEmergency`) vốn đã làm đúng, đây là kéo khung chat về ngang với nó — **cố ý không hardcode id khoa 21** vì cả dự án không special-case 21/22 và không ai "đặt lịch" cấp cứu — **(G) hạn mức AI đầu tiên của dự án**: trước đó **không có bất kỳ throttle nào** trên `/api/chat/**`, mỗi ảnh là một lượt gọi model trả tiền, một vòng lặp đốt sạch `OPENROUTER_API_KEY` dùng chung và chat hỏng cho MỌI người; `MAX_IMAGE_ANALYSES_PER_DAY = 10` theo khuôn `whyCannotBookWithoutPayment`, bộ đếm ở bảng `ai_image_usage` (**bảng chứ không phải map trong bộ nhớ**: hạn mức reset mỗi lần restart thì chỉ cần restart là lách được, và sai khi chạy nhiều instance) — **(H) hai lỗi bắt được lúc kiểm chứng, không nằm trong kế hoạch**: `@Transactional` đặt trên `countOneUse` **hoàn toàn vô hiệu** vì nó được gọi từ cùng bean nên không đi qua proxy Spring — gỡ bỏ và ghi rõ lý do tại chỗ, dựa vào transaction riêng của `repository.save`; và **nhánh PDF qua được phép kiểm hạn mức mà không tăng bộ đếm** (đo thật: 4 lượt tải nhưng bộ đếm chỉ ghi 3), tức vòng lặp gửi PDF chạy vô hạn — thêm `countOneAnalysis` công khai cho nhánh đó gọi tay — **(I) `urgency` lạ hạ về `SOON` chứ không phải `ROUTINE`**: model gõ sai chính tả mà bị tụt xuống mức nhẹ nhất là sai đúng hướng nguy hiểm; prompt còn **cấm trấn an** ("không sao đâu") và buộc **khi phân vân chọn mức nặng hơn** — **(J) `department_ids` đối chiếu `DepartmentRepository`** trước khi vẽ thẻ; đường chat vốn không kiểm gì, model bịa id nào cũng lọt — **(K) kiểm chứng end-to-end trên jar đã đóng gói với model thật**: ảnh giấy khám → `DOCUMENT` + lưu + khoa Tim mạch; **ảnh mắt sưng → `SYMPTOM`, "mắt trái", "kết mạc đỏ", khoa Nhãn khoa, urgency SOON**; chân dung bình thường → `OTHER`; PDF → `DOCUMENT` qua PDFBox; **ảnh triệu chứng để lại 0 dòng DB và 0 tệp trên đĩa** (kiểm bằng SQL + `ls`); hỏi *"nó có nguy hiểm không?"* ở lượt sau thì `reasoning` của model nhắc lại đúng "ảnh chụp mắt trái với dấu hiệu kết mạc đỏ" từ tấm ảnh **chưa từng được lưu**, và định tuyến khoa 6 (Nhãn khoa); hạn mức: ảnh thứ 10 qua, **ảnh thứ 11 bị chặn** kèm đúng câu tiếng Việt; mô tả cấp cứu bằng chữ → `is_emergency = true` + khoa 21; `./mvnw clean package` **5/5 test xanh** — **CHƯA commit, CHƯA deploy**; **thẻ đỏ cấp cứu và thẻ triệu chứng chưa mở bằng trình duyệt thật** (kiểm bằng JSON + đọc mã + `node --check`), và **nhánh ảnh cho ra EMERGENCY chưa dựng được ảnh thử đủ thuyết phục** nên mới kiểm bằng đường gõ chữ — `ChatImageService(+Impl)` + `ImageAnalysis` + `AiImageUsage` + `AiImageUsageRepository` (mới), `AiService` (`analyzeImage`, `appendAssistantNote`), `DocumentTextExtractor(+Impl)` (overload `byte[]`), `ExternalMedicalRecordService(+Impl)` (`applyAnalysis`), `UserMedicalDocumentController` (cửa hợp nhất), `ai-chat.js` (thẻ cấp cứu, thẻ triệu chứng, rẽ nhánh `kind`), `user/include/ai-chat.html` — ai-assistant.md, medical-records.md, code-structure.md, coding-conventions.md, environment-setup.md
- 2026-08-21 — `fix:` **hai lỗi CSS của lớp phủ admin, cả hai đều thuộc loại "không báo lỗi, chỉ trông sai"** — **(A) chữ nút trùng đúng màu nền nút**: `.admin-theme a { color: var(--at-primary) }` có độ ưu tiên **(0,1,1)** nên thắng `.btn-primary` của Bootstrap **(0,1,0)** — mọi `<a class="btn btn-primary">` trong khu admin ra chữ teal trên nền teal, đọc được đúng bằng không; mà `.btn-primary:hover` là **(0,2,0)** nên nó thắng ngược và chữ **chỉ hiện lúc rê chuột**, tức đi kiểm tra thì thấy đúng còn người dùng thật thì thấy nút trống trơn; đo bằng computed style: `color=rgb(0,107,99)` `bg=rgb(0,107,99)`, sau khi đổi thành `a:not(.btn)` thì `color=rgb(255,255,255)` — **(B) `height:100%` ăn mất `margin-bottom` của chính thẻ**: `.stat-tile` cao đúng bằng cột nên chiều cao HÀNG tính theo border-box của cột, còn `margin-bottom: 30px` tràn ra ngoài hàng và đè lên hàng sau — đo thật: thẻ cao 147,5px / cột cao 148px / **gap giữa hàng thẻ và hàng biểu đồ = 0px**, nên nửa trang thưa đúng còn hàng thẻ thì dính chặt vào biểu đồ bên dưới; sửa bằng `margin-bottom: 0` trên thẻ + `mb-4` trên 8 cột chứa thẻ, **không** bù bằng `calc()` vì chiều cao phần trăm giải trong hàng flex là chỗ dễ vỡ — kiểm chứng lại trên jar đã đóng gói bằng computed style + đo khoảng cách qua headless Chrome, `./mvnw clean package` 5/5 test xanh — `assets-admin/css/admin-theme.css`, `admin/dashboard.html` — coding-conventions.md
- 2026-08-21 — `feat:`/`fix:` **đính kèm hồ sơ bệnh án cũ NGAY TRONG KHUNG CHAT (hướng thứ hai), và sửa xưng hô ở mọi câu được gửi đi như lời của KHÁCH** — **(A) lỗi xưng hô rộng hơn chỗ được báo**: nút "Phân tích & tư vấn khoa" gửi đi câu *"Phân tích giúp **em** hồ sơ… **em** đã tải lên"*, tức bệnh nhân tự gọi mình là "em" trong chính bóng chat của họ — đọc như trợ lý đang tự nói chuyện với chính nó; rà tiếp thì **`suggested_prompts` do model sinh cũng dính y hệt** (quan sát thật: *"Em đang uống thuốc gì?"*), vì mục 0 của prompt chỉ ràng buộc `ai_reply` và `speech_reply` chứ chưa bao giờ nói gì về mảng đó — mà mảng đó là **lời của BỆNH NHÂN**, bấm vào là gửi đi như chính họ vừa gõ; nay mục 0 có luật riêng cho nó, **và nhắc lại ngay tại chỗ khai schema** vì đó mới là chỗ model nhìn khi sinh mảng; kiểm bằng model thật: `ai_reply` vẫn "em/anh chị", `suggested_prompts` đổi thành *"Tôi đau âm ỉ quanh rốn"* — **(B) hướng thứ hai: nút kẹp giấy trong khung chat** → `POST /user/medical-document/chat-upload` trả JSON; **cùng service, cùng chỗ lưu, cùng `whyCannotView`** nên hồ sơ gửi ở đây vẫn hiện trong trang hồ sơ và bác sĩ vẫn thấy — chỉ khác chỗ bắt đầu; **cố ý KHÔNG đặt dưới `/api/chat/**`**: tiền tố `/user/medical-document/**` đã `authenticated()`, còn `/api/chat/**` là `permitAll` nên mỗi endpoint đặt vào đó lại phải nhớ thêm một dòng ở khối 0 — quên một lần là mở đường **GHI** dữ liệu cho người lạ — **(C) lỗi bắt được lúc kiểm chứng, không có trong kế hoạch: "phiên hết hạn" bị báo thành "lỗi mạng"** — bản đầu xét `res.status === 401`, nhưng Spring Security chặn **trước** controller và trả **302** sang `/login`, `fetch` đi theo redirect rồi trả trang đăng nhập với **status 200**, nên nhánh 401 chưa từng chạy và mọi lượt hết phiên rơi vào catch báo *"không kết nối được"* — khách sẽ đi kiểm tra wifi; nhận diện bằng `res.redirected || res.url.indexOf('/login')`, đúng cách `/checkout-qr` đã phải làm, và sửa luôn javadoc vốn đang khẳng định sai rằng khách chưa đăng nhập nhận 401 — **(D) `skipNextDocumentsCard`, cờ sống đúng MỘT lượt**: câu tự gửi sau khi tải lên **bắt buộc** phải nhắc tới hồ sơ, đo bằng model thật — bỏ vế đó đi và hỏi trống *"Tôi nên khám chuyên khoa nào?"* thì model coi là mô tả mông lung và rơi về **khoa 22 (Y học gia đình)** thay vì đọc khối tiền sử đã tiêm (có vế đó thì ra **khoa 1 Tim mạch**, đúng); nhưng chính vế đó khớp `looksLikeMyDocumentsQuestion` nên thẻ danh sách hồ sơ in lại ngay dưới thẻ vừa in cách hai bóng chat — cờ cắt đúng một lần lặp đó và `sendMessage` **đọc rồi xoá ngay**, giữ lâu hơn là nuốt mất thẻ của câu hỏi thật ở lượt sau — **(E) ba chi tiết nhỏ mà hỏng thì không báo lỗi**: `isSending` dùng CHUNG với `sendMessage` (đang đọc hồ sơ mà khách gửi tiếp là hai lượt cùng ghi một `sessionId`); `fileInput.value = ''` sau mỗi lượt, thiếu nó thì chọn LẠI đúng tệp vừa rồi không kích hoạt `change` và nút trông như chết; và luật CSS nút mới phải viết `.ai-chat-input-area button.ai-chat-attach-btn` — luật cũ `.ai-chat-input-area button` là (0,1,1) nên một selector chỉ có class (0,1,0) **thua**, nút đính kèm sẽ ra tròn xanh y hệt nút Gửi — **(F) `toCard` đưa từ controller xuống service** vì nay có HAI endpoint trả cùng hình dạng đó mà trình duyệt lại đọc theo TÊN KHOÁ; hai bản sao là hai chỗ để một bên đổi tên còn bên kia thì không, và thẻ chat lặng lẽ in "undefined" — **(G) kiểm chứng end-to-end trên jar đã đóng gói với model thật**: đính kèm ảnh → `DONE` + gợi khoa Tim mạch; đính kèm PDF → `DONE`; `.exe` → **400 kèm đúng câu tiếng Việt** của `FileStorageService`; chưa đăng nhập → 302 chứ không phải 401 (chính là (C)); luồng đầy đủ tải lên → câu tự gửi → model trả `recommended_departments: [1]` để khung chat vẽ thẻ bác sĩ khoa Tim mạch; 2/2 ca định tuyến và 3/3 ca cờ một-lượt qua node; `./mvnw clean package` **5/5 test xanh** — **CHƯA commit, CHƯA deploy**; **nút kẹp giấy và các thẻ mới chưa mở bằng trình duyệt thật** (kiểm bằng HTML đã render + node) — `UserMedicalDocumentController`, `ExternalMedicalRecordService(+Impl)`, `PatientChatLookupApiController`, `AiService` (mục 0 + schema `suggested_prompts`), `ai-chat.js`, `user/include/ai-chat.html`, `user/profile.html` — ai-assistant.md, medical-records.md, coding-conventions.md
- 2026-08-21 — `feat:` **bệnh nhân tải lên hồ sơ bệnh án cũ từ nơi khác; AI đọc để tư vấn chuyên khoa, bác sĩ xem trước khi khám** — người chuyển từ viện khác/tuyến dưới sang trước nay **không có đường nào** đưa tiền sử vào hệ thống: `AiService.chatWithMemory` chỉ tiêm được bệnh án của lần khám `COMPLETED` gần nhất **tại chính viện này** (rỗng với mọi bệnh nhân mới), còn `showCreateForm` của bác sĩ cũng chỉ dựng timeline nội viện — **(A) `MedicalAttachment` KHÔNG tái sử dụng được, dù nghe như đúng chỗ**: entity đó khai `@JoinColumn(name="medical_record_id", nullable=false)` nên bắt buộc phải có một `MedicalRecord`, thứ chỉ tồn tại SAU khi bác sĩ viện này khám xong — hồ sơ cũ tồn tại TRƯỚC mọi lịch hẹn; tiện thể xác nhận entity đó là **mã chết hoàn toàn** (`findByMedicalRecordId` không có lời gọi nào, chỗ ghi duy nhất được gọi với `null` kèm comment "sẽ tích hợp sau"); entity mới `ExternalMedicalRecord` khoá thẳng vào `User`, **không `@AllArgsConstructor`**, mọi cột mới nullable hoặc có giá trị khởi tạo phía Java — **(B) hai đường đọc nội dung, và nhánh PDF là chỗ dễ nói dối nhất**: ảnh → thu nhỏ 1600px + base64 → model thị giác; PDF → PDFBox. **PDF bản scan trả chuỗi RỖNG** (không có lớp chữ), mà đưa chuỗi rỗng cho model là in ra một bản "tóm tắt" **bịa đặt** dưới tên hồ sơ bệnh án — nay ghi `UNREADABLE` kèm câu mời chụp ảnh từng trang; ngưỡng là `MIN_MEANINGFUL_CHARS = 60` chứ không phải `isBlank()` vì bản scan hay còn vài ký tự watermark, đủ qua phép thử rỗng nhưng không đủ để tóm tắt gì — **(C) thu nhỏ ảnh trước base64 là BẮT BUỘC**: giới hạn tải lên 10MB, base64 phình ~33%, một ảnh chụp bằng điện thoại thành request ~13MB và đốt token vô ích; dùng `imgscalr` vốn đã nằm trong `pom.xml` từ lâu mà **không ai dùng** (`service/ImageService` bị comment toàn bộ) nên không phát sinh dependency; PDFBox là dependency duy nhất thêm mới — OpenPDF sẵn có chỉ GHI, không có `PdfReader` nào — **(D) `analyzeDocumentImage` KHÔNG đi qua `AiMessage`**: trường `content` của lớp đó là `String` thuần và chính nó được tuần tự hoá vào `AiChatSession.chatHistoryJson`, đổi sang `Object` để nhét mảng multipart vào là làm hỏng **mọi phiên chat đang lưu trong DB**; payload dựng tay bằng `Map`, và `postOnce(...)` được tách khỏi `callModels` để hai loại request dùng chung **một** chỗ ghi log — kể cả nhánh `getError()`, thứ bắt lỗi hết-credit mà OpenRouter trả về kèm **HTTP 200** — **(E) `analyze()` và `upload()` đều KHÔNG `@Transactional`**: cái trước có lời gọi mạng giữa hàm (giam connection HikariCP, pool 10), cái sau ghi tệp ra đĩa mà rollback không thu hồi được — nên ghi tệp trước, lưu dòng sau, và **xoá tệp nếu lưu hỏng**, bằng không mỗi lỗi để lại một tệp mồ côi trong thư mục riêng tư — **(F) quyền xem gom vào `whyCannotView`**: chủ hồ sơ, hoặc bác sĩ **đã có lịch hẹn** với bệnh nhân đó; cố ý dò qua bảng `doctors` chứ **không đọc `User.roles`** (`@ManyToMany` LAZY — một lần chạm ngoài session là `LazyInitializationException` ở đúng chỗ đang gác quyền); **chỉ MỘT endpoint tải tệp** dùng chung cho cả hai đối tượng dù đường dẫn mang tiền tố `/user`, vì tách theo đối tượng là tạo ra hai bản kiểm quyền có thể lệch nhau — **(G) `/api/chat/my-documents` phải khai ở KHỐI 0** của `SecurityConfig`, y hệt `/api/chat/my-bookings`: `/api/chat/**` nằm trong `permitAll` ở dưới và Spring lấy luật khớp **đầu tiên**, khai dưới đó là hoàn toàn vô tác dụng và giao hồ sơ sức khoẻ cho người lạ — **(H) prompt: THÊM GIÁ TRỊ vào enum `lookup.type`, không thêm key** nên số key vẫn là **10** và `/skills/ai-schema-change` không kích hoạt (tiền lệ `sort_by=rating`); kèm một gạch đầu dòng ở 5B buộc model coi đây là **tiền sử tham khảo có thể đọc sai**, không được trình bày như chẩn đoán của chính nó — **(I) `escapeHtml` sinh ra cho nhánh này**: các thẻ tra cứu khác nội suy thẳng vì dữ liệu là tên bác sĩ/tên khoa lấy từ DB của mình, còn đây là chữ model đọc ra từ **một tệp người lạ tải lên** đi thẳng vào `innerHTML` của trang đã đăng nhập — **(J) hai lỗi bắt được lúc kiểm chứng, không nằm trong kế hoạch**: `looksLikeMyDocumentsQuestion` bản đầu chỉ liệt kê dạng **không dấu** nên trượt sạch khách gõ đủ dấu — `normalizeText` chỉ hạ chữ thường và gộp khoảng trắng, nó **không bỏ dấu** (đưa cả câu qua `stripDiacritics` thì lại biến "sáng" thành giới từ "sang", đúng bẫy đã ghi sẵn), nay liệt kê cả hai dạng như `looksLikeMyBookingQuestion`; và hai `th:text` viết dạng `${x != null} ? A : B` — đúng cái bẫy SpEL đã ghi ở coding-conventions.md, toàn bộ ternary phải nằm TRONG một cặp `${...}` — **(K) kiểm chứng end-to-end trên jar đã đóng gói với model thật**, không phải chỉ biên dịch được: ảnh chụp giấy khám → đọc đúng huyết áp 150/95, cholesterol 6.8, chẩn đoán và cả 3 thuốc, gợi khoa **Tim mạch (id có thật)**; PDF có chữ → `DONE`; PDF bản scan → `UNREADABLE` đúng câu mời chụp lại chứ **không** bịa tóm tắt; `.exe` bị từ chối và **không có tệp nào rơi vào đĩa**; `reasoning` của model chứng minh khối tiêm ngữ cảnh tới nơi và model **tự nói** nó không được trình bày như chẩn đoán, đồng thời tự điền `lookup.type = my_documents`; **quyền xem**: chủ hồ sơ 200, bệnh nhân khác 404, bác sĩ **có** lịch hẹn 200, bác sĩ **không** có lịch hẹn 404; xoá gỡ **cả** dòng DB lẫn tệp trên đĩa, người lạ xoá thì không mất gì; `MEDICAL_DOC_AI_ENABLED=false` → tệp vẫn lưu, `PENDING`, **0 lượt gọi AI**; 14/14 ca kiểm thử regex qua node; `./mvnw clean package` **5/5 test xanh** — **CHƯA commit, CHƯA deploy**; **giọng nói mới chỉ kiểm bằng đọc mã** (cần trình duyệt thật) — `ExternalMedicalRecord` + `ExternalMedicalRecordRepository` + `ExternalMedicalRecordService(+Impl)` + `DocumentTextExtractor(+Impl)` + `UserMedicalDocumentController` (mới), `AiService` (`postOnce`/`analyzeDocumentImage`/khối tiêm ngữ cảnh/prompt), `FileStorageService(+Impl)`, `BookingRepository`, `PatientChatLookupApiController`, `SecurityConfig`, `ProfileController`, `DoctorMedicalRecordController`, `pom.xml`, `application.properties`, `ai-chat.js`, `meditrust-voice-call.js`, `user/profile.html`, `doctor/medical-record-form.html`, `doctor/patient-records.html` — medical-records.md, ai-assistant.md, authentication-and-roles.md, code-structure.md, environment-setup.md, build-and-run.md
- 2026-08-20 — `feat:`/`fix:` **thiết kế lại dashboard admin + sidebar; hai thẻ tiền in ra chữ `null đ` là BA lỗi chồng nhau chứ không phải một** — **(A) lỗi nặng nhất là một vị từ KHÔNG BAO GIỜ đúng**: `sumTotalRefund()` lọc `status = CANCELED AND paymentStatus = 'PAID'`, nhưng **mọi** đường huỷ đều ghi đè `paymentStatus` trong cùng transaction (`BookingServiceImpl:255,265` → `REFUNDED`; `:268` → `FAILED`; `ProfileController:205,208` → `REFUNDED`; `PaymentController:107` → `FAILED`; `BookingController:140` → `FAILED`; `BookingCleanupTask:33` → `EXPIRED`) — tổ hợp đó khớp **0 dòng trên 62 lịch hẹn** và sẽ không bao giờ khớp, nên thẻ "Tiền hoàn trả" trả NULL vĩnh viễn kèm trend "không đổi 0.0%" từ ngày viết — **(B) thiếu `COALESCE`**: cả bốn truy vấn tiền đều `SUM` trần, mà `SUM` trên 0 dòng trả NULL trong SQL, nên trên **database chưa phát sinh giao dịch** — tức production ngay sau lần deploy 19/08 — thẻ "Tổng tiền đặt cọc" **cũng** ra `null`; khuôn đúng đã nằm sẵn ở `LeaveRequestRepository.sumApprovedDays` — **(C) `paymentStatus` là TRẠNG THÁI SỐNG chứ không phải sự kiện lịch sử**: lọc `= 'PAID'` nên một lịch đã thu tiền rồi hoàn lại chuyển sang `REFUNDED` và **số tiền đó biến mất khỏi tổng đã thu**; đo trên DB dev: thẻ in 10.300.000 trong khi thực tế đã thu 11.800.000, tức **1.500.000 bốc hơi không dấu vết** — nay "đã thu" gồm cả `('PAID','REFUNDED')`, và **"thực thu" cố ý KHÔNG có truy vấn riêng** mà được trừ trong Java từ đúng hai số kia, đó là thứ bảo đảm ba thẻ trên màn hình luôn cộng trừ khớp nhau kể cả khi dữ liệu lệch — **(D) sổ ví là kiểm toán ĐỘC LẬP, không phải nguồn doanh thu**: `payWithWallet` chỉ chạy ở nhánh WALLET nên VNPay và chuyển khoản **không bao giờ** ghi vào `wallet_transactions`, ngược lại `refundToWallet` chạy ở mọi đường hoàn tiền nên sổ ví **là** bản ghi hoàn tiền đầy đủ — nó bắt được **300.000đ mà bảng lịch hẹn đã mất** (ví 1.800.000 / 6 giao dịch, bookings 1.500.000 / 5 dòng); in cạnh nhau ở dòng đối soát chứ tuyệt đối không trộn vào phép tính — **(E) bố cục mới theo thứ tự "cần làm gì" trước "báo cáo"**: khối *Việc cần xử lý* toàn thời gian (mỗi dòng chỉ tồn tại khi số > 0, cạn thì hiện biên nhận trạng thái khoẻ) → 4 thẻ tiền tự đối soát → 4 thẻ vận hành → 4 biểu đồ → 2 bảng; **cố ý tính cảnh báo TOÀN THỜI GIAN** vì một lịch treo từ tháng 6 không được phép biến mất chỉ vì đang xem kỳ 7 ngày; **bỏ hẳn 4 thẻ luỹ kế** ("Tổng số Bệnh nhân/Bác sĩ/Lịch hẹn") xuống một dòng chân — một con số giống hệt nhau ở hai lần tải trang không dẫn tới quyết định nào; và **không làm thẻ "hôm nay" cỡ lớn** vì trên DB dev cả bốn đều bằng 0, bốn số 0 chiếm hết tầm mắt thì trang trông như hỏng — **(F) 4 việc khối cảnh báo phát hiện mà trước nay không màn hình nào hiển thị**: 29 lịch `CONFIRMED` đã qua ngày hẹn chưa đóng (47% tổng số, 10.450.000đ, cũ nhất 03/06), 16 lịch bỏ dở lúc thanh toán (25,8%, 6.700.000đ), 3.300.000đ chưa thu, và **5 dòng `appointment_type` hỏng mã ký tự** (hex `443F636820763F` = `D?ch v?`, di chứng lỗi `characterEncoding` sửa hôm 19/08) đang tự tách thành một nhóm "Dịch vụ" thứ hai trong mọi thống kê — **(G) biểu đồ theo ngày phải ZERO-FILL**: truy vấn gộp không trả dòng nào cho ngày trống nên bản cũ **nhảy cóc** qua ngày không có lịch hẹn — kỳ 7 ngày chỉ vẽ được **1 điểm**; nay service điền 0 trước khi đưa vào model, kiểm chứng: đúng 7 điểm, tổng 5 lượt — **(H) `record` KHÔNG dùng được cho DTO đọc từ Thymeleaf**: SpEL `ReflectivePropertyAccessor` tìm `getX()` chứ không tìm accessor kiểu record `x()`; đổi sang Lombok `@Getter` cho khớp cả khuôn sẵn có của dự án — đây là trang đích sau khi đăng nhập, không đáng đánh cược vào một chi tiết của tầng biểu thức — **(I) `activePage` bằng interceptor chứ không sửa 9 controller**: sidebar cũ hardcode trạng thái active (Dashboard vĩnh viễn sáng, 11 mục còn lại vĩnh viễn tối, đi đâu cũng không đổi); `AdminNavInterceptor` map tiền tố URL ở MỘT chỗ nằm cạnh chính cái sidebar nó phục vụ, thay cho ~29 điểm `addAttribute` rải rác mà mỗi trang thêm sau lại là một chỗ nữa để quên — **bắt buộc `postHandle`, không phải `@ControllerAdvice`**: `AdminCandidateController.downloadCv` trả `ResponseEntity<Resource>` nên một `@ModelAttribute` sẽ chạy 2 câu `COUNT` cho **mỗi lượt tải CV** rồi vứt đi, và `@ModelAttribute` chạy **trước** handler nên một truy vấn hỏng ở đó là HTTP 500 cho **mọi** URL admin (dự án không có global exception handler); **bắt buộc tiền tố `nav`** trên mọi thuộc tính vì `AbstractView` trộn FlashMap **trước** model của handler — trùng tên `errorMessage`/`successMessage` là nuốt sạch thông báo trên mọi redirect admin, không một dòng log — **(J) lớp phủ giao diện phải bọc `body.admin-theme`**: `admin/include/header :: pagehead` đang được **31** template dùng chung, không chỉ 17 trang admin mà cả 6 trang doctor, 4 trang head, 3 trang staff và `user/medical-record-detail.html` — một trang **bệnh nhân** nhìn thấy; nạp CSS mới không điều kiện là restyle luôn cả trang bệnh nhân, nên toàn bộ 81 selector đều bọc và 17 tệp admin được gắn `class="admin-theme"` — **(K) hai lỗi khác tìm ra khi rà**: mục "Hồ sơ cá nhân" của sidebar admin trỏ `/profile` là **link chết 404** (`ProfileController` khai `@RequestMapping("/user")` + `@GetMapping("/profile")` = `/user/profile`; `doctor/include/sidebar.html:89` dính đúng lỗi này, sửa luôn cả hai — nó im lặng vì cùng mục đó đã có sẵn trong dropdown avatar cách đó hai centimet), và `/api/dashboard/**` **không có luật phân quyền riêng** nên rơi xuống `anyRequest().authenticated()`, tức bệnh nhân đã đăng nhập cũng đọc được thống kê phòng khám — chuyển sang server-render số liệu qua `th:inline` làm `DashboardApiController` thành mã chết, **xoá nó là đóng luôn lỗ hổng** mà không cần thêm matcher — **(L) kiểm chứng end-to-end trên jar đã đóng gói, không phải chỉ biên dịch được**: 4 con số tiền khớp **chính xác** SQL đối chứng (11.800.000 / 1.500.000 / 10.300.000 / 3.300.000), `grep -c null` trên HTML đã render = **0**, 6 bộ lọc `/admin/manage-booking?filter=` khớp đúng số dòng của DB (29/16/8/5/30) và giá trị lạ rơi về không lọc thay vì lỗi, 10 trang admin đều 200 với **đúng 1 `<header>`, đúng 1 bootstrap.bundle**, nhóm sidebar tự mở đúng mục con trên cả 6 trang có nhóm, và 3 trang doctor + trang bệnh nhân `medical-record-detail` giữ nguyên `<body>` không class — `./mvnw clean package` **5/5 test xanh** — **CHƯA commit, CHƯA deploy** — `BookingRepository`, `WalletTransactionRepository`, `UserRepository`, `PostRepository`, `CandidateRepository`, `AdminDashboardService(+Impl)` + `AdminDashboardDTO` + `AdminNavInterceptor` (mới), `AdminDashboardService`→`AdminAiReportService` (đổi tên + bỏ 4 giá trị hardcode), `AdminController`, `AdminBookingController`, `AdminAiController`, `AdminDashboardSummaryDTO`, `WebConfig`, `admin-theme.css` + `stat-tile.html` + `ai-insight.html` (mới), `admin/dashboard.html` + `admin/include/sidebar.html` (viết lại), `admin/include/header.html`, `admin/booking-list.html`, `doctor/include/sidebar.html`, 17 tệp `admin/*.html`; **xoá** `DashboardApiController` + `DailyBookingStatsDTO` + `getBookingStatsForLast7Days()` — booking-flow.md, code-structure.md, coding-conventions.md, supporting-subsystems.md
- 2026-08-19 — `fix:` **khách chưa đăng nhập không nhận ra đường vào tài khoản; kèm 1 lỗi sắp xếp header mobile có sẵn từ trước** — chỗ tài khoản trên header bệnh nhân là một icon `bi-person-circle` trần cỡ `fs-5` (~20px): nó không nói lên rằng bấm vào đó để ĐĂNG NHẬP, mà icon người còn bị đọc thành "hồ sơ của tôi" — thứ khách chưa đăng nhập không có — nên với người mới thì lối đăng nhập coi như bị giấu, và ở 20px thì cũng khó bấm trúng trên điện thoại; thay bằng nút `.btn-header-login` **có chữ "Đăng nhập"** + icon `box-arrow-in-right`, kiểu **viền** chứ không nền đặc vì nó đứng ngay cạnh "Đặt lịch khám" (CTA chính) và hai nút nền đặc thì không còn nút nào nổi lên — **(A) luật <400px trong `responsive.css` là cái bẫy phải gỡ TRƯỚC**: nó nuốt chữ (`font-size:0`) rồi vẽ icon lịch bằng `::before` cho **mọi `.btn`** trong header, viết từ hồi header chỉ có đúng một nút — để nguyên thì nút "Đăng nhập" vừa thêm cũng bị nuốt chữ và mọc ra icon lịch, tức **giấu lại đúng thứ vừa bày ra** và còn giả dạng thành nút đặt lịch thứ hai; nay bám `.btn-header-book`, còn nút đăng nhập chỉ ẩn icon + siết đệm để **giữ chữ** — đo thật ở nhánh đó: logo 80 + nút 34 + 92 + hamburger 22 ≈ **276px**, dư 84px ở màn 360 và 44px ở màn 320, không tràn — **(B) lỗi có sẵn tìm ra lúc kiểm chứng, không nằm trong yêu cầu**: dưới 1200px theme sắp lại header bằng `order` (`main.css:214-238`: `.logo`=1, `.btn-getstarted`=2, `.navmenu`=3) nhưng dự án **không dùng `.btn-getstarted`** — nhóm nút là `.header-actions`, không khai `order` nên nhận mặc định **0** và xếp **trước cả logo**; hậu quả trên mọi trang bệnh nhân khổ điện thoại: hai nút dồn sang trái còn "HealCare" nằm giữa, đọc như trang bị vỡ — đã đối chứng bằng ảnh chụp **bản chưa sửa** để chắc chắn không phải do mình gây ra, rồi trả `.header-actions` về khe số 2 — **(C)** icon hồ sơ lúc ĐÃ đăng nhập cũng để `fs-5` trong khi ảnh đại diện ngay cạnh là 32px, nên cùng một chỗ to nhỏ khác nhau tuỳ người dùng có ảnh hay không → thêm `.header-avatar-icon` 2rem cho khớp — **(D) chú thích dev phải dùng dạng `<!--/* */-->`**: dạng `<!-- -->` thường được gửi thẳng ra trình duyệt, và chính nó làm bước `grep -c 'bi-person-circle'` của tôi báo nhầm là icon cũ vẫn còn trên trang khách — **(E) kiểm chứng bằng jar đã đóng gói + trình duyệt thật**, không chỉ đọc mã: 10 trang bệnh nhân đều 200 với đúng 1 `<header>` và đúng 1 nút Đăng nhập; đăng nhập `patient_tom` thì nút biến mất, còn lại dropdown + chuông + `.header-avatar-icon`; ảnh chụp ở 1440/820/520px cho thứ tự đúng **logo → nút → hamburger** và không tràn ngang; `./mvnw clean package` 5/5 test xanh — **KHÔNG commit, KHÔNG deploy** theo yêu cầu — `user/include/header.html`, `assets/css/responsive.css` — coding-conventions.md
- 2026-08-19 — `feat:` **đặt lịch không cần thanh toán trước ("thanh toán tại quầy"), kèm hạn mức chống spam** — trang đặt lịch trước đây BẮT BUỘC trả tiền trước (VNPay / VietQR / ví), nên không có đường nào cho người quen dùng thử mà không đụng tiền thật; thêm hình thức thứ tư `PAY_AT_COUNTER` tạo lịch ở `PENDING` + `UNPAID` chờ nhân viên xác nhận — **(A) ràng buộc quyết định toàn bộ thiết kế: `BookingCleanupTask` huỷ MỌI lịch `PENDING`+`UNPAID` quá 3 phút và không hề biết tới `paymentMethod`** — tức đúng hình dạng của lịch trả-tại-quầy, không sửa job là tính năng chết sau 3 phút **và chết im lặng** (bệnh nhân chỉ thấy lịch biến mất); lịch walk-in của lễ tân thoát được job chỉ nhờ nó là `CONFIRMED` chứ không phải nhờ chủ ý nào; thay finder bằng `findAbandonedPrepayBookings` với vế **`(b.paymentMethod IS NULL OR b.paymentMethod <> :keepMethod)`** — nửa `IS NULL` là **bắt buộc** vì cột nullable và trong SQL `NULL <> 'X'` cho UNKNOWN chứ không phải TRUE, thiếu nó là mọi lịch cũ thoát job vĩnh viễn, treo khung giờ mà không có lấy một dòng log; cố ý dùng **deny-list** để giữ nguyên hành vi cho giá trị chưa biết, đổi lại **mọi hình thức thêm sau này mặc định bị huỷ sau 3 phút** — bẫy sinh đôi với nhánh `else` ở controller — **(B) nhánh `else` catch-all trong `processAppointment` ÉP MỌI giá trị lạ thành VNPAY**, nên chỉ thêm `else if` là hôm nay đúng còn mai ai gõ sai chuỗi thì bệnh nhân chọn "không trả trước" bị đẩy thẳng sang cổng thanh toán, không exception không log; dựng lại thành 4 nhánh tường minh + một `else` **từ chối** — **(C) chống spam bằng `BookingService.whyCannotBookWithoutPayment(user)`** (`MAX_PAY_AT_COUNTER_BOOKINGS = 2`), đặt ở controller **chứ tuyệt đối không trong `reserve()`** vì đó là phễu chung cho cả walk-in của lễ tân; **chỉ đếm lịch trả-tại-quầy chứ không đếm mọi lịch sắp tới** — dự án hỗ trợ đặt hộ nên đếm tất là chặn một tài khoản đặt cho ba người nhà dù đã trả đủ tiền ba lần; mốc **`appointmentDate >= hôm nay` là thứ làm hạn mức TỰ NHẢ**, thiếu nó thì — do dự án không có màn hình đánh dấu no-show — một người quên đến khám hai lần bị khoá **vĩnh viễn** và không nhân viên nào có nút để gỡ; race check-then-insert chấp nhận, đây là gờ giảm tốc chứ không phải luật tiền bạc — **(D) hàm mới KHÔNG mâu thuẫn với ghi chú "there is deliberately no `whyCannotReserve()`"**: ghi chú đó từ chối gác **tranh chấp khung giờ** (một race), còn hạn mức của chính tài khoản đang đăng nhập là **state** — một câu `COUNT` trả lời được **trước khi vẽ radio**, nên form vô hiệu hoá được lựa chọn và in đúng câu server sẽ dùng để chặn; đã sửa thẳng đoạn văn đó trong rule file, không sửa là để lại một luật trông như bị vi phạm — **(E) cố ý KHÔNG gửi email**: `sendBookingConfirmation` nói "đã xác nhận", sai với lịch `PENDING`, rồi khi lễ tân xác nhận thật bệnh nhân nhận lá thứ hai gần y hệt — đúng thứ khiến người ta tưởng hệ thống lỗi rồi **ĐẶT LẠI**, tức chính cái spam đang chống; chuông `pushBookingEvent` là biên nhận — **(F) ba bẫy hiển thị**: badge "Chưa thanh toán" hiện đè lên lịch mới (nhãn đó đọc như một lỗi và khiến khách đặt lại) nên phải sửa **cả hai** span ở `profile.html` + bảng lễ tân + bảng admin (badge "Thu tại quầy" để người cầm hộp tiền nhìn ra ngay); `switch` trong `PdfExportServiceImpl` có `default` trả chuỗi thô nên quên thêm case là **phiếu thu in chữ `PAY_AT_COUNTER`** cho bệnh nhân cầm về; và **chú thích `<!-- -->` thường bị Thymeleaf gửi thẳng ra trình duyệt**, lại lặp một lần mỗi dòng trong `th:each` — nó làm chính bước kiểm chứng `grep -c` của tôi báo nhầm là badge chưa ẩn — **(G) kiểm chứng end-to-end trên production**: chèn 3 dòng `PENDING`+`UNPAID` quá hạn (`PAY_AT_COUNTER` / `VNPAY` / `NULL`) rồi đợi một nhịp cron → đúng 2 dòng sau bị huỷ, dòng đầu **sống sót**; đặt lịch thật qua HTTPS → `/user/profile`, `PENDING`+`UNPAID`+`PAY_AT_COUNTER`; đủ 2 lịch thì radio `disabled` kèm đúng câu từ chối và POST tự chế cũng bị chặn bằng **cùng câu đó**; `paymentMethod=BANANA` bị từ chối và **không tạo dòng VNPAY nào**; đẩy một lịch về ngày hôm qua → radio mở lại, trả ngày về → khoá lại — `BookingService(+Impl)`, `BookingRepository`, `BookingCleanupTask`, `BookingController`, `ProfileController`, `PdfExportServiceImpl`, `user/appointment.html`, `user/profile.html`, `receptionist/booking-list.html`, `admin/booking-list.html` — booking-flow.md, coding-conventions.md
- 2026-08-19 — `fix:` **giao diện vỡ trên điện thoại: 2 theme vốn responsive sẵn, nhưng markup của dự án đã vô hiệu hoá đúng vài chỗ then chốt** — **(A) menu bệnh nhân LUÔN bung trên mobile**: `<ul class="d-flex">` ở `user/include/header.html`, mà Bootstrap sinh `.d-flex{display:flex!important}` nên thắng `.navmenu ul{display:none}` của theme (`main.css:387`) — 22 trang có menu nằm đè lên nội dung dưới dạng panel `position:absolute` và nút hamburger vô nghĩa; đổi sang `d-xl-flex` (chỉ áp dụng từ 1200px, khớp đúng ngưỡng `max-width:1199px` của theme) là theme tự lo lại được, **không cần một dòng CSS override nào** — **(B) 11/23 trang bệnh nhân có nút hamburger CHẾT**: chỗ duy nhất bind `.mobile-nav-toggle` là `assets/js/main.js` mà 11 trang không nạp tệp đó; không nạp thêm được vì `main.js` gọi `new PureCounter()`, `GLightbox(...)`, `scrollTop.addEventListener` **vô điều kiện**, trang thiếu vendor tương ứng là ném lỗi — tách hẳn sang `assets/js/mobile-nav.js` nạp từ fragment header (1 tệp, 22 trang) và **gỡ khối cũ khỏi `main.js`**, vì để cả hai cùng bind là một cú chạm chạy `toggle()` hai lần, menu mở rồi đóng ngay, y hệt bẫy Bootstrap-nạp-hai-lần — **(C) LỖI NẶNG NHẤT, tìm ra lúc kiểm chứng chứ không có trong kế hoạch: `th:fragment` trùng tên thẻ** — `admin/include/header.html` có cả `<head th:fragment="header(title)">` lẫn `<header th:fragment="header-nav" id="header">`, mà Thymeleaf khớp selector theo tên fragment **HOẶC tên thẻ HOẶC id** rồi trả về **MỌI** nút khớp, nên `~{... :: header (title='X')}` kéo về cả hai: **toàn bộ 36 trang nhân viên render 2 thẻ `<header id="header">` chồng nhau**, trang bác sĩ/lễ tân mang luôn thanh header của admin kèm link `/admin/dashboard`; desktop không ai thấy vì cả hai đều `fixed-top` ở `top:0` và cái sau phủ lên cái trước — nhưng nó giết luôn sidebar mobile theo kiểu khó lần nhất: `main.js` bind bằng `select('.toggle-sidebar-btn')` tức **`querySelector`, chỉ lấy phần tử ĐẦU TIÊN**, và phần tử đầu tiên là nút của header bị chôn bên dưới, nên nút nhìn thấy được thì **không có listener nào**; đổi tên fragment `<head>` thành **`pagehead(title)`** ở cả 3 tệp header + 44 lời gọi — **(D)** nút `toggle-sidebar-btn` ở `doctor/include/header.html:32` còn **bị comment**, nên 15 trang doctor/head/staff không có cả nút để mà bấm; bỏ comment (sau khi sửa (C) thì nó mới thực sự được nối sự kiện) — **(E) 10 trang thiếu `<meta viewport>`** (`career-details`, `change-password`, `contact`, `doctor-schedule`, `medical-process`, `news-details`, `payment-result`, `working-hours`, `error/403`, `error/500`) nên trình duyệt render ở viewport ảo 980px rồi thu nhỏ; `payment-result.html` thiếu **cả `<meta charset>`** — **(F) 19 tệp có `<table>` không bọc `.table-responsive`** (tới 29 cột), và `simple-datatables` **không** tự thêm `overflow-x` (đã kiểm vendor CSS: `.datatable-container` không có luật đó) → thêm `assets-admin/js/responsive-admin.js` tự bọc lúc `window.load`, tức **sau** khi `main.js` khởi tạo datatables nên bảng `.datatable` được bọc cả cụm `.datatable-wrapper` thay vì bọc bảng trần rồi bị plugin bọc chồng lên — **(G) lưới lịch làm việc bị BÓP chứ không cuộn**: `.ws-grid-wrapper{overflow-x:auto}` (`work-schedule.css:273`) **chưa từng chạy lần nào** vì không phần tử nào trong DOM mang class đó (`work-schedule.js` gắn `.ws-grid` thẳng vào `#wsCalendarBody`), cộng thêm `.ws-grid{width:100%}` khiến lưới không bao giờ rộng hơn khối cha — trên màn 375px mỗi cột ngày còn ~47px; cho `#wsCalendarBody`/`.ws-month` cuộn ngang thật + `min-width` cho lưới — **(H) widget AI + dropdown thông báo cố định px**: `.ai-chat-box` 380px + `right:30px` = 410px và `min-width:320px` chặn co lại, `.admin-ai-sidepanel`/`.doctor-ai-sidepanel` 400px, dropdown `width:340px` — tất cả sinh scroll ngang cho cả trang; media query cho widget phải đặt **trong `<style>` của chính fragment** chứ không ở tệp phủ, vì `<style>` đó nằm trong `<body>` tức SAU `<link>` ở `<head>`, hai luật cùng `!important` + cùng specificity thì luật sau thắng — **(I)** `user/service-details.html` có **2 tài liệu HTML lồng nhau** (2 `<!DOCTYPE>`, 2 `<head>`), bản đầu 116 dòng đã comment gần hết nhưng vẫn làm trình duyệt mobile parse thất thường → xoá hẳn — **(J) kiểm chứng bằng jar đã đóng gói, không đoán**: đăng nhập thật 5 vai trò rồi quét 40 URL — mọi trang 200, **đúng 1 `<header>`, đúng 1 nút mở sidebar, đúng 1 viewport**, tài nguyên mới phục vụ 200 với đúng content-type, `node --check` 3 tệp JS, ngoặc CSS cân bằng, `./mvnw test` 5/5 xanh — **cố ý KHÔNG đụng**: `auth/login|register.html` (thiết kế riêng, đã có viewport và media query của chúng), `user/change-password.html` giữ header tối giản tự viết (đã đủ viewport + CSS), và `min-width` inline ở `head/clinic-roster|duty-roster` (nằm trong `.table-responsive` sẵn, đang ép cuộn ngang đúng ý) — `assets/css/responsive.css` + `assets/js/mobile-nav.js` + `assets-admin/css/responsive-admin.css` + `assets-admin/js/responsive-admin.js` (mới), `assets/js/main.js`, `assets-admin/css/work-schedule.css`, 3 fragment header + 3 fragment footer + 3 fragment ai-chat, 23 trang `user/*.html`, `error/403|500.html`, 44 lời gọi fragment `pagehead` — coding-conventions.md

- 2026-08-19 — `fix:` **admin KHÔNG xoá được bất kỳ tài khoản nhân viên nào, và câu báo lỗi chỉ sai thủ phạm** — phát hiện trong log production: `Cannot delete or update a parent row: foreign key constraint fails (staff_profiles, FK user_id → users.id)`; `StaffProfile.user` là `@OneToOne` **không cascade**, mà `ensureStaffProfiles` tạo một `StaffProfile` cho **mọi bác sĩ và lễ tân**, nên việc xoá hỏng với cả tài khoản chưa có một lịch hẹn nào — **(A) thứ tệ hơn con bug là câu thông báo**: `AdminController.deleteUser` bắt trần mọi `Exception` rồi in *"Không thể xóa User (đã có lịch hẹn hoặc liên kết Bác sĩ)"*, tức chỉ vào **lịch hẹn** trong khi thủ phạm là `staff_profiles` — admin sẽ đi xoá sạch lịch hẹn của bác sĩ đó rồi vẫn không xoá được, một chỉ dẫn sai còn hại hơn không có chỉ dẫn — **(B) hỏi schema thật thay vì đoán**: `information_schema.KEY_COLUMN_USAGE` cho **15 khoá ngoại / 13 bảng** trỏ vào `users`, chia làm hai nhóm theo đúng một tiêu chí — bản ghi đó có ý nghĩa độc lập với người dùng hay không; **CHẶN** khi là hồ sơ bác sĩ / có lịch hẹn (kèm bệnh án) / có giao dịch ví / là tác giả bài viết / đã ký duyệt đơn nghỉ–ca trực, **XOÁ KÈM** `shift_cover_requests` (cả hai chiều **và** theo từng ca trực của người đó — một lời mời do người KHÁC gửi tới ca của họ vẫn chặn được việc xoá `staff_shifts`), `staff_shifts`, `leave_requests`, `allergies`, `ai_chat_sessions`, `notifications`, `staff_profiles`; `user_roles` để JPA tự dọn — **(C)** `UserService.whyCannotDelete(user, currentUsername)` theo đúng khuôn `whyCannotCancel`: controller chặn thật, `manage-user.html` nhận `deleteBlockReasons` (`Map<Long,String>`) để **ẩn nút và in đúng câu đó**, nên URL gõ tay cũng không lách được và giao diện không bao giờ mời bấm một nút chắc chắn lỗi; luật "không cho Admin tự xoá mình" chuyển từ controller vào đây cho gọn về một chỗ — **(D) thứ tự các phép kiểm là quyết định về HIỆU NĂNG chứ không phải thẩm mỹ**: màn hình gọi `whyCannotDelete` cho **mọi** dòng trong 135 tài khoản, nên nhánh `doctorRepository.findByUserId` đặt đầu tiên để ~132 dòng thoát sau đúng một truy vấn, thay vì chạy đủ 6 phép kiểm × 135 dòng ≈ 800 truy vấn mỗi lần mở trang — **(E) kiểm chứng end-to-end trên production qua HTTPS**, không phải chỉ biên dịch được: đăng nhập admin bằng form → 302 `/admin/dashboard`; `/admin/manage-user` trả 200 với **132 dòng ẩn nút Xoá**; xoá bác sĩ id 60 bị chặn kèm đúng câu tiếng Việt; rồi dựng một tài khoản thử có đủ `staff_profile` + `user_roles` + `notifications` + `allergies` và xoá qua chính endpoint đó → *"Đã xoá người dùng thành công"*, cả 5 bảng về **0** — `UserService(+Impl)`, `AdminController`, `admin/manage-user.html`, 10 repository (`Booking`, `WalletTransaction`, `Post`, `LeaveRequest`, `StaffShift`, `ShiftCoverRequest`, `Allergy`, `AiChatSession`, `Notification`, `StaffProfile`) — coding-conventions.md
- 2026-08-19 — `fix:` **`deploy/backup.sh` báo lỗi quyền mỗi đêm dù bản dump vẫn đủ** — `mysqldump` 8 mặc định đọc thông tin tablespace, việc đó đòi quyền **PROCESS ở cấp TOÀN SERVER**, trong khi tài khoản app **cố ý** chỉ có quyền trên đúng một schema; nên mỗi lần chạy in `Access denied; you need (at least one of) the PROCESS privilege(s)` ra **stderr**, mà dòng cron lại `2>&1` vào log — tức mỗi đêm một dòng ERROR cho một bản sao lưu hoàn toàn bình thường, đúng kiểu nhiễu làm người ta thôi đọc log rồi bỏ sót sự cố thật; thêm `--no-tablespaces`, **không** `GRANT PROCESS` (mở quyền toàn server để né một thứ bản sao lưu không cần) — **kiểm chứng bằng PHỤC HỒI THẬT** vào database tạm chứ không chỉ nhìn kích thước tệp: 30 bảng, 135 users / 132 bác sĩ / 22 khoa khớp production, và tiếng Việt (`Vũ Hữu Hoàng Anh`, `Nội thần kinh`) nguyên vẹn — đây là thứ duy nhất chứng minh `--default-character-set=utf8mb4` có tác dụng, vì lỗi mã hoá chỉ lộ đúng lúc phục hồi thật, khi không còn bản nào để đối chiếu — **cạm bẫy gặp cùng lúc, không sửa bằng code**: đổi username một bác sĩ seed (`bs_ledinhphuc` → `bs_vhha`) làm `ensureExtraDoctors` (kiểm trùng bằng `existsByUsername`, không phải email) tưởng người đó chưa có nên tạo lại, đâm vào UNIQUE trên email; `DataInitializer` là `CommandLineRunner` nên đó là **chết cả tiến trình khởi động**, cộng `Restart=always` thành crash-loop 10 giây một lần và nginx trả 502 — cách đúng là `SEED_ENABLED=false` sau lần boot đầu (chính là giá trị `env.example` khuyến nghị), không phải sửa dữ liệu — `deploy/backup.sh` — environment-setup.md
- 2026-08-19 — `fix:` **deploy thật lần đầu lên Oracle Cloud phát hiện 2 lỗi chỉ lộ ra trên DB RỖNG** — dựng máy A1.Flex Ubuntu 24.04, mở 2 lớp tường lửa, cài Java 21 + MySQL 8 + nginx, đẩy jar 103MB + 133 ảnh, và app **chết ngay lần boot đầu** — **(A) `spring.jpa.hibernate.ddl-auto` BIẾN MẤT khỏi `application.properties`**: dòng này có ở `Init commit` (dòng 20) và bị xoá trong `a0a952c` lúc viết lại toàn bộ tệp theo dạng `${VAR:default}`; mặc định của Spring Boot với DataSource **không nhúng** là `none`, nên Hibernate không dựng bảng nào và biến `DDL_AUTO` mà `deploy/env.example` dành hẳn một khối bình luận để giải thích thì **nối vào hư không** — lỗi vô hình trên mọi máy dev vì DB ở đó đã có sẵn 27 bảng từ trước, chỉ nổ ở lần boot đầu trên schema trống dưới dạng `Table 'bookinghealthy.users' doesn't exist` ném ra từ chính `count()` của `DataInitializer`, tức thông báo trỏ vào *bảng* chứ không trỏ vào *cấu hình thiếu* — **(B) `characterEncoding=utf8mb4` trong `DB_URL` của `env.example`**: tham số này của Connector/J nhận **tên charset JAVA** chứ không phải tên charset MySQL, nên ném `Unsupported character encoding 'utf8mb4'` ngay lúc mở kết nối đầu tiên, trước cả khi có gì để mà sai — giá trị đúng là `UTF-8`, và Connector/J 8 tự ánh xạ nó sang `utf8mb4` phía máy chủ; hai lỗi này che nhau, sửa xong (A) mới thấy (B) — **(C) kiểm chứng sau khi sửa**: 27 bảng, 134 users / 132 bác sĩ / 22 khoa / 5 roles, `[Upload]` phân giải đúng `/var/lib/nnlhospital/{uploads,private}`, `Started BookingHealthyApplication in 17.022 seconds`, `/actuator/health` trả 200 — **(D)** hai dòng ERROR còn lại là ĐÚNG Ở GIAI ĐOẠN NÀY: `SchemaGuard` và ShedLock cùng báo thiếu `shedlock`, vì `db/manual/*.sql` thuộc bước sau và `SCHEMA_STRICT=false` cố ý cho nó chỉ log chứ không chặn khởi động — `src/main/resources/application.properties`, `deploy/env.example` — environment-setup.md
- 2026-08-18 — `chore:` **dọn file chết và file tạm: 35 tệp ra khỏi repo, ~292 MB khỏi đĩa** — **(A) `src/main/resources/static/uploads/` (26 tệp, 2,6 MB) CHƯA TỪNG được phục vụ một lần nào**: handler `/uploads/**` trong `WebConfig` chỉ giải từ `file:uploads/`, mà nó **cụ thể hơn** handler `/**` mặc định nên Spring dừng ở đó và trả 404 chứ **không rơi tiếp xuống classpath** — tức thư mục này không phải bản dự phòng mà là trọng lượng chết, lại còn **22/26 tệp trùng byte y hệt** `uploads/doctor-*.jpg`; xoá xong jar 105M → **103M** — **(B) `data.sql` xoá hẳn thay vì để đó cho `spring.sql.init.mode=never` canh**: Spring nhận diện tệp này **theo TÊN, không theo khai báo** (grep cả `src/` lẫn `pom.xml` không có một tham chiếu nào), nên để nguyên là cách đúng **một dòng property** khỏi việc chạy một script seed cũ đè lên cơ sở dữ liệu mà `DataInitializer` đang sở hữu; giữ lại dòng `never` làm chốt chặn nếu ai đó thêm tệp về — **(C) 8 template mồ côi (136 KB)**: `admin/doctor-form.html` (controller chỉ dùng `doctor-form-add`/`-edit`), `doctor/include/ai-chat.html` (`header.html:94` nhúng `ai-chat-doctor` chứ không phải tệp này), và 6 trang thừa của theme Bootstrap gốc (`faq`, `gallery`, `privacy`, `starter-page`, `terms`, `testimonials` — không controller nào trả về, chỉ liên kết chéo nhau bằng `href="faq.html"` vốn đã là link chết) — **cạm bẫy khi rà: `error/403|404|500.html` KHÔNG xuất hiện trong bất kỳ grep nào nhưng PHẢI GIỮ**, vì `BasicErrorController` phân giải chúng theo quy ước chứ không qua tên view — **(D) tệp tạm lúc chạy**: 8 ảnh `uploads/*_news.*` (5,6 MB, `NewsFeedService` tự tải lại) + 1 ảnh upload thử untracked — **(E) `target/` (270 MB) và `.idea/` (177 KB)**; `.idea/` xoá vì **`dataSources.xml` + `dataSources.local.xml` mang thông tin kết nối MySQL**, chứ không vì dung lượng — **(F) kiểm chứng chứ không đoán**: `./mvnw package` sau khi xoá cho **BUILD SUCCESS**, 228 tệp nguồn biên dịch sạch, **5/5 test xanh** (`MedicalRecordMailTemplateTest` 2, `PdfFontTest` 3), manifest có `Start-Class` đúng — rồi mới xoá `target/` lần nữa; app phải **không đang chạy** trước khi xoá `target/` (đã kiểm cổng 8090), nếu không `spring-boot:run` chết giữa chừng như build-and-run.md mô tả — **(G) sửa luôn một con số sai còn sót**: environment-setup.md vẫn ghi "142 ảnh bác sĩ" trong khi `git ls-files uploads` = **133** (142 là số tệp trong thư mục, phần chênh chính là ảnh tin tức vừa xoá) — đợt 17/08 mới sửa ở `deploy/README.md` mà bỏ quên rule file — **(H) hai thứ CỐ Ý để lại**: `graphify-out/` (**300 tệp / 14 MB đang được commit** từ `4a7d2d4`, trong đó **292 là cache AST gắn cứng `v0.9.26`** — đầu ra công cụ, không phải mã nguồn, nên `git rm -r --cached` + thêm vào `.gitignore`) và **11 ảnh upload thời dev trong `uploads/`** (`haui_logo`, `Gemini_Generated_Image_*`, `chatbox`, `anhthe3x4`…) — **không xoá được bằng mắt** vì `User.avatar` / `Doctor.avatar` / ảnh `Service` có thể đang trỏ tới, và ảnh mất thì trang chỉ hiện hình vỡ chứ không báo lỗi gì; phải truy vấn DB trước — `src/main/resources/static/uploads/` (26 tệp), `src/main/resources/data.sql`, 8 template, `uploads/*_news.*`, `target/`, `.idea/` — environment-setup.md
- 2026-08-17 — `docs:`/`chore:` **bộ deploy hoàn chỉnh cho hạ tầng miễn phí, kèm 3 lỗ chặn đường mà runbook cũ không thấy** — `deploy/` từ đợt 15/08 đã có runbook 8 bước tốt nhưng **giả định một máy chủ Linux đã dựng xong** và không nói dùng nhà cung cấp nào; rà lại thì có ba thứ khiến nó không chạy được tới cùng — **(A) `deploy/`, `db/` và `src/test/` CHƯA HỀ được commit** (`git ls-files` trả về **0** cho cả ba): `git clone` trên máy chủ là không có `db/manual/*.sql`, nên bước 4 của chính runbook không chạy được và `SCHEMA_STRICT=true` sau đó chặn khởi động — phát hiện ra thì đã dựng xong cả máy chủ; mất luôn `PdfFontTest` + `MedicalRecordMailTemplateTest`, hai bài test canh đúng hai lỗi mà ứng dụng **nuốt vào log** (thiếu font in PDF, template email hỏng), tức "không thấy lỗi" và "chạy đúng" trông giống hệt nhau — **(B) không có unit systemd** dù README bước 2 đã nói tới user `nnlhospital` và `/etc/nnlhospital/.env`; thêm `deploy/nnlhospital.service` với `Restart=always` và **`SuccessExitStatus=143`** (JVM thoát mã 143 khi nhận SIGTERM, thiếu dòng này thì mọi lần `systemctl stop` đều bị systemd ghi là thất bại), `After=` chứ **không** `Requires=mysql.service` (Requires kéo app chết theo mỗi lần MySQL khởi động lại, trong khi Restart=always đã tự thử lại tới khi MySQL lên) — **(C) `env.example` dùng bình luận CUỐI DÒNG, mà `EnvironmentFile=` của systemd không hiểu**: `SCHEMA_STRICT=true   # ghi chú` gán nguyên chuỗi `"true   # ghi chú"` và Spring ném lỗi ép kiểu boolean, thông báo lỗi **không hề nhắc tới dấu #**; cùng lỗ đó, giá trị có dấu cách hoặc `& ? *` phải đặt nháy kép — app password Gmail có dấu cách, `NEWS_FETCH_CRON` có cả hai, và dấu `&` trong `DB_URL` không đặt nháy khiến bash trong `backup.sh` cắt dòng làm đôi rồi chạy nền, tức **mật khẩu DB biến mất mà không báo gì**; viết lại toàn bộ `env.example` theo khuôn "bình luận đứng riêng + giá trị đặc biệt đặt nháy kép" — **(D)** `env.example` còn đặt `SCHEMA_STRICT=true` **mâu thuẫn với bước 4 của chính README**: lần boot đầu trên DB rỗng thì cả 4 đối tượng `SchemaGuard` kiểm đều chưa tồn tại, nên `true` chặn khởi động đúng lúc cần Hibernate dựng bảng — đổi mặc định về `false` kèm giải thích thứ tự bật lại — **(E)** thêm `deploy/backup.sh` (mysqldump `--single-transaction` + tar `uploads/`+`private/`, giữ 7 bản, mật khẩu qua `--defaults-extra-file` chứ **không** qua dòng lệnh vì `ps aux` đọc được; bắt buộc `--default-character-set=utf8mb4` — thiếu là tên bệnh nhân và chẩn đoán tiếng Việt hỏng mã trong tệp dump và chỉ lộ ra đúng lúc phục hồi, tức lúc không còn bản nào để đối chiếu) — **(F)** thêm `deploy/DEPLOY-ORACLE-FREE.md`: runbook cụ thể cho **Oracle Cloud Always Free** (A1.Flex ARM 2 OCPU/12GB — hạn mức 2026 đã giảm một nửa từ 4/24), gồm các bẫy chỉ Oracle mới có — **tường lửa HAI lớp** (Security List của VCN *và* iptables trong Ubuntu vốn có sẵn luật REJECT; mở một lớp thì trình duyệt quay vô tận còn log nginx trống trơn), cổng 25 bị chặn vĩnh viễn nhưng 587 thông nên Gmail vẫn gửi được, Home Region **không đổi được** về sau, instance Always Free bị thu hồi khi để không — và **thứ tự nginx/certbot**: `nginx.conf.example` trỏ tới chứng chỉ chưa tồn tại nên phải dựng server block HTTP tối thiểu → `certbot --nginx` → mới cài cấu hình đầy đủ, cộng luật Cloudflare phải để SSL mode `Full (strict)` (để `Flexible` là vòng lặp chuyển hướng vô tận) — **(G)** sửa "142 ảnh bác sĩ" → **133** (`git ls-files uploads` = 133; 142 là số tệp trong thư mục, chênh lệch là ảnh tin tức `NewsFeedService` tải về, không commit và không cần chép sang) và bổ sung đường đi cho máy dev Windows vì `rsync` không có sẵn — **(H)** đo sẵn phạm vi món nợ CSRF để lần sau khỏi đoán: **51 chỗ `method="post"` trong 31 template + 12 lời gọi `fetch` POST**, kèm ngoại lệ bắt buộc giữ là `/api/payment/webhook` (gọi server-to-server, không có phiên, đã tự xác thực bằng bí mật trong header) — `deploy/nnlhospital.service` + `deploy/backup.sh` + `deploy/DEPLOY-ORACLE-FREE.md` (mới), `deploy/env.example` (viết lại), `deploy/README.md` — build-and-run.md
- 2026-08-16 — `fix:` **bật spring-session-jdbc xong là MỌI lượt đăng nhập trả HTTP 500** — `NotSerializableException: com.bookinghealthy.model.User` ném từ `JdbcIndexedSessionRepository.serialize` lúc ghi `SPRING_SESSION_ATTRIBUTES`, tức là **sau** khi controller đã chạy xong nên khách nhận trang 500 trần của Tomcat chứ không phải `error/500.html`; gốc rễ: `CustomUserDetails` giữ nguyên entity `User`, mà hồi phiên còn nằm trong RAM Tomcat thì **không ai tuần tự hoá nó bao giờ** — khiếm khuyết tồn tại suốt đời dự án và chỉ nổ đúng vào lúc đổi kho lưu phiên, đúng thứ đã ghi trong `deploy/README.md` là "không kiểm chứng được bằng đọc code" — **(A)** cả hai principal nay **rút phẳng ngay tại constructor** thành chuỗi thuần (cùng luật với `MedicalRecordMailDTO`: dữ liệu vượt ranh giới luồng/tuần tự hoá thì không được là entity); `User.roles` là `@ManyToMany` nên giữ entity còn kéo cả `PersistentSet` của Hibernate vào phiên — vừa không tuần tự hoá được vừa là dữ liệu đông cứng tới lúc đăng xuất — **(B) trình biên dịch không hề cảnh báo được**: `UserDetails` **đã** kế thừa `Serializable` nên `CustomUserDetails` luôn *hứa* mình tuần tự hoá được, chỉ có kiểu của field âm thầm phá lời hứa; ngược lại `OAuth2User` **KHÔNG** kế thừa `Serializable` (Spring khai tay ở `DefaultOAuth2User` của họ), nên `CustomOAuth2User` trước giờ **không tuần tự hoá được chút nào** — đăng nhập Google/Facebook hỏng y hệt, chỉ khác là nó nổ sau khi đã bay qua nhà cung cấp rồi mới quay về nên trông giống "nút Google bị hỏng" — **(C) luật phải giữ khi sửa tiếp**: `CustomOAuth2User.getAttributes()` **bắt buộc giữ nguyên map thô của nhà cung cấp**, vì **7 chỗ** nhận diện người dùng bằng `principal.getAttribute("email")` (`AiController` ×3, `DoctorAiController`, `DoctorExamAiController`, `PatientChatLookupApiController`, `BookingController`, `ProfileController`, `CurrentUserService`) — rút gọn map đó là cả 7 đường đọc `null` và người đăng nhập bằng Google **âm thầm** thành khách vãng lai, không một ngoại lệ nào được ném; chỉ `OAuth2UserInfo` bị rút thành 3 chuỗi vì đó là phần duy nhất vừa không tuần tự hoá được vừa chỉ dùng để đọc tên/email/ảnh — **(D)** đổi hình dạng principal thì phải xoá `SPRING_SESSION_ATTRIBUTES` + `SPRING_SESSION` (phiên cũ giải tuần tự hoá vào lớp mới sẽ hỏng); kiểm chứng lại **từ jar đã đóng gói**: 4 vai trò điều hướng đúng dashboard, `SPRING_SECURITY_CONTEXT` thật sự nằm trong MySQL, `principal.fullName`/`principal.avatar` vẫn render, `/api/notifications` và `/api/chat/my-bookings` vẫn phân giải đúng người — riêng **Google/Facebook vẫn phải thử bằng trình duyệt thật**, `curl` không bắn được đường đó — `CustomUserDetails`, `CustomOAuth2User` — authentication-and-roles.md, deploy/README.md
- 2026-08-15 — `fix:` **rà soát toàn dự án cho việc DEPLOY: chạy tốt ở dev nhưng hỏng/bị khai thác khi lên production** — **(P0-1) `mvn package` chưa từng ra jar chạy được**: `pom.xml` thiếu hẳn `spring-boot-maven-plugin`, nên build vẫn báo SUCCESS mà ra jar MỎNG không nhúng dependency, không có `Main-Class` — `java -jar` chết ngay với *"no main manifest attribute"*; vô hình suốt trên máy dev vì ở đó luôn chạy `./mvnw spring-boot:run` (goal `run` phân giải theo tiền tố và lấy version từ pluginManagement của parent, còn execution `repackage` CHỈ được gắn khi plugin khai trong `<build><plugins>`) — **(P0-2) toàn bộ cấu hình và bí mật hardcode**: viết lại `application.properties` theo dạng `${BIEN:mặc-định-dev}` (cố ý MỘT file, không tách `application-prod.properties`: hai file là hai chỗ phải giữ đồng bộ và khi quên một khoá thì nó IM LẶNG rơi về giá trị dev, tức prod chạy bằng mật khẩu dev mà không có lấy một dòng log), thêm `VnPayProperties`/`VietQrProperties` (`@ConfigurationProperties` đầu tiên của dự án — **không** dùng mẹo gắn `@Value` lên setter tĩnh: đó là mìn thứ tự khởi tạo, ai đọc field tĩnh trước khi bean dựng xong sẽ nhận `null` mà mã nguồn không hề cho thấy), xoá `VNPayConfig.vnp_ReturnUrl` (mã CHẾT không nơi nào đọc mà lại ghi `localhost:8080` trong khi app chạy 8090), thêm `deploy/env.example` + `deploy/nginx.conf.example` + `deploy/README.md` và **checklist XOAY 5 bí mật đã lộ trong lịch sử git** — **(P1-1)** `server.forward-headers-strategy=framework` + `app.base-url` thay cho `getScheme()+getServerName()+":"+getServerPort()` ở `BookingController` (sau nginx đó là scheme/host/port NỘI BỘ nên VNPay không gọi về được, mà kể cả đúng host thì nó LUÔN nối `:port` → `https://domain:443/...` lệch URL đã đăng ký merchant); sửa `TimeZone.getTimeZone("Etc/GMT+7")` → `Asia/Ho_Chi_Minh` (**POSIX đảo dấu: `Etc/GMT+7` là UTC−7, lệch 14 tiếng** — sandbox bỏ qua nên ẩn hoàn toàn ở dev, VNPay production sẽ báo giao dịch hết hạn ngay khi vừa tạo); `getIpAddress` bỏ đọc tay `X-FORWARDED-FOR` (sau proxy là chuỗi `"ip1, ip2"`, sai định dạng) — **(P1-2) `/payment-return` KHÔNG hề kiểm `vnp_SecureHash`** (lỗ hổng nặng nhất tìm được, không có trong danh sách rà soát đầu): hàm cũ chỉ bind `vnp_ResponseCode`+`vnp_OrderInfo` rồi tin luôn, mà route rơi xuống `anyRequest().authenticated()` → **bất kỳ ai đăng nhập cũng tự gõ URL để đánh dấu lịch BẤT KỲ là đã thanh toán**, chiếm chỗ khám thật và kích hoạt email thật (sandbox không làm nhẹ điều đó — tài sản bị chiếm là lịch hẹn); nay 6 lớp: chữ ký → mã merchant → tra theo `vnp_TxnRef` (**cột mới, nullable**; trước đây mã này sinh ngẫu nhiên rồi VỨT ĐI nên chỉ còn cách bóc chuỗi tự do `vnp_OrderInfo`) → quyền sở hữu → đối số tiền bằng `compareTo` (không `equals` — khác scale) → chỉ xử lý khi còn `UNPAID`; `buildHashData` dùng CHUNG cho chiều đi và chiều xác thực, vì lệch một quy tắc mã hoá là mọi giao dịch thật bị từ chối rồi người ta lại "sửa" bằng cách bỏ kiểm tra — **(P1-3) webhook chuyển khoản chưa từng khớp lần nào**: QR in `"HEALCARE " + id` còn webhook so `"MDTRUST"` — hai hằng số hardcode ở hai file, nên khách chuyển tiền THẬT, webhook nhận rồi lặng lẽ trả `SUCCESS` không đụng gì, và 3 phút sau `BookingCleanupTask` huỷ lịch vì "chưa thanh toán"; thêm `util/PaymentMemo` (một chỗ sinh, một chỗ đọc, cùng lấy tiền tố từ `vietqr.memo-prefix`) và bóc id bằng regex `HEALCARE\s*(\d+)` chứ **không** `replaceAll("[^0-9]","")` (nội dung ngân hàng thật kèm mã tham chiếu và ngày tháng, gom hết chữ số lại là ghi nhận tiền cho nhầm lịch của người khác); webhook nay bắt buộc **bí mật trong header** so bằng `MessageDigest.isEqual` (để rỗng = đóng hẳn, an toàn hơn mở toang), parser chịu được CẢ HAI shape SePay phẳng và Casso lồng `data[]` (bản cũ deref thẳng `payload.get("description")` nên payload Casso ném NPE rồi trả 400 trông y hệt lỗi mạng), đối chiếu SỐ TIỀN, và chống trùng bằng `bank_txn_ref` (ngân hàng gửi lại là hành vi bình thường, mỗi lần gửi lại là một email nữa tới bệnh nhân); bỏ `System.out.println` in nguyên payload tài chính — **(P1-3b) IDOR**: `/checkout-qr` và `/api/payment/check-status` đều `permitAll` và chỉ xét `paymentStatus == UNPAID`, nên khách vãng lai dò `?id=` là đọc được tên bệnh nhân, số tiền, bác sĩ, giờ khám của người khác; siết `"/api/payment/**"` xuống đúng `"/api/payment/webhook"` trong **khối 0** và thêm kiểm quyền sở hữu ở controller; trang QR cũng thôi poll vô hạn (đếm ngược 3 phút khớp `BookingCleanupTask`, nhận diện cả trường hợp phiên hết hạn trả về HTML trang login kèm status 200) — **(P1-4) hai đường ghi tệp trỏ vào cây MÃ NGUỒN**: `CareerController` ghi `src/main/resources/static/uploads/cv/` (đã hỏng sẵn cả ở dev — nơi ghi và nơi đọc là hai thư mục khác nhau) và `AdminServiceController` ghi `src/main/resources/static/assets/img/health/` rồi đọc lại từ classpath, tức ảnh admin tải lên **chưa từng hiển thị lần nào**; gom cả 7 chỗ ghi vào `FileStorageService` (đường dẫn TUYỆT ĐỐI từ `app.upload-dir` — để tương đối là bám theo CWD của tiến trình mà container/systemd hầu như luôn đặt khác chỗ jar; lọc tên tệp qua `StringUtils.getFilename` vì `getOriginalFilename()` là dữ liệu client và một tên chứa `..\..\` ghi được ra ngoài thư mục upload; allowlist đuôi; thêm số ngẫu nhiên cạnh `currentTimeMillis` vì hai tệp cùng mili-giây đè nhau); ảnh dịch vụ xử lý bằng **hai `addResourceLocations`** cho `/assets/img/health/**` (ảnh mới từ đĩa, ảnh seed cũ từ trong jar) nên **không phải migrate dữ liệu, không phải sửa template**; **CV chuyển hẳn ra `app.private-dir`** + endpoint `/admin/candidates/{id}/cv` có kiểm quyền — CV mang họ tên, số điện thoại, email, quá trình làm việc, để dưới `/uploads` (vốn permitAll và sắp do nginx phục vụ thẳng, nơi Spring Security không chạm tới) là công khai cho ai đoán đúng tên tệp — **(P1-5)** `/actuator/health` `permitAll` + phần còn lại `hasRole("ADMIN")`: trước đây `/actuator/**` rơi xuống `anyRequest().authenticated()` nên mọi healthcheck nhận 302 về `/login` và instance bị đánh unhealthy rồi restart trong vòng lặp; **tuyệt đối không mở `env`/`configprops`** — chúng in ra đúng những bí mật vừa đưa ra biến môi trường — **(P2-1) chống đặt trùng khung giờ ở tầng DB**: `slotLocks` là `ReentrantLock` trong MỘT JVM nên instance thứ hai là hai bệnh nhân đặt trúng một khung; **UNIQUE 3 cột trần KHÔNG dùng được** vì bản ghi CANCELED không bị xoá (mọi đường huỷ chỉ lật `status`, và `BookingCleanupTask` tự sinh một dòng CANCELED trên đúng khoá đó mỗi 3 phút cho mọi lượt thanh toán bỏ dở) → dùng **cột sinh NULL-khi-CANCELED** + unique index (MySQL cho phép nhiều NULL); cột cố ý **không map vào entity** (Hibernate chỉ THÊM chứ không sửa cột lạ nên `ddl-auto=update` để yên, và INSERT liệt kê cột tường minh nên không đụng cột generated) — **phần tinh tế nhất: `@Transactional` phải CHUYỂN từ 3 controller xuống 3 method của service**, vì theo đặc tả JPA một `PersistenceException` đánh dấu giao dịch rollback-only, nên controller bắt lỗi và trả redirect tử tế rồi proxy mới ném `UnexpectedRollbackException` lúc commit — khách nhận trang 500 thay vì câu tiếng Việt; kèm `saveAndFlush` (ở `reassign`/`rescheduleByUser` là UPDATE, Hibernate hoãn tới commit tức NGOÀI khối try) và `.trim()` nhãn khung giờ (collation mặc định MySQL 8 không bỏ qua khoảng trắng cuối); DDL để ở `db/manual/*.sql` + `SchemaGuard` kiểm lúc boot (log to, `SCHEMA_STRICT=true` thì chặn khởi động) thay vì kéo Flyway vào giữa lúc sắp deploy — **(P2-2)** `spring.task.scheduling.pool.size=4`: pool mặc định là **1 luồng**, mà `MedicalNewsTask` chiếm luồng hàng phút (5 RSS + 2 bài × HTTP 10s + LLM tối đa 2×25s + tải ảnh) nên suốt thời gian đó `BookingCleanupTask` đứng im — **(P2-3) ShedLock** cho 6 job (cờ sẵn có không đủ theo ba kiểu khác nhau: `AppointmentReminderTask` ghi cờ SAU khi gửi, `FollowUpReminderTask` ghi TRƯỚC, `ClinicRegistrationTask.remindDoctorsToRegister` **không ghi cờ nào cả**), bắt buộc `usingDbTime()` (thiếu là so đồng hồ giữa các máy, lệch giờ = âm thầm chạy hai lần) và `lockAtLeastFor` (job xong trong 2 giây sẽ nhả khoá kịp cho instance kia chạy lại); **`AiController.cleanUpExpiredLocks` TUYỆT ĐỐI không gắn** — nó dọn map trong bộ nhớ của chính JVM đó, khoá phân tán vào là cache của các instance khác phình mãi — **(P2-4)** `spring-session-jdbc`: `SecurityContext`, `state` của OAuth2 và **FlashMap** đều nằm trong session RAM của Tomcat, mà FlashMap là kênh báo lỗi DUY NHẤT của ứng dụng (không có `@ControllerAdvice` nào); `ATTRIBUTE_BYTES` nâng lên `MEDIUMBLOB` vì `BLOB` chỉ 64KB và MySQL **cắt cụt** khi vượt → biểu hiện là người dùng bị đăng xuất ngẫu nhiên; cookie `SameSite=lax` chứ **không** `strict` (strict không gửi cookie khi điều hướng từ site khác về, phá cả callback OAuth2 lẫn đường VNPay trả kết quả) — **(P0-4)** `DataInitializer` nhận `seed.*` (mật khẩu admin qua env + cảnh báo khi còn mặc định, cờ tắt tài khoản demo, cờ tắt hẳn seed) — **phát hiện lúc kiểm chứng: DB dev ĐANG CÓ một lượt đặt trùng thật** (lịch #17 và #18, hai bệnh nhân khác nhau, cùng bác sĩ 8 / 2026-07-20 / 13:30, đều CONFIRMED+PAID, tạo cách nhau 0,6 giây) — bằng chứng sống rằng khoá trong bộ nhớ không đủ; phải xử lý dòng này trước khi tạo được `uk_bookings_slot` — `pom.xml`, `application.properties`, `VNPayConfig`, `VnPayProperties`+`VietQrProperties`+`ShedLockConfig`+`SchemaGuard` (mới), `PaymentService`, `PaymentController`, `VietQRController`, `PaymentMemo` (mới), `FileStorageService(+Impl)` (mới), `WebConfig`, `SecurityConfig`, `BookingServiceImpl`, `BookingService`, `BookingRepository`, `Booking`, `BookingController`, `ReceptionistWalkInController`, `UserBookingEditController`, `ProfileController`, `AdminDoctorController`, `AdminPostController`, `AdminServiceController`, `AdminCandidateController`, `CareerController`, `NewsFeedServiceImpl`, `DataInitializer`, `AiController`, 5 task, `checkout-qr.html`, `candidate-list.html`, `db/manual/*.sql`, `deploy/*` — build-and-run.md, environment-setup.md, authentication-and-roles.md, booking-flow.md, supporting-subsystems.md, code-structure.md, coding-conventions.md
- 2026-08-11 — `fix:` **khách chọn xong bác sĩ, hệ thống lặng lẽ chốt sang NGƯỜI KHÁC** — khách hỏi bác sĩ đánh giá/chuyên môn cao nhất, chọn một người, trợ lý bảo "em mở trang đặt lịch để anh/chị chọn khung giờ"; khách nói "chọn luôn giờ cho tôi chiều nay" thì bị đổi sang bác sĩ khác **mà không một lời giải thích**, chỉ vì người vừa chọn không đăng ký ca chiều đó — **(A) gốc rễ: không có chỗ nào nhớ bác sĩ khách vừa CHỌN**; câu tiếp theo gần như không bao giờ nhắc lại tên và model cũng hay bỏ tên khỏi `booking_target`, nên `requestedDoctorName` rỗng → không ai được ghim → `selectedDoctor = availableDoctors[0]`, tức đổi người sau lưng khách ngay sau khi họ vừa mất công so đánh giá để chọn; thêm `lastChosenDoctor`, **ghi ở ĐÚNG MỘT nơi là `finishBookingHandoff`** (mọi thẻ đã chốt đều đi qua đó) cộng `window.MediTrustChat.rememberChosenDoctor` cho lớp gọi vốn không dựng thẻ; **chỉ ghim khi CÙNG KHOA** (khách đổi triệu chứng sang khoa khác thì bác sĩ cũ hết liên quan), tên khách nêu đích danh vẫn thắng, `parseCancelIntent` xoá ghim — ghim xong thì phần giải thích tự đúng vì `/slot-alternatives` nhận `doctorId` và trả `reason = OFF_DUTY` kèm câu "không đăng ký ca làm việc"; kiểm chứng bằng dữ liệu thật: KHÔNG ghim thì cùng câu đó chốt sang *Nguyễn Thị Vân Anh*, CÓ ghim thì giữ *Vũ Hữu Hoàng Anh* + `fallback` + lý do — **(B)** nới luật chống-đổi-người: `!selectedDoctor && (requestedDoctorName || candidateDoctorName)` trả `doctorNotFound` thay vì rơi về `availableDoctors[0]` — **(C) chọn xong là gợi ý luôn khung gần nhất**: thêm `offerNearestSlot` gọi `/api/chat/doctor-availability` (days=7), lấy ngày đầu tiên có `firstFreeSlot`; **ngày đó không phải ngày đầu dải thì BẮT BUỘC đọc `anchor.reasonText` trước** để khách biết vì sao phải dời, chứ không đọc trống một cái ngày khác rồi để họ tự đoán; 7 ngày không còn khung nào thì **HỎI** có muốn tìm bác sĩ khác không và để `awaitingConfirm = null` (không có lịch nào để một tiếng "vâng" chốt), tuyệt đối không tự đổi; `confirmBooking` mở `/appointment` đã điền sẵn bác sĩ + ngày + khung, giọng nói vẫn không tự tạo lịch hẹn — tiện thể `buildAppointmentUrl` được mở ra qua `MediTrustChat` để nút "Chọn" trên thẻ và câu mời của loa dùng chung một hàm dựng URL — `ai-chat.js`, `meditrust-voice-call.js` — ai-assistant.md
- 2026-08-11 — `feat:`/`fix:` **chế độ GỌI không đọc danh sách bác sĩ gợi ý — khách rảnh tay nghe được đúng sự im lặng** — thẻ chat in ra đầy đủ nhưng loa không nói gì, vì `notifyReply` **chỉ mang `availability`** sang lớp gọi; ba nhánh tra cứu còn lại (danh sách bác sĩ theo tiêu chí, hồ sơ một bác sĩ, lịch hẹn của chính khách) rơi thẳng xuống `say(spoken)` — mà câu của model ở các lượt đó chỉ là lời dẫn rỗng "Dạ em xem giúp anh/chị ngay ạ", nên khách nghe xong không có một thông tin nào; nay payload mang cả `lookup` và `meditrust-voice-call.js` có `describeDoctorFilter` / `describeDoctorProfile` / `describeMyBookings`, kèm `resolveDoctorChoice` cho khách **chọn bằng lời** ("người thứ hai", "bác sĩ Thu Hà", "ai cũng được") — **(A) ngân sách một hơi đọc:** bản đầu dài 421 ký tự, vượt `MAX_CHUNK_CHARS` = 300 nên `splitIntoChunks` xé làm hai utterance và trình duyệt chèn quãng nghỉ thật **đúng vào dấu chấm** — tái hiện y nguyên lỗi "đứng hình giữa câu" vừa sửa hôm 10/08; thêm `SPEECH_BUDGET = 270` và **cắt dần bác sĩ cho tới khi lọt một hơi**, bỏ luôn câu "trong danh sách trên màn hình" (chế độ rảnh tay thì khách có nhìn màn hình đâu) — **(B)** `describeDoctorFilter` trả `{text, offered}` chứ không trả chuỗi: **danh sách giữ lại để chọn phải ĐÚNG BẰNG những người vừa đọc lên**, giữ nhiều hơn là "người thứ ba" chọn trúng một cái tên khách chưa từng nghe — **(C) một tiêu chí, một đơn vị, mọi dòng:** bản đầu ưu tiên đọc số sao cho ai có đánh giá, nên bảng xếp theo kinh nghiệm lại lẫn "5 sao" với "18 năm kinh nghiệm" — nghe thì không lướt ngược lên được như nhìn thẻ, nên khách mất hẳn cái để so — **(D)** `speakNumber` **loại null TRƯỚC khi `Number()`**, vì `Number(null)` là `0` chứ không phải `NaN`, tức bác sĩ chưa ai chấm sẽ được đọc lên là **"0 sao"** — đúng lời khẳng định mà cả tính năng này sinh ra để chặn — **(E)** chọn xong đặt `awaitingConfirm` là handoff **KHÔNG có khung giờ** và hỏi *"em mở trang đặt lịch để anh/chị chọn khung giờ nhé?"* chứ không phải "xác nhận lịch nhé?" (chưa có giờ nào để xác nhận), giọng nói vẫn TUYỆT ĐỐI không tự tạo lịch hẹn; `pendingChoiceList` bị xoá ở đầu mỗi `onChatReply` và trong `startCall`/`hangUp`/`enterEmergency` nên không sống sót sang câu hỏi khác hay cuộc gọi khác; `resolveDoctorChoice` khớp TÊN trước THỨ TỰ ("bác sĩ Hai" là tên người) và **không đoán bừa** — không nhận ra thì trả `null` cho model, vì đoán sai là mở trang đặt lịch với người khách không hề chọn; tiện thể `appointmentUrl` được gắn sẵn vào từng dòng trong `resolveDoctorFilter` để nút "Chọn" trên thẻ và câu mời của loa dùng CHUNG một URL — `ai-chat.js`, `meditrust-voice-call.js` — ai-assistant.md
- 2026-08-10 — `fix:` **"bác sĩ đánh giá tốt nhất" đang xếp hạng bằng 5 sao QUẢNG CÁO chứ không phải đánh giá thật** — trang `/doctors` cố ý hiện **5.0 cho bác sĩ chưa ai chấm** (`doc.rating || 5.0` — trong JS `0.0` là falsy, mà `getAverageRating` trả `0.0` chứ không `null`; `DoctorDTO(Doctor)` cũng mặc định 5.0), nên hỏi "bác sĩ đánh giá cao nhất" là trợ lý đem con số không ai chấm đi giới thiệu, đứng trên người thật sự được 4.8 sau 50 lượt — **(A) chưa hề có tiêu chí `rating`**: `/api/chat/doctors/filter` chỉ biết `price` và mặc định kinh nghiệm, prompt cũng chỉ liệt kê `experience | price | earliest`; thêm `sortBy=rating` với **khoá đầu tiên là CÓ ĐÁNH GIÁ HAY CHƯA, không phải điểm số** (`hasReview desc → avgRating desc → reviewCount desc → kinh nghiệm → id`; khoá thứ ba để 5.0 sau 20 lượt đứng trên 5.0 sau đúng 1 lượt), người chưa có đánh giá **xuống cuối chứ không bị loại** — cả viện mới có vài chục lượt, loại thẳng là trả `[]` và đáp một câu hỏi hợp lệ bằng "em chưa tìm được bác sĩ nào" — **(B)** điểm phải chấm TOÀN BỘ ứng viên **trước khi cắt** danh sách, mà `getAverageRating`+`countReviews` từng người là 2×132 truy vấn khi khách không nêu khoa; thêm `ReviewService.getRatingStats(ids)` gom **một** truy vấn, trả `RatingStats(average, count)` và **cố ý KHÔNG có mặt** bác sĩ chưa ai chấm (đừng `COALESCE` về 0.0 — chính chỗ đó là thứ phân biệt "5.0 thật" với "chưa ai chấm") — **(C) câu của khách không tới được nhánh nào**: "đặt lịch với bác sĩ có đánh giá tốt nhất" có chữ "đặt lịch" nên `looksLikeDoctorFilterQuestion` tự loại, mà luồng đặt lịch **không có gì để chốt** (không tên, không giờ, thường không cả khoa) nên hoặc chết ở `NO_DEPARTMENT` hoặc lấy đại `availableDoctors[0]` — tức chọn hộ khách đúng cái việc khách vừa nhờ so sánh; thêm `DOCTOR_SUPERLATIVE` (**bắt buộc có chữ "nhất"**, để "bác sĩ Bình đánh giá mấy sao?" vẫn là `doctor_info` hỏi về MỘT người) làm ngoại lệ đứng trên luật "câu có chữ đặt lịch thuộc luồng đặt lịch", kèm cửa thoát tương ứng trong `resolveBookingHandoff` — thiếu nó thì cả hai nhánh cùng chạy: vừa in bảng so sánh vừa đặt soft-lock 3 phút và bật đếm ngược cho một dòng trong bảng — **(D)** `extractDoctorName` coi cả cụm tiêu chí là TÊN NGƯỜI nên khách nhận nguyên văn *"Em chưa tìm thấy bác sĩ có đánh giá tốt nhất"*; bổ sung `NOT_A_DOCTOR_NAME`: `danh gia`, `uy tin`, `noi tieng`, `kinh nghiem`, `tay nghe`… — **(E)** **"đánh giá" và "chuyên môn" là HAI tiêu chí, không được gộp**: đánh giá là điểm bệnh nhân khác chấm, chuyên môn là số năm + học vị; thẻ chat nay **nói rõ đã xếp theo tiêu chí nào**, in kèm **số lượt** cạnh điểm (`⭐ 4.8 (12 đánh giá)` — số lượt mới là thứ phân biệt điểm thật với điểm mặc định), chèn dòng "Các bác sĩ dưới đây chưa có lượt đánh giá nào ạ:" tại ranh giới, và nói thẳng khi **không ai** có đánh giá thay vì im lặng đưa ra một danh sách để khách tự hiểu là bảng xếp hạng sao; 5 sao quảng cáo ở `/doctors` **giữ nguyên** — đó là lựa chọn kinh doanh, chỉ khung chat là không được nhắc lại nó; `/skills/ai-schema-change` KHÔNG đổi (vẫn 10 key, chỉ thêm một giá trị vào enum `lookup.filter.sort_by`) — `ReviewRepository`, `ReviewService(+Impl)`, `PatientChatLookupApiController`, `AiService`, `ai-chat.js` — ai-assistant.md, supporting-subsystems.md
- 2026-08-10 — `fix:` **trợ lý luôn gợi ý CÙNG MỘT bác sĩ và khẳng định sai "khung giờ trống sớm nhất"** — khách gõ *"tôi đau bụng"* rồi xin 9h30 sáng thì luôn bị chốt đúng một người (BS. Nguyễn Văn Quân — id nhỏ nhất khoa Tiêu hoá), kèm câu "khung giờ trống sớm nhất", trong khi nhiều bác sĩ cùng khoa trống đúng 9h30; hoá ra **chưa từng có xếp hạng nào cả** (`grep '\.sort('` trong `ai-chat.js` = 0 kết quả) — **(A)** trình duyệt lấy thẳng `availableDoctors[0]`; **(B)** endpoint `/api/chat/doctors/department/{id}` **MÙ GIỜ** (không hề có tham số time/session/date), `findByDepartmentId` **không `ORDER BY`**, và `.limit(3)` chạy **TRƯỚC KHI** tính chỗ trống — mỗi khoa ~6 bác sĩ nên **một nửa khoa không bao giờ được xét**, đúng những người khách bảo "có thể đăng ký gần nhất"; lại quét theo BÁC SĨ và dừng ở ngày mở đầu tiên của CHÍNH MÌNH nên preview người A là ngày mai còn người B là thứ Sáu tuần sau, hai người không bao giờ so được với nhau — nay nhận `date`/`time`/`session`, **quét theo NGÀY** (cả khoa cùng được chấm trên một ngày, dừng ở ngày đầu có người rảnh) và xếp theo *ghim đích danh → trống đúng giờ khách xin → ít ca quanh giờ đó → tổng ca ngày → kinh nghiệm → id*, cắt 3 **SAU** khi sort; **luật sống còn: xếp hạng chỉ ĐỔI THỨ TỰ, KHÔNG LOẠI AI** — lọc bỏ người bận sẽ làm bác sĩ khách nêu đích danh mà kín lịch biến mất (→ `doctorNotFound`, "chưa tìm thấy bác sĩ X" về người có thật) và khoa kín lịch trả `[]` (→ `NO_DOCTORS`, giết luôn đường giải thích lý do); **(C)** danh sách đã xếp hạng thì **có sẵn mà bị vứt đi**: `/slot-alternatives` trả `sameTimeDoctors` kể cả khi bác sĩ được chọn đang rảnh, nhưng nhánh thành công dựng handoff không gắn `alternatives` — nay gắn, thẻ đã chốt liệt kê bác sĩ khác kèm nút **Đổi** (dùng chung `buildDoctorChoiceRow` với nút "Chọn" của thẻ hỏng, đều là `<a href>` nên **không phát sinh `/hold-slot` thứ hai**), bắt buộc `pickOtherDoctors` **loại chính người vừa chọn** vì server không tự loại; **(D)** câu "khung giờ trống sớm nhất" in ra mỗi khi `suggested = true` với **ĐÚNG 0 dữ liệu** về bác sĩ khác — nay hoặc nêu **lý do thật** ("vì quanh giờ đó bác sĩ chỉ có 1 ca"), hoặc giữ so sánh nhất nhưng **nêu rõ phạm vi** ("sớm nhất **của bác sĩ**"); không có số liệu thì **không nêu lý do** (`null` = im lặng, không phải "0 ca"), và `describeLoad` ("quanh giờ đó") chỉ được dùng khi khách thật sự nêu giờ — không thì `describeDayLoad` ("hôm đó"); **(E)** tách `parseWishes`/`buildDeptUrl` vì endpoint có **hai** nơi gọi — dải thẻ bác sĩ trước đây không gửi giờ nên khách đọc 3 thẻ rồi thẻ xác nhận bên dưới chốt một bác sĩ **thứ tư** không hề có trên màn hình; `buildDeptUrl` cố ý **không gửi `date`** khi khách chưa nêu ngày (ngày lúc đó mới được quyết định từ chính response này — vòng tròn), server trả `matchedDate` cho biết đã chấm cả khoa trên ngày nào; tiện thể `scheduleKnown` chuyển sang **tính lười** vì `hasRegisteredSchedule` gọi lại đúng `findEffective` mà `offDutySlots` vừa gọi — bớt ~1/3 truy vấn cho mọi nơi dùng `DaySlots` — `AiController`, `DoctorDTO`, `ai-chat.js`, `meditrust-voice-call.js` — ai-assistant.md, booking-flow.md
- 2026-08-10 — `fix:` **trợ lý giọng nói nghỉ quá lâu ở mỗi dấu chấm, nghe như đứng hình giữa câu** — hai nguyên nhân chồng nhau, cùng nằm ở `MediTrustVoice.speak()`: **(A)** trần một utterance là **180 ký tự (~8 giây ở rate 1.5)**, trong khi lỗi thật của Chrome là hàng đợi TTS ngủ sau **~15 GIÂY chứ không phải sau N ký tự** — và cái đó đã có keep-alive lo từ lâu; hậu quả là một câu trả lời lịch bình thường (~230 ký tự) bị xé làm hai `SpeechSynthesisUtterance`, mà giữa hai utterance trình duyệt LUÔN chèn một quãng nghỉ thật, lại thêm việc `splitIntoChunks` chỉ cắt được tại `.!?` nên quãng nghỉ đó rơi **đúng vào dấu chấm** — nâng `MAX_CHUNK_CHARS` lên 300 (~13 giây, vẫn dưới watchdog và keep-alive đánh thức ở giây thứ 10) nên câu trả lời thường giờ chỉ còn MỘT utterance, hết hẳn quãng hở; **(B)** giọng Việt tự ngắt ở dấu chấm lâu hơn hẳn dấu phẩy, mà `toSpeechText` lại **tự chế thêm dấu chấm**: mỗi `<br>` và mỗi thẻ đóng `</p>/</div>/</li>` đều bị đổi thành `". "`, nên thẻ trả lời lịch ghép từ nhiều `<div>` bị đọc thành một chuỗi mảnh vụn rời rạc — thêm `joinSentences()` đổi dấu chấm GIỮA bài thành dấu phẩy, đặt trong `speak()` chứ không trong `toSpeechText()` để phủ luôn nhánh `raw: true` (các câu tiếng Việt tự viết trong lớp gọi thoại, vốn là chỗ ghép nhiều mệnh đề nhất); ba thứ tuyệt đối không được đụng: **dấu chấm CUỐI** (câu cần chỗ hạ giọng), **`?` và `!`** (trợ lý luôn kết bằng câu hỏi — mất ngữ điệu hỏi là khách không biết mình đang được hỏi), và dấu chấm **không có khoảng trắng phía sau** (đó là dấu phân cách hàng nghìn kiểu Việt Nam, `350.000 đ`); tiện thể keep-alive `pause()`/`resume()` chỉ còn bật khi nội dung **> 150 ký tự** — cú pause/resume giữa chừng có thể tạo tiếng giật, mà câu ngắn thì chắc chắn đọc xong trước watchdog nên không đáng trả giá đó; **không dùng lookbehind** (Safari cũ coi là lỗi cú pháp và vứt cả file), và `\p{L}` với cờ `u` được bọc try/catch hạ xuống bản ASCII — `meditrust-voice.js` — coding-conventions.md, ai-assistant.md
- 2026-08-09 — `feat:`/`fix:` **chatbot trả lời được câu HỎI về lịch làm việc, thôi nói mập mờ, thôi âm thầm đổi ngày — kèm 4 nhóm tra cứu dữ liệu thật** — khách gõ *"bác sĩ X chiều nay bận à?"* nhận một câu chung chung rồi bị đẩy sang ngày khác không hiểu vì sao; đọc code thì là **ba lỗi chồng nhau**: **(A) câu HỎI không có đường nào tới dữ liệu** — `/api/chat/slot-alternatives` chỉ có ĐÚNG MỘT nơi gọi nằm trong `resolveBookingHandoff`, mà hàm đó thoát ngay khi `booking_intent = false`, tức **hệ thống chỉ tra được lịch khi khách đang ĐẶT chứ không tra được khi khách HỎI**, trong khi mục 1 và 5B của prompt CẤM model tự khẳng định bác sĩ có khám hay không và hứa "hệ thống lo phần lịch" — lời hứa chưa từng được thực hiện ở nhánh này nên bên dưới trống trơn và khách tự hiểu thành "bác sĩ đang rảnh"; thêm `GET /api/chat/doctor-availability` (endpoint RIÊNG chứ không tái dùng `/slot-alternatives`: hàm kia bắt buộc `departmentId` và duyệt cả khoa, về ngữ nghĩa là một LỜI MỜI ĐẶT LỊCH nên dùng lại là mời giao diện gán vào `pendingAlternatives`, lại chỉ xử lý được MỘT ngày nên không trả lời nổi "tuần này bác sĩ làm ngày nào") — **(B) câu chữ mập mờ chết người** — `buildReasonText` in *"bác sĩ X không có ca khám ạ"*, tiếng Việt đọc được **hai nghĩa ngược nhau** ("không đăng ký ca làm việc" vs "không có ai đặt khám"), đây đúng chỗ khách hỏi lại "vậy sao không đặt cho tôi?"; viết lại toàn bộ theo hai vế ĐỐI NHAU — `OFF_DUTY` luôn nói **"không đăng ký ca làm việc"**, `BOOKED` luôn mở đầu **"bác sĩ CÓ khám … nhưng"** — và **xoá hẳn cụm "không có ca khám" khỏi mã nguồn** — **(C) đổi ngày sau lưng khách** — `findOtherTimes` quét tới 7 ngày và trả khung của NGÀY KHÁC, nhưng thẻ chat chỉ nhắc ở một dòng phụ nên khách bấm nút là tới nhầm hôm; thêm `otherTimesDate`/`otherTimesMovedDay`/`otherTimesText` và đưa câu đổi ngày lên **DÒNG TIÊU ĐỀ** ở cả thẻ chat lẫn câu đọc — **(D) `scheduleKnown`** — `slotsOutsideWorkingHours` trả rỗng cho CẢ HAI trường hợp "khám cả ngày" và "chưa đăng ký lịch nào", nên `workingRanges()` **bịa ra một ngày làm việc 07:30-11:30 + 13:30-17:30** cho bác sĩ chưa từng đăng ký; thêm `BookingService.hasRegisteredSchedule` **chỉ cho lớp giải thích**, luật đặt lịch giữ nguyên "chưa đăng ký = không giới hạn" (siết chỗ đó là khoá sạch lịch 132 bác sĩ seed) — **(E) `worstReasonIn` → `summarizeReasonsIn`** — hàm cũ ưu tiên `BOOKED > … > OFF_DUTY`, **ngược hẳn javadoc của `blockReason`**, nên buổi chiều 6 khung ngoài ca + 2 khung có người đặt chỉ báo "đã có người đặt" và khách lại tưởng bác sĩ rảnh cả buổi; nay đếm theo từng lý do, nêu CẢ HAI (tối đa 2 mệnh đề vì lớp giọng nói đọc nguyên văn), loại `PAST` khỏi vế phụ vì "đã trôi qua" là tính chất của đồng hồ chứ không phải của bác sĩ — **(F) `extractDateHint` không hiểu "chiều nay"** nên câu hỏi không có ngày, hệ thống mượn `lastHandoffDate` và trả lời về một ngày khách không hề hỏi; cố ý KHÔNG gọi `sessionAlreadyPassed` ở nhánh này (hỏi lúc 18h về "chiều nay" là hỏi buổi chiều VỪA QUA, đẩy sang tuần sau là trả lời câu khác) — **(G) nhận diện** — key JSON thứ 10 `lookup` (gom cả 4 nhóm tra cứu vào MỘT key để lần sau khỏi migrate schema nữa, `MAX_TOKENS` 1200→1500, thêm mục 5D và một gạch đầu dòng ở 5B nói rõ `lookup` chỉ là trường ĐỊNH TUYẾN — thiếu câu đó model đọc key mới thành giấy phép tự trả lời, đảo ngược đúng luật sinh ra nó) **cộng** regex `looksLikeAvailabilityQuestion` vớt khi model bỏ sót; ở đây **regex đứng TRÊN tín hiệu model**, ngược doctrine hiện tại, vì model bị chính prompt cấm suy luận về lịch nên tín hiệu của nó yếu hơn; `resolveBookingHandoff` được thêm cửa thoát vì **`booking_intent` DÍNH từ lượt trước** (mục 5 bắt model giữ nguyên trường khách không nhắc) nên giữa luồng đặt lịch một câu hỏi vẫn đặt soft-lock 3 phút và bật đếm ngược chuyển trang — **(H) 4 nhóm tra cứu**: lịch làm việc cả tuần (miễn phí từ `week[]`), **lịch hẹn của chính khách** (`/api/chat/my-bookings`, **phải khai `authenticated()` ở KHỐI 0** vì `/api/chat/**` là permitAll và Spring lấy luật khớp đầu tiên — thiếu dòng đó là mở lịch hẹn bệnh nhân cho người lạ; mỗi dòng kèm `whyCannotCancel`/`whyCannotReschedule` nên thẻ chat không mời bấm nút chắc chắn lỗi), **giá khám & đánh giá thật** (`getAverageRating` trả **0.0 chứ không null** nên "chưa có đánh giá" phải xét `reviewCount`, đúng con bug từng báo đỏ mọi bác sĩ mới), **lọc bác sĩ theo tiêu chí** (giới tính đọc từ `User.gender`, TUYỆT ĐỐI không thêm field vào `Doctor` vì entity đó dùng `@AllArgsConstructor` theo vị trí trong `DataInitializer`) — bất biến của mọi nhánh tra cứu: **không gán `pendingAlternatives`, không gọi `/hold-slot`, không đếm ngược, không ghi `lastHandoffDate`** (ba cái đầu tự đúng nhờ không bao giờ gọi `finishBookingHandoff`; cái cuối cần biến riêng `lastAvailabilityDoctor`), thẻ dùng **màu xanh thông tin** chứ không phải hổ phách "yêu cầu của bạn hỏng", lớp giọng nói bắt buộc `awaitingConfirm = null` và kết bằng câu hỏi MỞ vì không có lịch hẹn nào để một tiếng "vâng" chốt — `AiController`, `BookingService(+Impl)`, `PatientChatLookupApiController` (mới), `SecurityConfig`, `AiService`, `AiRequest`, `ai-chat.js`, `meditrust-voice-call.js` — ai-assistant.md, booking-flow.md, authentication-and-roles.md, code-structure.md, ai-schema-change/SKILL.md
- 2026-08-09 — `fix:` **rà soát toàn dự án: khóa hẳn mọi thao tác lên dữ liệu QUÁ KHỨ thay vì cho bấm rồi mới báo lỗi** — **(A) tạo lịch cho quá khứ:** `reserve()` chưa từng kiểm tra ngày/giờ, mà `min` của ô ngày cùng việc gạch bỏ khung giờ đã qua đều tính lúc **tải trang** — tab mở từ hôm qua (hoặc POST tự chế) vẫn đặt được lịch cho ngày đã qua **và ví vẫn bị trừ tiền**; thêm sàn chung ở `reserve()` nhưng **cố ý chỉ chặn tới mức NGÀY**, vì quầy lễ tân đăng ký khách vãng lai đang đứng trước mặt nên khung 13:30 vẫn phải nhận được lúc 13:45 — luật chặt hơn ("khung giờ phải còn ở tương lai") đặt ở `BookingController.processAppointment`, đúng ngữ cảnh bệnh nhân TỰ đặt — **(B) admin/lễ tân thao tác lên lịch đã qua:** đường của bác sĩ đã có `whyStaffCannotChange` từ 2026-07-30 nhưng **admin và lễ tân bị bỏ sót**: admin "xác nhận" được lịch của năm ngoái (email + chuông vẫn bay đi) và hủy được ca `COMPLETED` → hoàn tiền cho lần khám có thật; lễ tân `confirm` chỉ xét `status == PENDING` nên một dòng PENDING từ tuần trước vẫn lọt; nay cả hai đều gọi `whyStaffCannotChange` và **hai template nhận `actionBlockReasons` để ẩn nút + in lý do** (chỉ in cho dòng PENDING/CONFIRMED, vì CANCELED/COMPLETED thì cột Trạng thái đã nói rồi); tiện thể `deleteBooking` được bù luật "chỉ xóa lịch đã hủy/đang chờ" vốn **chỉ nằm trong template**, nên URL gõ tay xóa vĩnh viễn được cả ca đã khám kèm hồ sơ bệnh án — **(C) hàng chờ khám:** `pushToEndOfQueue` chỉ xét trạng thái còn `resetQueuePosition` **không kiểm tra gì cả**, mà bộ lọc ngày không chặn quá khứ → lễ tân lùi về tháng trước là gắn/gỡ được nhãn "đến trễ" của ca đã khám xong; thêm `ReceptionService.whyCannotReorderQueue` làm nguồn sự thật duy nhất cho cả server lẫn giao diện, ngày cũ chỉ còn xem lại được — **(D) hủy/chuyển hàng loạt:** `findBookingsForChange` trả cả lịch quá khứ nên hủy hàng loạt cho ngày cũ là hoàn tiền cho những ca bệnh nhân đã đến khám; chặn theo **NGÀY** ở controller chứ **không** đụng vào `cancelWithRefund` (guard ở đó sẽ phá đúng tình huống công cụ này sinh ra: "bác sĩ ốm giữa buổi", nơi vài khung của hôm nay đã trôi qua), template ẩn trọn vùng thao tác nhưng **giữ bảng bệnh nhân để tra cứu** — **(E) nút đánh giá:** hiện với MỌI ca `COMPLETED` kể cả đã đánh giá, nên bệnh nhân mở modal, chấm sao, gõ nhận xét, bấm Gửi rồi mới nhận câu "Bạn đã đánh giá dịch vụ này rồi" và mất trắng phần đã gõ; thêm `ReviewService.hasReview` + `reviewedBookingIds` → đổi thành nhãn "Đã đánh giá" — **(F) phụ lục bệnh án:** `addMedicalAddendum` nhận `recordId` thẳng từ form và **không kiểm tra quyền sở hữu**, tức đổi một con số trong request là ghi thêm được vào bệnh án đã đóng của bệnh nhân do đồng nghiệp khám — đây là đường ghi duy nhất chạm tới dữ liệu lâm sàng đã chốt; nay buộc ca khám phải của chính mình và `recordId` phải khớp `bookingId` — **(G) 4 modal bắt điền xong mới báo lỗi:** ô ngày của đăng ký ca trực, đơn nghỉ phép, báo bận đột xuất (thêm cả `max` = ngày mai theo luật "trong vòng 24 giờ tới") và phân công trực của trưởng khoa nay có `th:min`/`th:max`; lưu ý `today` phải thêm vào `HeadApprovalController.prepare` chứ không phải `HeadRosterController` — `head/duty-roster` do controller kia render — `BookingServiceImpl`, `BookingController`, `AdminBookingController`, `ReceptionistBookingController`, `ReceptionistQueueController`, `ReceptionistScheduleChangeController`, `ReceptionService(+Impl)`, `ReviewService(+Impl)`, `ProfileController`, `DoctorMedicalRecordController`, `StaffWorkScheduleController`, `HeadApprovalController`, `admin/booking-list.html`, `receptionist/booking-list.html`, `receptionist/queue.html`, `receptionist/schedule-change.html`, `user/profile.html`, `staff/work-schedule.html`, `head/duty-roster.html` — booking-flow.md, coding-conventions.md, medical-records.md, supporting-subsystems.md
- 2026-08-08 — `feat:` **khám xong là bệnh nhân nhận ngay hồ sơ bệnh án + đơn thuốc điện tử (email & chuông)** — trước đây bác sĩ bấm "Lưu Bệnh Án" xong là bệnh án nằm im trong DB, bệnh nhân chỉ biết nếu tự vào `/user/profile` bấm xem; thêm `MedicalRecordDeliveryService(+Impl)` gửi song song email (template mới `email/medical-record-ready.html`: chẩn đoán + ICD-10, triệu chứng, chỉ số sinh tồn, BẢNG thuốc, lời dặn, **đính kèm PDF đơn thuốc**) và một `Notification` vào chuông — chuông cố ý dùng `push` chứ không `pushBookingEvent` vì link phải trỏ thẳng `/user/medical-record/view/{bookingId}`, hàm kia luôn gắn link về lịch sử đặt lịch bắt bệnh nhân tự dò lại đúng ca vừa khám; **4 quyết định là để tránh thất bại âm thầm**: (1) `deliver` gọi SAU khi `createAdvancedMedicalRecord` commit, không nhét vào trong service `@Transactional` đó — thư báo cho một bệnh án còn có thể rollback thì không rút lại được; (2) lời gọi nằm NGOÀI khối try/catch lưu bệnh án ở controller, vì rơi vào nhánh catch đó là bác sĩ bị đá ngược về form khám và lần Lưu sau chắc chắn báo *"Lịch hẹn này đã có hồ sơ bệnh án!"* — ca khám kẹt cứng chỉ vì lỗi gửi thư; (3) `deliver` KHÔNG `@Async` và KHÔNG `@Transactional` (phải ở luồng request nơi open-in-view còn mở session để dựng PDF và đọc quan hệ LAZY; phần chậm thật là SMTP thì đã `@Async` sẵn); (4) thư nhận `MedicalRecordMailDTO` **chuỗi thuần** chứ không nhận entity, vì `Booking.user`/`Booking.doctor`/`Doctor.user` đều LAZY và một proxy chưa nạp đọc ở luồng `@Async` sẽ ném `LazyInitializationException` **rơi đúng vào khối catch nuốt lỗi của EmailServiceImpl** — thư im lặng không tới, log trông y hệt lỗi SMTP; PDF bọc try/catch RIÊNG vì `resources/fonts/` **đang rỗng** (`buildPrescription` ném ngay, tức nút in phiếu thu/đơn thuốc của lễ tân cũng đang hỏng sẵn) — thiếu tệp đính kèm thì chấp nhận được vì đơn thuốc đã nằm trong thân thư, mất luôn cả thư thì không; tách `util/VitalSignFormatter` dùng chung với `PdfExportServiceImpl` để giấy cầm tay và thư trong hộp mail không ghi huyết áp theo hai kiểu; thêm bài test ĐẦU TIÊN của dự án (`MedicalRecordMailTemplateTest`) dựng thật template bằng **`SpringTemplateEngine`** — lỗi template ở đây không bao giờ tự lộ vì bị catch nuốt, và bản `TemplateEngine` trần dùng OGNL (laxer, lại còn không có trên classpath) nên test bằng nó sẽ xanh trong khi thư thật hỏng; test bắt được 3 biểu thức sai ngay lần đầu: SpEL bắt buộc hai vế `and`/`or` là boolean nên `${mail.medicines.isEmpty()} and ${mail.prescriptionText}` ném lỗi ép kiểu, và `th:text="${x} != null ? ..."` bị đọc thành giá trị rồi tới chữ literal — `MedicalRecordDeliveryService(+Impl)`, `MedicalRecordMailDTO`, `VitalSignFormatter`, `email/medical-record-ready.html`, `MedicalRecordMailTemplateTest` (mới), `EmailService(+Impl)`, `PdfExportServiceImpl`, `DoctorMedicalRecordController` — medical-records.md, supporting-subsystems.md, code-structure.md, coding-conventions.md, environment-setup.md
- 2026-08-08 — `feat:`/`fix:` **tin tức y tế: thôi bịa, chuyển sang thu thập từ báo chính thống** — `MedicalNewsTask` cũ KHÔNG hề lấy gì từ internet: nó chỉ bảo model *"hãy VIẾT một bản tin cảnh báo về một dịch bệnh đang bùng phát (VD: Cúm A, Sốt xuất huyết...)"* rồi lưu thẳng kết quả — không URL, không ngày, không nguồn, nên mọi con số ca mắc và địa danh trong bài đều do model nghĩ ra mà lại đứng dưới tên bệnh viện; nay thêm `NewsFeedService(+Impl)` tải bài THẬT về trước (RSS → lọc theo tuổi bài + `existsBySourceUrl` → bóc toàn văn), AI chỉ còn được TÓM TẮT đúng phần đã tải, bài vẫn vào DRAFT chờ admin duyệt — danh sách nguồn là **allow-list** ở `NewsSourceCatalog` (VnExpress + Tuổi Trẻ đã chạy thật, SKĐS để tắt chờ verify), mỗi nguồn bọc try/catch riêng nên một feed chết không làm rỗng cả mẻ (đã chứng kiến thật: VnExpress timeout mà Tuổi Trẻ vẫn về 37 bài); thêm `Post.sourceUrl`/`sourceName` (**nullable**, bẫy `NOT NULL` không DEFAULT) làm **khóa chống trùng thật sự** thay cho `findByTitleContainingIgnoreCase(nguyên tiêu đề)` vốn gần như không bao giờ khớp, kèm khối "Nguồn: … — Đọc bản gốc" ở `news-details.html`; **bắt buộc `Jsoup.clean(content, Safelist.basic())`** vì template render `th:utext` tức HTML sống trên trang công khai; bài khớp `OUTBREAK_KEYWORDS` được gắn tiền tố `[Cảnh báo y tế]` — thiếu bước này thì banner cảnh báo trong khung chat AI **chết lặng lẽ** vì tiêu đề thật từ báo không đời nào chứa cụm đó; thêm `AiService.getStatelessResponse` (không `AiChatSession`, không phát lại 6 tin nhắn của bài hôm trước, trả `null` thay vì câu tiếng Việt "Hệ thống AI đang bận..." mà code cũ đem đi `readTree`); đổi `fixedDelay` → cron (bản cũ chạy lại sau MỖI lần devtools restart) — **4 lỗi chỉ lộ ra khi chạy thật với feed thật**: nhãn cảnh báo so bằng `contains` trần nên bài *"…tìm khách mua **dịch vụ** tăng kích thước…"* bị gắn "[Cảnh báo y tế]" (chuỗi "dịch" nằm trong "dịch vụ"; y hệt bẫy "tả" trong "mô tả", "dại" trong "dại dột") nên nay so theo TỪ TRỌN VẸN bằng lối đệm dấu cách và bỏ hết từ khóa đơn mơ hồ; Tuổi Trẻ dùng `M/d/yyyy h:mm:ss AM` với **U+202F** (narrow no-break space) trước AM/PM, nhìn y hệt dấu cách nên **toàn bộ 37 bài của báo này bị loại** cho tới khi chuẩn hóa; CDN ảnh VnExpress trả **403 chỉ vì chữ "Bot"** trong User-Agent (cùng URL với chuỗi trình duyệt thì 200); và CDN đó trả **webp cho URL kết thúc bằng `.jpg`** nên phải lấy đuôi file theo `Content-Type` thật, vì Spring đặt Content-Type lúc phục vụ ảnh theo ĐUÔI FILE — tiện thể sửa 2 bẫy của form admin mà tính năng này chắc chắn kích hoạt: form bind `@ModelAttribute Post` nên `sourceUrl`/`sourceName` không có input là bị ghi rỗng khi admin sửa nháp, và form **chưa từng có ô `status`** trong khi `Post.status` khởi tạo sẵn `"PUBLISHED"` — mở nháp sửa một chữ rồi Lưu là bài tự xuất bản VÀ bắn thông báo tới toàn bộ bệnh nhân dù chưa ai bấm "Duyệt"; thêm `POST /admin/manage-news/fetch-now` (POST vì đây là thao tác ghi) để chạy tay thay vì đợi cron — `NewsFeedService(+Impl)`, `NewsSourceCatalog`, `NewsFeedItem` (mới), `MedicalNewsTask` (viết lại), `AiService`, `Post`, `PostRepository`, `AdminPostController`, `pom.xml` (+jsoup), `application.properties` (+`news.fetch.*`), `user/news-details.html`, `admin/post-form.html`, `admin/post-list.html` — supporting-subsystems.md, ai-assistant.md, environment-setup.md, build-and-run.md, code-structure.md
- 2026-08-07 — `feat:` **khai báo dị ứng (bệnh nhân tự khai + bác sĩ ghi nhận)** — bảng `allergies`, `AllergyRepository` và cả `MedicalRecordService.addPatientAllergy` đều đã có sẵn, nhưng **không có bất kỳ đường ghi nào** (`addPatientAllergy` chưa từng có ai gọi), nên bảng rỗng vĩnh viễn — kéo theo thẻ đỏ "CẢNH BÁO DỊ ỨNG" ở form khám **chưa từng hiện lần nào** và trợ lý AI đối chiếu đơn thuốc luôn báo "không ghi nhận dị ứng nào"; thêm `AllergyService(+Impl)` (chỗ DUY NHẤT ghi vào bảng, có chặn trùng tác nhân không phân biệt hoa thường) + `UserAllergyController` cho bệnh nhân tự khai trong tab **"Hồ sơ y tế" vốn đã có trên giao diện nhưng ruột chỉ là câu giữ chỗ**, + `DoctorMedicalRecordController.addAllergyDuringExam` cho bác sĩ ghi giữa ca khám — endpoint bác sĩ **trả JSON và gọi bằng `fetch`**, vì form khám là MỘT thẻ `<form>` lớn nên một lần tải lại trang là mất sạch triệu chứng/chẩn đoán/đơn thuốc đang gõ dở; thêm cột `Allergy.source` (`PATIENT`/`DOCTOR`) là **`String` chứ không enum** (bẫy cột `ENUM(...)` native của MySQL) và **cố ý nullable** (bẫy `NOT NULL` không DEFAULT vừa làm hỏng toàn bộ việc đặt lịch), dùng để: hiện badge nguồn trên thẻ đỏ, đưa vào prompt AI (model nay nói rõ mục nào "bệnh nhân tự khai, chưa được xác nhận lâm sàng"), và chặn bệnh nhân xoá mục do bác sĩ ghi — luật xoá nằm trong `AllergyService.whyCannotDelete` theo đúng khuôn `whyCannotCancel` nên nút bị ẩn và POST tự chế đều bị chặn bởi cùng một hàm; **xoá `MedicalRecordService.addPatientAllergy`** vì để cạnh đường mới là có hai lối vào một bảng mà chỉ một lối áp luật — `AllergyService(+Impl)` + `UserAllergyController` (mới), `Allergy`, `AllergyRepository`, `MedicalRecordService(+Impl)`, `ProfileController`, `DoctorMedicalRecordController`, `DoctorExamAiController`, `SecurityConfig`, `user/profile.html`, `doctor/medical-record-form.html` — medical-records.md, ai-assistant.md, authentication-and-roles.md, code-structure.md, environment-setup.md
- 2026-08-07 — `feat:`/`fix:` **đủ 8 ô AI Insight + 4 trợ lý AI tại form khám** — **(A)** dashboard bác sĩ chỉ có 1/8 ô có AI (ô "Đánh giá TB"); việc này từng làm ở `2287cc2` rồi bị revert bởi `25d2a3a` nên tài liệu mô tả sai thực tế — khôi phục nguyên 5 file (`DoctorInsightService(+Impl)`, `DoctorInsightDTO`, `DoctorDashboardStatsDTO`, fragment `ai-insight.html`) và áp tay 3 file đã đổi vì HEAD đi tiếp; câu chữ vẫn là **luật cố định Java, không gọi LLM**, AI thật chỉ chạy khi BẤM vào ô (một listener **uỷ quyền** + `preventDefault` vì 2 thẻ đầu nằm TRONG `<a>`, câu hỏi đi qua `data-ai-prompt` chứ không `th:onclick` vì tiếng Việt có dấu nháy làm vỡ JS), kéo theo xoá `/quick-review-advice` ở CẢ HAI nơi nên con số trên thẻ và câu nhận định dùng chung một biến — **(B)** sửa 2 lỗi đang sống mà bản revert đã cuốn theo: biểu đồ "Phân bố Đánh giá" **dán nhãn ngược hoàn toàn** (`getRatingDistribution` trả 5★→1★ còn trục X ghi 1★→5★) và bác sĩ **chưa có đánh giá nào bị báo đỏ oan** "cần khắc phục ngay" (vì `getAverageRating` trả `0.0` chứ không `null` nên nhánh "chưa đủ đánh giá" là code chết) — **(C)** thêm `DoctorExamAiController` (`/api/doctor/chat/exam/**`, tách khỏi `DoctorAiController` vốn đã dài, nằm cùng tiền tố nên **không cần luật phân quyền mới**) với 4 trợ lý ở form khám: đối chiếu đơn thuốc với **dị ứng đọc thẳng từ DB** (không nhận từ client — đây là dữ liệu an toàn người bệnh), soạn nháp lời dặn **bắt buộc đúng cụm "Tái khám sau N ngày"** mà `FollowUpReminderTask` bóc tách (viết tự do thì nhắc tái khám im lặng không chạy), gợi ý mã ICD-10, tóm tắt bệnh sử — cả 4 đều KHÔNG `@Transactional` (gọi mạng giữa hàm), đều qua `requireOwnedBooking` (403 giống nhau cho "không phải của bạn" và "không tồn tại" để không dò được id), và chỉ GỢI Ý chứ không tự ghi vào `MedicalRecord` — `DoctorExamAiController` + `ExamAssistRequest` (mới), `DoctorInsightService(+Impl)` + 2 DTO + `ai-insight.html` (khôi phục), `DoctorDashboardController`, `DoctorAiController`, `doctor/dashboard.html`, `doctor/medical-record-form.html` — ai-assistant.md, code-structure.md, medical-records.md, coding-conventions.md
- 2026-08-07 — `feat:`/`fix:` **chuông thông báo cho BỆNH NHÂN** — chuông ở header bệnh nhân vốn là thẻ `<a href="#">` chết (không dropdown, không badge, không script, không API), nên các `Notification` mà `FollowUpReminderTask` ĐÃ ghi cho bệnh nhân từ trước không có đường nào tới được họ; thêm `UserNotificationApiController` (`GET /api/notifications` + `POST /read`, tách khỏi `/api/staff/notifications` vì endpoint đó gộp 4 khối nhắc chỉ dành cho nhân viên), `user-notifications.js` (id `userNotif*` chứ không `staffNotif*` vì `medical-record-detail.html` nhúng cả hai khu), markup + script đặt trong fragment header nên **22 template** bệnh nhân có chuông chỉ với một lần sửa — **(B)** gắn `NotificationService.pushBookingEvent` (mới, gom câu tóm tắt + link về một chỗ cho 11 nơi gọi) cạnh MỌI lời gọi email hướng tới bệnh nhân: đặt lịch (ví/VNPay/VietQR/tại quầy), xác nhận (bác sĩ/admin/lễ tân), dời lịch, hủy lịch, đổi bác sĩ; riêng `ProfileController` phải gọi thêm vì nó tự hoàn tiền chứ không đi qua `cancelWithRefund` — **(C)** `pushToAllPatients` (`@Async`, `saveAll` theo lô 500, **không** kèm `@Transactional` vì thứ tự proxy `@Async`/`@Transactional` không xác định) bắn tin mới tới mọi `ROLE_USER`, guard chống trùng nằm ở `AdminPostController` (đọc trạng thái TRƯỚC khi ghi, ở cả `publishPost` lẫn `savePost`, vì `/publish/{id}` là GET nên F5 gọi lại được) — **(D)** `Booking.reminderSent` + `AppointmentReminderTask` (7h30 hằng ngày, nhắc lịch NGÀY MAI qua email + chuông, finder có `@EntityGraph` vì luồng cron không có open-in-view) + `EmailService.sendAppointmentReminder` dùng lại `general-notification.html` qua `sendGeneral` tách chung — tiện thể sửa 2 lỗi chặn đường: **`~{user/index :: footer}` là fragment KHÔNG tồn tại** ở 6 template (`career-details`, `careers`, `doctor-schedule`, `medical-process`, `payment-result`, `working-hours`) làm Thymeleaf vỡ render tại chỗ đó, mất footer lẫn mọi `<script>` phía sau — tức mất luôn Bootstrap nên chuông hiện mà không mở được; và `checkout-qr.html` / `payment-result.html` không nạp Bootstrap JS bản nào — `UserNotificationApiController` (mới), `AppointmentReminderTask` (mới), `user-notifications.js` (mới), `NotificationService(+Impl)`, `EmailService(+Impl)`, `Booking`, `BookingRepository`, `SecurityConfig`, `BookingServiceImpl`, `ReceptionServiceImpl`, `BookingController`, `PaymentController`, `VietQRController`, `ProfileController`, `AdminBookingController`, `AdminPostController`, `DoctorDashboardController`, `ReceptionistBookingController`, `ReceptionistWalkInController`, `user/include/header.html` + 8 template bệnh nhân — supporting-subsystems.md, authentication-and-roles.md, booking-flow.md, coding-conventions.md, medical-records.md, environment-setup.md
- 2026-08-06 — `feat:`/`fix:` hoàn thiện ô "AI Insight" trên dashboard bác sĩ: từ 1 ô lên đủ **8 ô** (3 thẻ thống kê còn lại + 4 panel biểu đồ) — thêm `DoctorInsightService(+Impl)` giữ toàn bộ luật, `DoctorInsightDTO` (advice + colorClass + **prompt**) và `DoctorDashboardStatsDTO` gói số liệu controller đã đọc sẵn; fragment dùng chung `doctor/include/ai-insight.html` thay cho markup lặp, một listener **uỷ quyền** + `preventDefault` thay 8 `onclick` (2 thẻ đầu nằm TRONG thẻ `<a>` nên bấm vào ô là vừa mở chat vừa nhảy trang), câu hỏi đi qua `data-ai-prompt` chứ không qua `th:onclick` vì tiếng Việt có dấu nháy làm vỡ JS — **bỏ hẳn `fetch`**: nội dung ô nay render tại server nên không còn spinner/nhánh lỗi mạng và con số trên thẻ với câu nhận định dùng chung một biến, kéo theo xoá `/quick-review-advice` ở CẢ HAI nơi (`DoctorAiController` + bản song sinh chết trong `DoctorDashboardController`) — tiện thể sửa 2 lỗi có thật ở đúng panel đang gắn insight: **(1)** biểu đồ "Phân bố Đánh giá" đọc ngược (`getRatingDistribution` trả 5★→1★ còn nhãn trục X ghi 1★→5★, cột 5 sao bị dán nhãn "1 Sao"), **(2)** bác sĩ chưa có đánh giá nào bị báo đỏ "cần khắc phục ngay" vì `getAverageRating` trả **0.0 chứ không null** nên nhánh "chưa đủ đánh giá" là code chết — `DoctorInsightService(+Impl)`, `DoctorInsightDTO`, `DoctorDashboardStatsDTO`, `DoctorDashboardController`, `DoctorAiController`, `doctor/dashboard.html`, `doctor/include/ai-insight.html` (mới) — ai-assistant.md, code-structure.md
- 2026-07-30 — `fix:` chuông thông báo bấm không ra gì ở trang bác sĩ/admin (trưởng khoa thì chạy) — `doctor/dashboard.html`, `doctor/booking-manager.html`, `admin/dashboard.html`, `admin/booking-list.html` vừa nhúng fragment footer (vốn đã nạp bootstrap.bundle) vừa tự khai lại thẻ `<script>` bootstrap, nên Bootstrap chạy HAI lần và data-api gắn hai handler lên `document`: một cú bấm chạy `toggle()` hai lần (mở rồi đóng ngay) làm mọi dropdown/modal/alert trên các trang đó như bị liệt — trang `/head/**` chỉ nạp một bản nên vẫn chạy; bỏ các thẻ script trùng, riêng `user/medical-record-detail.html` thêm `th:unless` vì bệnh nhân không được nhúng footer nên vẫn cần bản riêng — tiện thể sửa `~{user/index :: footer}` (fragment KHÔNG tồn tại) ở chính file đó, lỗi cũ làm trang xem hồ sơ của BỆNH NHÂN vỡ giữa chừng ở `</main>`, mất footer lẫn nút xuất PDF — `doctor/dashboard.html`, `doctor/booking-manager.html`, `admin/dashboard.html`, `admin/booking-list.html`, `user/medical-record-detail.html` — coding-conventions.md
- 2026-07-30 — `feat:`/`fix:` chặn mọi thao tác lên dữ liệu QUÁ KHỨ + thông báo quyết định + trưởng khoa tự xếp ca khoa — **(A) bác sĩ hủy được lịch quá khứ:** `confirmBooking`/`cancelBooking` chỉ kiểm tra quyền sở hữu nên bác sĩ hủy được cả lịch `COMPLETED` (ví bệnh nhân bị hoàn tiền cho ca đã khám thật) và "xác nhận" được lịch năm ngoái kèm email; thêm `BookingService.whyStaffCannotChange` (không áp mốc 24 tiếng để vẫn hủy được lịch đột xuất, so cả giờ nên ca 08:00 đóng từ trưa), controller chặn thật + template làm mờ nút kèm lý do qua `actionBlockReasons`, `cancelBooking` thôi tự hoàn tiền mà đi qua `cancelWithRefund` (kèm lý do vào sổ ví), và luật "chỉ khám đúng ngày hẹn" nay có ở server chứ không chỉ trong template — **(B) trưởng khoa duyệt được đơn/ca đã hết hiệu lực:** thêm `whyCannotDecide`/`whyCannotDecideShift` chặn cả duyệt lẫn từ chối khi kỳ nghỉ/ca đã trôi qua (giữ được việc "từ chối đơn đã duyệt" để thu hồi quyết định), bù guard trạng thái mà `rejectShift` chưa hề có, tách khối "Đơn đã hết hiệu lực" và `countPendingInDepartment` chỉ đếm việc còn làm được để chuông/thẻ đếm không báo mãi — **(C) bác sĩ không biết đơn được duyệt hay không:** thêm entity `Notification` + repository + service, ghi thông báo song song với email ở mọi quyết định, `/api/staff/notifications` gộp thông báo đã lưu với các nhắc tính động + `unreadCount` + `POST /read`, bỏ mục "quyết định 7 ngày" tính động (sẽ hiện hai lần), chuông chuyển vào fragment header dùng chung nên có trên MỌI trang nhân viên (`staff-notifications.js` mới, tách khỏi `work-schedule.js` vốn thoát sớm khi không có lưới lịch) — **(D) trưởng khoa chỉ có nút "tự xếp lịch":** thêm `/head/clinic-roster` (bảng Bác sĩ × Thứ × Sáng/Chiều, lưu 1 lần) kiểm theo ĐỘ PHỦ CỦA KHOA thay vì bắt mỗi bác sĩ đủ 7 ngày, nhưng vẫn buộc mỗi bác sĩ ≥1 ca/tuần vì tuần trống hoàn toàn bị `findEffective` hiểu là "chưa đăng ký" và rơi về mẫu tuần cũ, cùng `POST /head/duty-roster/assign` phân công trực (dùng nguyên `validateShift`, ca vào thẳng APPROVED) — `BookingService(+Impl)`, `DoctorDashboardController`, `DoctorBookingManagerController`, `DoctorBookingRequestController`, `DoctorMedicalRecordController`, `doctor/booking-manager.html`, `doctor/booking-requests.html`, `LeaveService(+Impl)`, `LeaveRequestRepository`, `StaffScheduleService(+Impl)`, `HeadApprovalController`, `HeadRosterController` (mới), `head/leave-requests.html`, `head/duty-roster.html`, `head/clinic-roster.html` (mới), `head/dashboard.html`, `head/include/sidebar.html`, `Notification` + `NotificationRepository` + `NotificationService(+Impl)` (mới), `ClinicRosterRowDTO` (mới), `StaffScheduleApiController`, `doctor/include/header.html`, `staff/work-schedule.html`, `staff-notifications.js` (mới), `work-schedule.js` — booking-flow.md, supporting-subsystems.md, code-structure.md, coding-conventions.md, environment-setup.md
- 2026-07-29 — `fix:` 2 lỗ hổng còn lại của chatbot — **(1) phân quyền:** `/api/doctor/**` khai `permitAll` TRƯỚC `/api/doctor/chat/**`.hasRole("DOCTOR") mà `DoctorAiController` lại `@RequestMapping("/api/doctor/chat")`, nên Spring lấy luật khớp đầu tiên và toàn bộ trợ lý AI của bác sĩ MỞ CÔNG KHAI (người lạ đọc được tên bệnh nhân, chẩn đoán, lịch khám); thêm khối 0 khai các API bị khoá quyền lên trên mọi `permitAll`; đồng thời `/api/doctors/search` (khác segment với `/api/doctor/**`) rơi xuống `authenticated()` làm khách vãng lai không tra được tên bác sĩ — **(2) lưới khung giờ thứ 12:** `DoctorAiController` tự liệt kê danh sách giờ riêng còn sót ca tối 17:30–19:00 nên báo bác sĩ "rảnh lúc 18:30" (giờ bệnh nhân không đặt được), lại bỏ qua `Schedule` nên báo trống cả ngày bác sĩ nghỉ; nay đọc `TimeSlotService.allSlots()` + lọc qua `BookingService.slotsOutsideWorkingHours`, và bỏ qua booking có `appointmentTime` không parse được (trước đây 1 dòng dữ liệu lạ là 500 cả trợ lý) — `SecurityConfig`, `DoctorAiController`, `TimeSlotService` — authentication-and-roles.md, ai-assistant.md, sync-slot-grid/SKILL.md
- 2026-07-28 — `fix:` rà soát toàn diện chatbot, sửa 30 lỗi/điểm mơ hồ chia 4 nhóm — **(A) đặt sai giờ/ngày:** nhánh "khách nêu giờ" của `resolveBookingHandoff` thôi tự dựng khung giờ từ `booking_target` của model và thôi hỏi `booked-slots` (API này không biết lưới nên "10h20"/"7 giờ tối" đều thành "còn trống"), nay đi qua `/slot-alternatives`; `normalizeTimeHint` không còn bắt chữ "h" của "hôm/hoặc" ("đau 3 hôm nay" → 15:00), hiểu "8h kém 15"; `extractDateHint` xét tên thứ trước "hôm nay/ngày mai", sửa "thứ 2 tuần sau" dư 7 ngày, chỉ nhận dấu "/" và validate ngày (hết cảnh "sốt 39-40 độ" → ngày 39/40), thêm ngày mốt / cuối tuần / N ngày nữa / mùng 5 tháng 8 — **(B) nhầm bác sĩ & ý định:** `pickBestDoctorMatch` trùng tên thì HỎI LẠI thay vì chọn bừa, so khớp không dấu; `extractDoctorName` có stoplist ("bác sĩ nào cũng được"), sửa thứ tự cắt dấu câu; thêm `parseCancelIntent` (gõ "thôi không đặt nữa" không còn bị chốt lịch) và `looksLikeBookingRequest` ("phí đặt lịch bao nhiêu?" không kích hoạt luồng đặt); `resolveAlternativeChoice` hiểu "đổi lịch sang bác sĩ khác" là đổi BÁC SĨ — **(C) im lặng khi lỗi + UX:** thay mọi `return null` câm bằng `{error}` có câu giải thích, phân biệt lỗi mạng với "không tìm thấy bác sĩ", thu hẹp try quanh `JSON.parse` (hết cảnh đổ `reasoning`/`patient_summary` ra chat), guard `ai_reply` thiếu, `safeStorage` bọc sessionStorage, đếm ngược 5s + nút Huỷ thay redirect 900ms, cờ `isSending` + `isComposing` — **(D) backend:** 2 NPE làm khách kẹt "Hệ thống bận" vĩnh viễn, timeout RestTemplate + bỏ `@Transactional` khỏi hàm gọi mạng, bỏ model id không tồn tại, log lỗi OpenRouter (HTTP 200 kèm `error`), `max_tokens`, validate input `/ask`, chặn ngày quá khứ/quá xa, `resolveCanonicalSlot` theo khung chứa, soft-lock chỉ đặt khi chốt (`POST /hold-slot`) — `AiService`, `AiController`, `AiRequest`, `AiResponse`, `BookingHealthyApplication`, `ai-chat.js`, `meditrust-voice-call.js` — ai-assistant.md, booking-flow.md, coding-conventions.md, sync-slot-grid/SKILL.md
- 2026-07-27 — `fix:` role trưởng khoa "chỉ thấy giao diện, không dùng được": tài khoản chỉ có ROLE_HEAD_DOCTOR bị đăng nhập rơi về `/` như bệnh nhân, admin liệt kê thành "Bệnh nhân", và không có chỗ gán khoa phụ trách nên `/head` báo chưa được gán — `SecurityConfig` (successHandler thêm nhánh HEAD_DOCTOR → `/head/dashboard`, đặt SAU DOCTOR), `OAuth2LoginSuccessHandler` (nhánh tương tự), `admin/manage-user.html` (hiển thị đủ 5 vai trò), `AdminController` (`syncHeadOfDepartment` + dropdown khoa khi tick Trưởng khoa, gán/gỡ `StaffProfile.headOfDepartment`), `admin/user-form.html` (nhãn vai trò tiếng Việt, ô "Khoa phụ trách", script ẩn/hiện) — authentication-and-roles.md
- 2026-07-26 — `fix:` khách xin "đổi sang buổi chiều" lại được xếp buổi SÁNG ngày khác — `ai-chat.js` (`extractSessionHint` bỏ alias `sang`/`toi` trùng giới từ "sang" và đại từ "tôi", xét chiều/tối trước sáng; giờ & ngày lấy từ câu khách trước, `lastHandoffDate` sau, `booking_target` của model cuối cùng vì model bịa giờ "09:00 - 11:00" và bịa ngày; khách nêu BUỔI cũng là `suggested` nên thẻ ghi "em chọn giúp ... trong buổi sáng"), `AiService` (mục 5B cấm tường thuật "em kiểm tra khung giờ ..." và cấm nêu giờ/ngày cụ thể trong `ai_reply`) — ai-assistant.md
- 2026-07-26 — `fix:` trợ lý AI mời khung giờ bác sĩ đang nghỉ, và khi không đặt được thì âm thầm đổi sang giờ khác mà vẫn ghi "khung giờ anh/chị vừa chọn" — `AiController` (2 endpoint lọc theo `Schedule` qua `BookingService`, `slotBlockReason`/`DaySlots`, `reason`+`reasonText`+`requestedDoctorWorkingRanges`, tham số `session`, quét sang ngày làm việc kế tiếp, bỏ 2 injection chết), `ai-chat.js` (`extractDateHint` hiểu tên thứ/`tuần sau`, `extractSessionHint`, `isSlotBookable`, `makeHandoff`, dựng lại `resolveBookingHandoff` 3 nhánh, thẻ `suggested` nói thật, sửa khóa `meditrust_session_id` bị lọt khoảng trắng), `meditrust-voice-call.js` (đọc lý do, nhánh fallback luôn là câu hỏi mở), `AiService` (mục 1 + 5B, `buildTodayBlock`), `BookingServiceImpl.rescheduleByUser` (chặn ngoài ca khám) — ai-assistant.md, booking-flow.md, sync-slot-grid/SKILL.md
- 2026-07-25 — `docs:` thêm 2 skill quy trình cho các điểm hay vỡ vì lặp thủ công: `sync-slot-grid` (11 nơi khai báo lưới khung giờ) và `ai-schema-change` (9 keys JSON + 4 script frontend); sửa 2 chỗ tài liệu sai — `.claude/skills/sync-slot-grid/SKILL.md`, `.claude/skills/ai-schema-change/SKILL.md` — booking-flow.md (bổ sung `doctor-details.html`, `index.html` vào danh sách template), ai-assistant.md (frontend đọc key là 4 file JS chứ không phải template; 2 chỗ đếm key viết khác nhau)
- 2026-07-25 — `fix:` đăng ký ca khám chỉ ăn vào TUẦN SAU (trước đây lưu một lần là đổi cả tuần hiện tại lẫn quá khứ), khóa lịch tuần hiện tại và các tuần đã qua, chốt đăng ký lúc 22:00 CN — `Schedule.weekStart` (cột mới), `ScheduleRepository.findEffective`/`findEffectiveOn`, `LeavePolicy.weekStartOf`/`CLINIC_DEADLINE_*`, `StaffScheduleServiceImpl` (`replaceClinicSchedules` theo tuần, `autoFillFullWeek`, `getEvents` gắn `readOnly`), `BookingServiceImpl`, `ScheduleInfoController`, `DoctorController`, `DataInitializer`, bỏ `DoctorService.registerSchedule`/`deleteSchedule` + route `/doctor/schedule/delete/{id}`, `work-schedule.html|js`, `work-schedule.css` — supporting-subsystems.md, booking-flow.md, code-structure.md
- 2026-07-25 — `feat:` đăng ký ca khám cho TUẦN SAU (mỗi ngày tối thiểu 1 ca), nhắc bác sĩ vào sáng CN + tự động xếp lịch cả tuần tối CN nếu chưa đăng ký, trưởng khoa nhắc/chốt thủ công — `StaffProfile.clinicRegisteredForWeek`, `StaffScheduleServiceImpl` (validate mỗi ngày ≥1 ca, `nextWeekStart`/`sendNextWeekRegistrationReminders`/`autoRegisterUnregisteredDoctors`), `ClinicRegistrationTask` (cron CN), `StaffScheduleApiController` (nhắc ở chuông), `HeadApprovalController` (`/head/clinic/remind|finalize`), `work-schedule.html`, `head/dashboard.html` — supporting-subsystems.md
- 2026-07-25 — `fix:` đặt lịch không theo lịch làm việc (bác sĩ chỉ đăng ký ca sáng vẫn chọn được giờ chiều) + trang bác sĩ lúc ẩn lúc hiện (tự ẩn thẻ khi hết chỗ) — `BookingService.isSlotWithinWorkingHours`/`slotsOutsideWorkingHours`, `BookingApi.booked-slots`, `BookingController.processAppointment` (chặn server), `user/doctors.html` (không ẩn thẻ, báo "Hết lịch trống") — booking-flow.md
- 2026-07-24 — `feat:` đăng ký ca khám theo tuần (bảng Thứ×buổi, lưu 1 lần), lịch full-width co giãn + Lịch tháng dạng ô ngày, trực lặp nhiều tuần; sửa lỗi ca khám hiện chồng 2 khối (do lưu cả StaffShift lẫn Schedule) — `StaffScheduleServiceImpl` (`saveClinicTemplate`, bỏ `syncClinicSchedule`, `getEvents` bỏ qua clinic StaffShift), `StaffWorkScheduleController` (`/clinic/save`, `repeatWeeks`), `work-schedule.js|css` (lưới tháng + full-width), `work-schedule.html` — supporting-subsystems.md
- 2026-07-24 — `feat:` lịch làm việc, lịch trực & nghỉ phép cho bác sĩ + lễ tân (đăng ký ca khám / phiên trực đúng luật, nghỉ phép theo quota BLLĐ 2019 & Luật BHXH 2024, role trưởng khoa duyệt đơn, tìm người thay ca, giao diện lịch theo ảnh mẫu) — `LeavePolicy`, `StaffProfile`/`StaffShift`/`LeaveRequest`/`ShiftCoverRequest` + enum, `StaffScheduleService`/`LeaveService`/`ShiftCoverService`, `controller/staff|head`, `StaffScheduleApiController`, `templates/staff|head`, `work-schedule.css|js`, `DataInitializer.ensureStaffProfiles/ensureHeadDoctors`, `SecurityConfig` — supporting-subsystems.md, authentication-and-roles.md, code-structure.md, booking-flow.md, environment-setup.md
- 2026-07-24 — `fix:` bỏ khung giờ tối 17:30–20:30 khỏi đặt khám (ca khám chỉ trong giờ hành chính, ngoài giờ là phiên trực) — `BookingApi`/`AiController` (`ALL_SLOTS`), `TimeSlotService`, `ReceptionServiceImpl`, `DoctorServiceImpl.registerSchedule`, `AiService` (prompt giờ làm việc), `ai-chat.js`, 5 template đặt lịch — booking-flow.md, ai-assistant.md
- 2026-07-23 — `fix:` đồng bộ toàn bộ ảnh bác sĩ về một phong cách (chân dung studio, áo blouse trắng, nền xám) cho cả 132 bác sĩ — `uploads/doctor-*.jpg`, `uploads/bs-*.jpg` — environment-setup.md
- 2026-07-23 — `feat:` bổ sung 5 bác sĩ cho mỗi chuyên khoa (110 bác sĩ + lịch trực + ảnh đại diện) — `DoctorSeedData` (mới), `DataInitializer.ensureExtraDoctors`, `uploads/bs-*.jpg` — environment-setup.md, code-structure.md
- 2026-07-24 — `fix:` khách chọn "hướng 1" / "đổi bác sĩ" mà trợ lý vẫn hỏi lại (model không hề biết hệ thống vừa gợi ý gì) — `ai-chat.js` (`resolveAlternativeChoice`, `handoffFromAlternative`, `buildAlternativeContext`, `finishBookingHandoff`) — ai-assistant.md
- 2026-07-24 — `feat:` khung giờ kín thì báo ngay và gợi ý bác sĩ cùng khoa ít ca khám nhất quanh giờ đó (kèm lý do), bỏ tự nhảy trang khi lệch giờ — `AiController./slot-alternatives`, `ai-chat.js` (`buildSlotFullHtml`), `meditrust-voice-call.js` (`describeSlotFull`), `AiService` (mục 5B/5C: cấm nhận vơ đã giữ chỗ, chống hỏi lặp) — ai-assistant.md, booking-flow.md
- 2026-07-24 — `fix:` đặt nhầm khung giờ khi khách sửa giờ (xin 10h30 nhưng điều hướng ra 10h) — `ai-chat.js` (`slotStartTime`, `normalizeTimeHint`, `lastHandoffDate`, cờ `fallback`), `meditrust-voice-call.js` (đọc rõ khi phải đổi giờ), `AiService` (luật ghi lại `appointment_time`) — ai-assistant.md
- 2026-07-23 — `fix:` đặt nhầm bác sĩ khi khách yêu cầu đổi người, trợ lý hỏi lại dù khách đã nói rõ, xưng hô em/anh-chị, tăng tốc độ đọc và giảm thời gian chờ — `AiController` (tham số `doctorId`), `ai-chat.js` (`pickBestDoctorMatch`, `extractDoctorName`, `doctorNotFound`), `meditrust-voice-call.js` (`parseYesNo`), `AiService` (luật xưng hô + luật đổi bác sĩ) — ai-assistant.md
- 2026-07-23 — `feat:` giao tiếp bằng giọng nói cho trợ lý AI (nút mic/loa ở cả 3 khung chat + chế độ gọi rảnh tay cho bệnh nhân) — `meditrust-voice.js`, `meditrust-voice-call.js`, `ai-chat.js` (`window.MediTrustChat`), `AiService` (`speech_reply`), 3 template `ai-chat` — ai-assistant.md, coding-conventions.md, environment-setup.md
- 2026-07-23 — `docs:` thêm quy tắc bắt buộc ghi nhật ký tiến độ vào `.claude/rules/` — `.claude/rules/progress-log.md`, `CLAUDE.md` — progress-log.md (mới)
- 2026-07-23 — `feat:` thay đổi lịch khám phía client (bệnh nhân tự dời lịch tại `/user/booking/edit/{id}`) — `UserBookingEditController`, `BookingService.whyCannotReschedule` / `rescheduleByUser`, `templates/user/` — booking-flow.md
- 2026-07-23 — `feat:` role lễ tân: đặt lịch tại quầy, hủy/dời lịch, hàng đợi khám — `controller/receptionist/`, `ReceptionServiceImpl.sortByQueue`, `DataInitializer.ensureReceptionistAccount` — authentication-and-roles.md, booking-flow.md
- 2026-07-21 — `feat:` AI điều hướng tự động đặt lịch (`booking_intent` / `booking_target`) — `AiService`, `templates/*/include/ai-chat*.html` — ai-assistant.md
- 2026-07-21 — `fix:` lỗi hiển thị ảnh avatar và thanh tìm kiếm — `ImageService`, `WebConfig`, static assets — —
