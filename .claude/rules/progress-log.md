# Progress Log & Documentation Upkeep (MANDATORY)

**Every time work is completed in this repo, the documentation must be updated in the same change.** Do not treat a task as finished until this rule has been applied — it is part of the task, not optional follow-up.

## What to update

1. **This file** — append an entry to the log below for every completed piece of work (new feature, bug fix, refactor, config change, removed code).
2. **The topic rule file(s) under `.claude/rules/`** — if the change alters how the app actually behaves, update the rule that owns that topic so it stays accurate:
   - booking / slots / cancel / reschedule / queue → [booking-flow.md](booking-flow.md)
   - roles, URL authorization, login flows → [authentication-and-roles.md](authentication-and-roles.md)
   - new controller/service/repository/entity layout, new package → [code-structure.md](code-structure.md)
   - AI prompt, JSON schema, models, memory → [ai-assistant.md](ai-assistant.md)
   - medical records cluster → [medical-records.md](medical-records.md)
   - wallet, recruitment, content, QR, email, dashboards → [supporting-subsystems.md](supporting-subsystems.md)
   - properties, DB, seed data, uploads, port → [environment-setup.md](environment-setup.md)
   - Maven, dependencies, run/build commands → [build-and-run.md](build-and-run.md)
   - conventions, gotchas, error handling, testing → [coding-conventions.md](coding-conventions.md)
3. **[CLAUDE.md](../../CLAUDE.md)** — only when a *new* rule file is created (add the `@` import) or when the one-line project description at the top no longer fits.

If a change spans several topics, update every rule file it touches. If a documented behaviour is deleted, delete the sentence describing it — stale rules are worse than missing ones.

## Entry format

Newest first. One line per completed unit of work:

```
- YYYY-MM-DD — `type:` short Vietnamese description — files/areas touched — rule files updated (or `—`)
```

`type` follows the commit convention: `feat:` / `fix:` / `refactor:` / `docs:` / `chore:`.

Keep entries short (one line). Details belong in the topic rule file, not here.

---

## Log
- 2026-08-09 — `feat:`/`fix:` **chatbot trả lời được câu HỎI về lịch làm việc, thôi nói mập mờ, thôi âm thầm đổi ngày — kèm 4 nhóm tra cứu dữ liệu thật** — khách gõ *"bác sĩ X chiều nay bận à?"* nhận một câu chung chung rồi bị đẩy sang ngày khác không hiểu vì sao; đọc code thì là **ba lỗi chồng nhau**: **(A) câu HỎI không có đường nào tới dữ liệu** — `/api/chat/slot-alternatives` chỉ có ĐÚNG MỘT nơi gọi nằm trong `resolveBookingHandoff`, mà hàm đó thoát ngay khi `booking_intent = false`, tức **hệ thống chỉ tra được lịch khi khách đang ĐẶT chứ không tra được khi khách HỎI**, trong khi mục 1 và 5B của prompt CẤM model tự khẳng định bác sĩ có khám hay không và hứa "hệ thống lo phần lịch" — lời hứa chưa từng được thực hiện ở nhánh này nên bên dưới trống trơn và khách tự hiểu thành "bác sĩ đang rảnh"; thêm `GET /api/chat/doctor-availability` (endpoint RIÊNG chứ không tái dùng `/slot-alternatives`: hàm kia bắt buộc `departmentId` và duyệt cả khoa, về ngữ nghĩa là một LỜI MỜI ĐẶT LỊCH nên dùng lại là mời giao diện gán vào `pendingAlternatives`, lại chỉ xử lý được MỘT ngày nên không trả lời nổi "tuần này bác sĩ làm ngày nào") — **(B) câu chữ mập mờ chết người** — `buildReasonText` in *"bác sĩ X không có ca khám ạ"*, tiếng Việt đọc được **hai nghĩa ngược nhau** ("không đăng ký ca làm việc" vs "không có ai đặt khám"), đây đúng chỗ khách hỏi lại "vậy sao không đặt cho tôi?"; viết lại toàn bộ theo hai vế ĐỐI NHAU — `OFF_DUTY` luôn nói **"không đăng ký ca làm việc"**, `BOOKED` luôn mở đầu **"bác sĩ CÓ khám … nhưng"** — và **xoá hẳn cụm "không có ca khám" khỏi mã nguồn** — **(C) đổi ngày sau lưng khách** — `findOtherTimes` quét tới 7 ngày và trả khung của NGÀY KHÁC, nhưng thẻ chat chỉ nhắc ở một dòng phụ nên khách bấm nút là tới nhầm hôm; thêm `otherTimesDate`/`otherTimesMovedDay`/`otherTimesText` và đưa câu đổi ngày lên **DÒNG TIÊU ĐỀ** ở cả thẻ chat lẫn câu đọc — **(D) `scheduleKnown`** — `slotsOutsideWorkingHours` trả rỗng cho CẢ HAI trường hợp "khám cả ngày" và "chưa đăng ký lịch nào", nên `workingRanges()` **bịa ra một ngày làm việc 07:30-11:30 + 13:30-17:30** cho bác sĩ chưa từng đăng ký; thêm `BookingService.hasRegisteredSchedule` **chỉ cho lớp giải thích**, luật đặt lịch giữ nguyên "chưa đăng ký = không giới hạn" (siết chỗ đó là khoá sạch lịch 132 bác sĩ seed) — **(E) `worstReasonIn` → `summarizeReasonsIn`** — hàm cũ ưu tiên `BOOKED > … > OFF_DUTY`, **ngược hẳn javadoc của `blockReason`**, nên buổi chiều 6 khung ngoài ca + 2 khung có người đặt chỉ báo "đã có người đặt" và khách lại tưởng bác sĩ rảnh cả buổi; nay đếm theo từng lý do, nêu CẢ HAI (tối đa 2 mệnh đề vì lớp giọng nói đọc nguyên văn), loại `PAST` khỏi vế phụ vì "đã trôi qua" là tính chất của đồng hồ chứ không phải của bác sĩ — **(F) `extractDateHint` không hiểu "chiều nay"** nên câu hỏi không có ngày, hệ thống mượn `lastHandoffDate` và trả lời về một ngày khách không hề hỏi; cố ý KHÔNG gọi `sessionAlreadyPassed` ở nhánh này (hỏi lúc 18h về "chiều nay" là hỏi buổi chiều VỪA QUA, đẩy sang tuần sau là trả lời câu khác) — **(G) nhận diện** — key JSON thứ 10 `lookup` (gom cả 4 nhóm tra cứu vào MỘT key để lần sau khỏi migrate schema nữa, `MAX_TOKENS` 1200→1500, thêm mục 5D và một gạch đầu dòng ở 5B nói rõ `lookup` chỉ là trường ĐỊNH TUYẾN — thiếu câu đó model đọc key mới thành giấy phép tự trả lời, đảo ngược đúng luật sinh ra nó) **cộng** regex `looksLikeAvailabilityQuestion` vớt khi model bỏ sót; ở đây **regex đứng TRÊN tín hiệu model**, ngược doctrine hiện tại, vì model bị chính prompt cấm suy luận về lịch nên tín hiệu của nó yếu hơn; `resolveBookingHandoff` được thêm cửa thoát vì **`booking_intent` DÍNH từ lượt trước** (mục 5 bắt model giữ nguyên trường khách không nhắc) nên giữa luồng đặt lịch một câu hỏi vẫn đặt soft-lock 3 phút và bật đếm ngược chuyển trang — **(H) 4 nhóm tra cứu**: lịch làm việc cả tuần (miễn phí từ `week[]`), **lịch hẹn của chính khách** (`/api/chat/my-bookings`, **phải khai `authenticated()` ở KHỐI 0** vì `/api/chat/**` là permitAll và Spring lấy luật khớp đầu tiên — thiếu dòng đó là mở lịch hẹn bệnh nhân cho người lạ; mỗi dòng kèm `whyCannotCancel`/`whyCannotReschedule` nên thẻ chat không mời bấm nút chắc chắn lỗi), **giá khám & đánh giá thật** (`getAverageRating` trả **0.0 chứ không null** nên "chưa có đánh giá" phải xét `reviewCount`, đúng con bug từng báo đỏ mọi bác sĩ mới), **lọc bác sĩ theo tiêu chí** (giới tính đọc từ `User.gender`, TUYỆT ĐỐI không thêm field vào `Doctor` vì entity đó dùng `@AllArgsConstructor` theo vị trí trong `DataInitializer`) — bất biến của mọi nhánh tra cứu: **không gán `pendingAlternatives`, không gọi `/hold-slot`, không đếm ngược, không ghi `lastHandoffDate`** (ba cái đầu tự đúng nhờ không bao giờ gọi `finishBookingHandoff`; cái cuối cần biến riêng `lastAvailabilityDoctor`), thẻ dùng **màu xanh thông tin** chứ không phải hổ phách "yêu cầu của bạn hỏng", lớp giọng nói bắt buộc `awaitingConfirm = null` và kết bằng câu hỏi MỞ vì không có lịch hẹn nào để một tiếng "vâng" chốt — `AiController`, `BookingService(+Impl)`, `PatientChatLookupApiController` (mới), `SecurityConfig`, `AiService`, `AiRequest`, `ai-chat.js`, `meditrust-voice-call.js` — ai-assistant.md, booking-flow.md, authentication-and-roles.md, code-structure.md, ai-schema-change/SKILL.md
- 2026-08-09 — `fix:` **rà soát toàn dự án: khóa hẳn mọi thao tác lên dữ liệu QUÁ KHỨ thay vì cho bấm rồi mới báo lỗi** — **(A) tạo lịch cho quá khứ:** `reserve()` chưa từng kiểm tra ngày/giờ, mà `min` của ô ngày cùng việc gạch bỏ khung giờ đã qua đều tính lúc **tải trang** — tab mở từ hôm qua (hoặc POST tự chế) vẫn đặt được lịch cho ngày đã qua **và ví vẫn bị trừ tiền**; thêm sàn chung ở `reserve()` nhưng **cố ý chỉ chặn tới mức NGÀY**, vì quầy lễ tân đăng ký khách vãng lai đang đứng trước mặt nên khung 13:30 vẫn phải nhận được lúc 13:45 — luật chặt hơn ("khung giờ phải còn ở tương lai") đặt ở `BookingController.processAppointment`, đúng ngữ cảnh bệnh nhân TỰ đặt — **(B) admin/lễ tân thao tác lên lịch đã qua:** đường của bác sĩ đã có `whyStaffCannotChange` từ 2026-07-30 nhưng **admin và lễ tân bị bỏ sót**: admin "xác nhận" được lịch của năm ngoái (email + chuông vẫn bay đi) và hủy được ca `COMPLETED` → hoàn tiền cho lần khám có thật; lễ tân `confirm` chỉ xét `status == PENDING` nên một dòng PENDING từ tuần trước vẫn lọt; nay cả hai đều gọi `whyStaffCannotChange` và **hai template nhận `actionBlockReasons` để ẩn nút + in lý do** (chỉ in cho dòng PENDING/CONFIRMED, vì CANCELED/COMPLETED thì cột Trạng thái đã nói rồi); tiện thể `deleteBooking` được bù luật "chỉ xóa lịch đã hủy/đang chờ" vốn **chỉ nằm trong template**, nên URL gõ tay xóa vĩnh viễn được cả ca đã khám kèm hồ sơ bệnh án — **(C) hàng chờ khám:** `pushToEndOfQueue` chỉ xét trạng thái còn `resetQueuePosition` **không kiểm tra gì cả**, mà bộ lọc ngày không chặn quá khứ → lễ tân lùi về tháng trước là gắn/gỡ được nhãn "đến trễ" của ca đã khám xong; thêm `ReceptionService.whyCannotReorderQueue` làm nguồn sự thật duy nhất cho cả server lẫn giao diện, ngày cũ chỉ còn xem lại được — **(D) hủy/chuyển hàng loạt:** `findBookingsForChange` trả cả lịch quá khứ nên hủy hàng loạt cho ngày cũ là hoàn tiền cho những ca bệnh nhân đã đến khám; chặn theo **NGÀY** ở controller chứ **không** đụng vào `cancelWithRefund` (guard ở đó sẽ phá đúng tình huống công cụ này sinh ra: "bác sĩ ốm giữa buổi", nơi vài khung của hôm nay đã trôi qua), template ẩn trọn vùng thao tác nhưng **giữ bảng bệnh nhân để tra cứu** — **(E) nút đánh giá:** hiện với MỌI ca `COMPLETED` kể cả đã đánh giá, nên bệnh nhân mở modal, chấm sao, gõ nhận xét, bấm Gửi rồi mới nhận câu "Bạn đã đánh giá dịch vụ này rồi" và mất trắng phần đã gõ; thêm `ReviewService.hasReview` + `reviewedBookingIds` → đổi thành nhãn "Đã đánh giá" — **(F) phụ lục bệnh án:** `addMedicalAddendum` nhận `recordId` thẳng từ form và **không kiểm tra quyền sở hữu**, tức đổi một con số trong request là ghi thêm được vào bệnh án đã đóng của bệnh nhân do đồng nghiệp khám — đây là đường ghi duy nhất chạm tới dữ liệu lâm sàng đã chốt; nay buộc ca khám phải của chính mình và `recordId` phải khớp `bookingId` — **(G) 4 modal bắt điền xong mới báo lỗi:** ô ngày của đăng ký ca trực, đơn nghỉ phép, báo bận đột xuất (thêm cả `max` = ngày mai theo luật "trong vòng 24 giờ tới") và phân công trực của trưởng khoa nay có `th:min`/`th:max`; lưu ý `today` phải thêm vào `HeadApprovalController.prepare` chứ không phải `HeadRosterController` — `head/duty-roster` do controller kia render — `BookingServiceImpl`, `BookingController`, `AdminBookingController`, `ReceptionistBookingController`, `ReceptionistQueueController`, `ReceptionistScheduleChangeController`, `ReceptionService(+Impl)`, `ReviewService(+Impl)`, `ProfileController`, `DoctorMedicalRecordController`, `StaffWorkScheduleController`, `HeadApprovalController`, `admin/booking-list.html`, `receptionist/booking-list.html`, `receptionist/queue.html`, `receptionist/schedule-change.html`, `user/profile.html`, `staff/work-schedule.html`, `head/duty-roster.html` — booking-flow.md, coding-conventions.md, medical-records.md, supporting-subsystems.md
- 2026-08-08 — `feat:` **khám xong là bệnh nhân nhận ngay hồ sơ bệnh án + đơn thuốc điện tử (email & chuông)** — trước đây bác sĩ bấm "Lưu Bệnh Án" xong là bệnh án nằm im trong DB, bệnh nhân chỉ biết nếu tự vào `/user/profile` bấm xem; thêm `MedicalRecordDeliveryService(+Impl)` gửi song song email (template mới `email/medical-record-ready.html`: chẩn đoán + ICD-10, triệu chứng, chỉ số sinh tồn, BẢNG thuốc, lời dặn, **đính kèm PDF đơn thuốc**) và một `Notification` vào chuông — chuông cố ý dùng `push` chứ không `pushBookingEvent` vì link phải trỏ thẳng `/user/medical-record/view/{bookingId}`, hàm kia luôn gắn link về lịch sử đặt lịch bắt bệnh nhân tự dò lại đúng ca vừa khám; **4 quyết định là để tránh thất bại âm thầm**: (1) `deliver` gọi SAU khi `createAdvancedMedicalRecord` commit, không nhét vào trong service `@Transactional` đó — thư báo cho một bệnh án còn có thể rollback thì không rút lại được; (2) lời gọi nằm NGOÀI khối try/catch lưu bệnh án ở controller, vì rơi vào nhánh catch đó là bác sĩ bị đá ngược về form khám và lần Lưu sau chắc chắn báo *"Lịch hẹn này đã có hồ sơ bệnh án!"* — ca khám kẹt cứng chỉ vì lỗi gửi thư; (3) `deliver` KHÔNG `@Async` và KHÔNG `@Transactional` (phải ở luồng request nơi open-in-view còn mở session để dựng PDF và đọc quan hệ LAZY; phần chậm thật là SMTP thì đã `@Async` sẵn); (4) thư nhận `MedicalRecordMailDTO` **chuỗi thuần** chứ không nhận entity, vì `Booking.user`/`Booking.doctor`/`Doctor.user` đều LAZY và một proxy chưa nạp đọc ở luồng `@Async` sẽ ném `LazyInitializationException` **rơi đúng vào khối catch nuốt lỗi của EmailServiceImpl** — thư im lặng không tới, log trông y hệt lỗi SMTP; PDF bọc try/catch RIÊNG vì `resources/fonts/` **đang rỗng** (`buildPrescription` ném ngay, tức nút in phiếu thu/đơn thuốc của lễ tân cũng đang hỏng sẵn) — thiếu tệp đính kèm thì chấp nhận được vì đơn thuốc đã nằm trong thân thư, mất luôn cả thư thì không; tách `util/VitalSignFormatter` dùng chung với `PdfExportServiceImpl` để giấy cầm tay và thư trong hộp mail không ghi huyết áp theo hai kiểu; thêm bài test ĐẦU TIÊN của dự án (`MedicalRecordMailTemplateTest`) dựng thật template bằng **`SpringTemplateEngine`** — lỗi template ở đây không bao giờ tự lộ vì bị catch nuốt, và bản `TemplateEngine` trần dùng OGNL (laxer, lại còn không có trên classpath) nên test bằng nó sẽ xanh trong khi thư thật hỏng; test bắt được 3 biểu thức sai ngay lần đầu: SpEL bắt buộc hai vế `and`/`or` là boolean nên `${mail.medicines.isEmpty()} and ${mail.prescriptionText}` ném lỗi ép kiểu, và `th:text="${x} != null ? ..."` bị đọc thành giá trị rồi tới chữ literal — `MedicalRecordDeliveryService(+Impl)`, `MedicalRecordMailDTO`, `VitalSignFormatter`, `email/medical-record-ready.html`, `MedicalRecordMailTemplateTest` (mới), `EmailService(+Impl)`, `PdfExportServiceImpl`, `DoctorMedicalRecordController` — medical-records.md, supporting-subsystems.md, code-structure.md, coding-conventions.md, environment-setup.md
- 2026-08-08 — `feat:`/`fix:` **tin tức y tế: thôi bịa, chuyển sang thu thập từ báo chính thống** — `MedicalNewsTask` cũ KHÔNG hề lấy gì từ internet: nó chỉ bảo model *"hãy VIẾT một bản tin cảnh báo về một dịch bệnh đang bùng phát (VD: Cúm A, Sốt xuất huyết...)"* rồi lưu thẳng kết quả — không URL, không ngày, không nguồn, nên mọi con số ca mắc và địa danh trong bài đều do model nghĩ ra mà lại đứng dưới tên bệnh viện; nay thêm `NewsFeedService(+Impl)` tải bài THẬT về trước (RSS → lọc theo tuổi bài + `existsBySourceUrl` → bóc toàn văn), AI chỉ còn được TÓM TẮT đúng phần đã tải, bài vẫn vào DRAFT chờ admin duyệt — danh sách nguồn là **allow-list** ở `NewsSourceCatalog` (VnExpress + Tuổi Trẻ đã chạy thật, SKĐS để tắt chờ verify), mỗi nguồn bọc try/catch riêng nên một feed chết không làm rỗng cả mẻ (đã chứng kiến thật: VnExpress timeout mà Tuổi Trẻ vẫn về 37 bài); thêm `Post.sourceUrl`/`sourceName` (**nullable**, bẫy `NOT NULL` không DEFAULT) làm **khóa chống trùng thật sự** thay cho `findByTitleContainingIgnoreCase(nguyên tiêu đề)` vốn gần như không bao giờ khớp, kèm khối "Nguồn: … — Đọc bản gốc" ở `news-details.html`; **bắt buộc `Jsoup.clean(content, Safelist.basic())`** vì template render `th:utext` tức HTML sống trên trang công khai; bài khớp `OUTBREAK_KEYWORDS` được gắn tiền tố `[Cảnh báo y tế]` — thiếu bước này thì banner cảnh báo trong khung chat AI **chết lặng lẽ** vì tiêu đề thật từ báo không đời nào chứa cụm đó; thêm `AiService.getStatelessResponse` (không `AiChatSession`, không phát lại 6 tin nhắn của bài hôm trước, trả `null` thay vì câu tiếng Việt "Hệ thống AI đang bận..." mà code cũ đem đi `readTree`); đổi `fixedDelay` → cron (bản cũ chạy lại sau MỖI lần devtools restart) — **4 lỗi chỉ lộ ra khi chạy thật với feed thật**: nhãn cảnh báo so bằng `contains` trần nên bài *"…tìm khách mua **dịch vụ** tăng kích thước…"* bị gắn "[Cảnh báo y tế]" (chuỗi "dịch" nằm trong "dịch vụ"; y hệt bẫy "tả" trong "mô tả", "dại" trong "dại dột") nên nay so theo TỪ TRỌN VẸN bằng lối đệm dấu cách và bỏ hết từ khóa đơn mơ hồ; Tuổi Trẻ dùng `M/d/yyyy h:mm:ss AM` với **U+202F** (narrow no-break space) trước AM/PM, nhìn y hệt dấu cách nên **toàn bộ 37 bài của báo này bị loại** cho tới khi chuẩn hóa; CDN ảnh VnExpress trả **403 chỉ vì chữ "Bot"** trong User-Agent (cùng URL với chuỗi trình duyệt thì 200); và CDN đó trả **webp cho URL kết thúc bằng `.jpg`** nên phải lấy đuôi file theo `Content-Type` thật, vì Spring đặt Content-Type lúc phục vụ ảnh theo ĐUÔI FILE — tiện thể sửa 2 bẫy của form admin mà tính năng này chắc chắn kích hoạt: form bind `@ModelAttribute Post` nên `sourceUrl`/`sourceName` không có input là bị ghi rỗng khi admin sửa nháp, và form **chưa từng có ô `status`** trong khi `Post.status` khởi tạo sẵn `"PUBLISHED"` — mở nháp sửa một chữ rồi Lưu là bài tự xuất bản VÀ bắn thông báo tới toàn bộ bệnh nhân dù chưa ai bấm "Duyệt"; thêm `POST /admin/manage-news/fetch-now` (POST vì đây là thao tác ghi) để chạy tay thay vì đợi cron — `NewsFeedService(+Impl)`, `NewsSourceCatalog`, `NewsFeedItem` (mới), `MedicalNewsTask` (viết lại), `AiService`, `Post`, `PostRepository`, `AdminPostController`, `pom.xml` (+jsoup), `application.properties` (+`news.fetch.*`), `user/news-details.html`, `admin/post-form.html`, `admin/post-list.html` — supporting-subsystems.md, ai-assistant.md, environment-setup.md, build-and-run.md, code-structure.md
- 2026-08-07 — `feat:` **khai báo dị ứng (bệnh nhân tự khai + bác sĩ ghi nhận)** — bảng `allergies`, `AllergyRepository` và cả `MedicalRecordService.addPatientAllergy` đều đã có sẵn, nhưng **không có bất kỳ đường ghi nào** (`addPatientAllergy` chưa từng có ai gọi), nên bảng rỗng vĩnh viễn — kéo theo thẻ đỏ "CẢNH BÁO DỊ ỨNG" ở form khám **chưa từng hiện lần nào** và trợ lý AI đối chiếu đơn thuốc luôn báo "không ghi nhận dị ứng nào"; thêm `AllergyService(+Impl)` (chỗ DUY NHẤT ghi vào bảng, có chặn trùng tác nhân không phân biệt hoa thường) + `UserAllergyController` cho bệnh nhân tự khai trong tab **"Hồ sơ y tế" vốn đã có trên giao diện nhưng ruột chỉ là câu giữ chỗ**, + `DoctorMedicalRecordController.addAllergyDuringExam` cho bác sĩ ghi giữa ca khám — endpoint bác sĩ **trả JSON và gọi bằng `fetch`**, vì form khám là MỘT thẻ `<form>` lớn nên một lần tải lại trang là mất sạch triệu chứng/chẩn đoán/đơn thuốc đang gõ dở; thêm cột `Allergy.source` (`PATIENT`/`DOCTOR`) là **`String` chứ không enum** (bẫy cột `ENUM(...)` native của MySQL) và **cố ý nullable** (bẫy `NOT NULL` không DEFAULT vừa làm hỏng toàn bộ việc đặt lịch), dùng để: hiện badge nguồn trên thẻ đỏ, đưa vào prompt AI (model nay nói rõ mục nào "bệnh nhân tự khai, chưa được xác nhận lâm sàng"), và chặn bệnh nhân xoá mục do bác sĩ ghi — luật xoá nằm trong `AllergyService.whyCannotDelete` theo đúng khuôn `whyCannotCancel` nên nút bị ẩn và POST tự chế đều bị chặn bởi cùng một hàm; **xoá `MedicalRecordService.addPatientAllergy`** vì để cạnh đường mới là có hai lối vào một bảng mà chỉ một lối áp luật — `AllergyService(+Impl)` + `UserAllergyController` (mới), `Allergy`, `AllergyRepository`, `MedicalRecordService(+Impl)`, `ProfileController`, `DoctorMedicalRecordController`, `DoctorExamAiController`, `SecurityConfig`, `user/profile.html`, `doctor/medical-record-form.html` — medical-records.md, ai-assistant.md, authentication-and-roles.md, code-structure.md, environment-setup.md
- 2026-08-07 — `feat:`/`fix:` **đủ 8 ô AI Insight + 4 trợ lý AI tại form khám** — **(A)** dashboard bác sĩ chỉ có 1/8 ô có AI (ô "Đánh giá TB"); việc này từng làm ở `2287cc2` rồi bị revert bởi `25d2a3a` nên tài liệu mô tả sai thực tế — khôi phục nguyên 5 file (`DoctorInsightService(+Impl)`, `DoctorInsightDTO`, `DoctorDashboardStatsDTO`, fragment `ai-insight.html`) và áp tay 3 file đã đổi vì HEAD đi tiếp; câu chữ vẫn là **luật cố định Java, không gọi LLM**, AI thật chỉ chạy khi BẤM vào ô (một listener **uỷ quyền** + `preventDefault` vì 2 thẻ đầu nằm TRONG `<a>`, câu hỏi đi qua `data-ai-prompt` chứ không `th:onclick` vì tiếng Việt có dấu nháy làm vỡ JS), kéo theo xoá `/quick-review-advice` ở CẢ HAI nơi nên con số trên thẻ và câu nhận định dùng chung một biến — **(B)** sửa 2 lỗi đang sống mà bản revert đã cuốn theo: biểu đồ "Phân bố Đánh giá" **dán nhãn ngược hoàn toàn** (`getRatingDistribution` trả 5★→1★ còn trục X ghi 1★→5★) và bác sĩ **chưa có đánh giá nào bị báo đỏ oan** "cần khắc phục ngay" (vì `getAverageRating` trả `0.0` chứ không `null` nên nhánh "chưa đủ đánh giá" là code chết) — **(C)** thêm `DoctorExamAiController` (`/api/doctor/chat/exam/**`, tách khỏi `DoctorAiController` vốn đã dài, nằm cùng tiền tố nên **không cần luật phân quyền mới**) với 4 trợ lý ở form khám: đối chiếu đơn thuốc với **dị ứng đọc thẳng từ DB** (không nhận từ client — đây là dữ liệu an toàn người bệnh), soạn nháp lời dặn **bắt buộc đúng cụm "Tái khám sau N ngày"** mà `FollowUpReminderTask` bóc tách (viết tự do thì nhắc tái khám im lặng không chạy), gợi ý mã ICD-10, tóm tắt bệnh sử — cả 4 đều KHÔNG `@Transactional` (gọi mạng giữa hàm), đều qua `requireOwnedBooking` (403 giống nhau cho "không phải của bạn" và "không tồn tại" để không dò được id), và chỉ GỢI Ý chứ không tự ghi vào `MedicalRecord` — `DoctorExamAiController` + `ExamAssistRequest` (mới), `DoctorInsightService(+Impl)` + 2 DTO + `ai-insight.html` (khôi phục), `DoctorDashboardController`, `DoctorAiController`, `doctor/dashboard.html`, `doctor/medical-record-form.html` — ai-assistant.md, code-structure.md, medical-records.md, coding-conventions.md
- 2026-08-07 — `feat:`/`fix:` **chuông thông báo cho BỆNH NHÂN** — chuông ở header bệnh nhân vốn là thẻ `<a href="#">` chết (không dropdown, không badge, không script, không API), nên các `Notification` mà `FollowUpReminderTask` ĐÃ ghi cho bệnh nhân từ trước không có đường nào tới được họ; thêm `UserNotificationApiController` (`GET /api/notifications` + `POST /read`, tách khỏi `/api/staff/notifications` vì endpoint đó gộp 4 khối nhắc chỉ dành cho nhân viên), `user-notifications.js` (id `userNotif*` chứ không `staffNotif*` vì `medical-record-detail.html` nhúng cả hai khu), markup + script đặt trong fragment header nên **22 template** bệnh nhân có chuông chỉ với một lần sửa — **(B)** gắn `NotificationService.pushBookingEvent` (mới, gom câu tóm tắt + link về một chỗ cho 11 nơi gọi) cạnh MỌI lời gọi email hướng tới bệnh nhân: đặt lịch (ví/VNPay/VietQR/tại quầy), xác nhận (bác sĩ/admin/lễ tân), dời lịch, hủy lịch, đổi bác sĩ; riêng `ProfileController` phải gọi thêm vì nó tự hoàn tiền chứ không đi qua `cancelWithRefund` — **(C)** `pushToAllPatients` (`@Async`, `saveAll` theo lô 500, **không** kèm `@Transactional` vì thứ tự proxy `@Async`/`@Transactional` không xác định) bắn tin mới tới mọi `ROLE_USER`, guard chống trùng nằm ở `AdminPostController` (đọc trạng thái TRƯỚC khi ghi, ở cả `publishPost` lẫn `savePost`, vì `/publish/{id}` là GET nên F5 gọi lại được) — **(D)** `Booking.reminderSent` + `AppointmentReminderTask` (7h30 hằng ngày, nhắc lịch NGÀY MAI qua email + chuông, finder có `@EntityGraph` vì luồng cron không có open-in-view) + `EmailService.sendAppointmentReminder` dùng lại `general-notification.html` qua `sendGeneral` tách chung — tiện thể sửa 2 lỗi chặn đường: **`~{user/index :: footer}` là fragment KHÔNG tồn tại** ở 6 template (`career-details`, `careers`, `doctor-schedule`, `medical-process`, `payment-result`, `working-hours`) làm Thymeleaf vỡ render tại chỗ đó, mất footer lẫn mọi `<script>` phía sau — tức mất luôn Bootstrap nên chuông hiện mà không mở được; và `checkout-qr.html` / `payment-result.html` không nạp Bootstrap JS bản nào — `UserNotificationApiController` (mới), `AppointmentReminderTask` (mới), `user-notifications.js` (mới), `NotificationService(+Impl)`, `EmailService(+Impl)`, `Booking`, `BookingRepository`, `SecurityConfig`, `BookingServiceImpl`, `ReceptionServiceImpl`, `BookingController`, `PaymentController`, `VietQRController`, `ProfileController`, `AdminBookingController`, `AdminPostController`, `DoctorDashboardController`, `ReceptionistBookingController`, `ReceptionistWalkInController`, `user/include/header.html` + 8 template bệnh nhân — supporting-subsystems.md, authentication-and-roles.md, booking-flow.md, coding-conventions.md, medical-records.md, environment-setup.md
- 2026-08-06 — `feat:`/`fix:` hoàn thiện ô "AI Insight" trên dashboard bác sĩ: từ 1 ô lên đủ **8 ô** (3 thẻ thống kê còn lại + 4 panel biểu đồ) — thêm `DoctorInsightService(+Impl)` giữ toàn bộ luật, `DoctorInsightDTO` (advice + colorClass + **prompt**) và `DoctorDashboardStatsDTO` gói số liệu controller đã đọc sẵn; fragment dùng chung `doctor/include/ai-insight.html` thay cho markup lặp, một listener **uỷ quyền** + `preventDefault` thay 8 `onclick` (2 thẻ đầu nằm TRONG thẻ `<a>` nên bấm vào ô là vừa mở chat vừa nhảy trang), câu hỏi đi qua `data-ai-prompt` chứ không qua `th:onclick` vì tiếng Việt có dấu nháy làm vỡ JS — **bỏ hẳn `fetch`**: nội dung ô nay render tại server nên không còn spinner/nhánh lỗi mạng và con số trên thẻ với câu nhận định dùng chung một biến, kéo theo xoá `/quick-review-advice` ở CẢ HAI nơi (`DoctorAiController` + bản song sinh chết trong `DoctorDashboardController`) — tiện thể sửa 2 lỗi có thật ở đúng panel đang gắn insight: **(1)** biểu đồ "Phân bố Đánh giá" đọc ngược (`getRatingDistribution` trả 5★→1★ còn nhãn trục X ghi 1★→5★, cột 5 sao bị dán nhãn "1 Sao"), **(2)** bác sĩ chưa có đánh giá nào bị báo đỏ "cần khắc phục ngay" vì `getAverageRating` trả **0.0 chứ không null** nên nhánh "chưa đủ đánh giá" là code chết — `DoctorInsightService(+Impl)`, `DoctorInsightDTO`, `DoctorDashboardStatsDTO`, `DoctorDashboardController`, `DoctorAiController`, `doctor/dashboard.html`, `doctor/include/ai-insight.html` (mới) — ai-assistant.md, code-structure.md
- 2026-07-30 — `fix:` chuông thông báo bấm không ra gì ở trang bác sĩ/admin (trưởng khoa thì chạy) — `doctor/dashboard.html`, `doctor/booking-manager.html`, `admin/dashboard.html`, `admin/booking-list.html` vừa nhúng fragment footer (vốn đã nạp bootstrap.bundle) vừa tự khai lại thẻ `<script>` bootstrap, nên Bootstrap chạy HAI lần và data-api gắn hai handler lên `document`: một cú bấm chạy `toggle()` hai lần (mở rồi đóng ngay) làm mọi dropdown/modal/alert trên các trang đó như bị liệt — trang `/head/**` chỉ nạp một bản nên vẫn chạy; bỏ các thẻ script trùng, riêng `user/medical-record-detail.html` thêm `th:unless` vì bệnh nhân không được nhúng footer nên vẫn cần bản riêng — tiện thể sửa `~{user/index :: footer}` (fragment KHÔNG tồn tại) ở chính file đó, lỗi cũ làm trang xem hồ sơ của BỆNH NHÂN vỡ giữa chừng ở `</main>`, mất footer lẫn nút xuất PDF — `doctor/dashboard.html`, `doctor/booking-manager.html`, `admin/dashboard.html`, `admin/booking-list.html`, `user/medical-record-detail.html` — coding-conventions.md
- 2026-07-30 — `feat:`/`fix:` chặn mọi thao tác lên dữ liệu QUÁ KHỨ + thông báo quyết định + trưởng khoa tự xếp ca khoa — **(A) bác sĩ hủy được lịch quá khứ:** `confirmBooking`/`cancelBooking` chỉ kiểm tra quyền sở hữu nên bác sĩ hủy được cả lịch `COMPLETED` (ví bệnh nhân bị hoàn tiền cho ca đã khám thật) và "xác nhận" được lịch năm ngoái kèm email; thêm `BookingService.whyStaffCannotChange` (không áp mốc 24 tiếng để vẫn hủy được lịch đột xuất, so cả giờ nên ca 08:00 đóng từ trưa), controller chặn thật + template làm mờ nút kèm lý do qua `actionBlockReasons`, `cancelBooking` thôi tự hoàn tiền mà đi qua `cancelWithRefund` (kèm lý do vào sổ ví), và luật "chỉ khám đúng ngày hẹn" nay có ở server chứ không chỉ trong template — **(B) trưởng khoa duyệt được đơn/ca đã hết hiệu lực:** thêm `whyCannotDecide`/`whyCannotDecideShift` chặn cả duyệt lẫn từ chối khi kỳ nghỉ/ca đã trôi qua (giữ được việc "từ chối đơn đã duyệt" để thu hồi quyết định), bù guard trạng thái mà `rejectShift` chưa hề có, tách khối "Đơn đã hết hiệu lực" và `countPendingInDepartment` chỉ đếm việc còn làm được để chuông/thẻ đếm không báo mãi — **(C) bác sĩ không biết đơn được duyệt hay không:** thêm entity `Notification` + repository + service, ghi thông báo song song với email ở mọi quyết định, `/api/staff/notifications` gộp thông báo đã lưu với các nhắc tính động + `unreadCount` + `POST /read`, bỏ mục "quyết định 7 ngày" tính động (sẽ hiện hai lần), chuông chuyển vào fragment header dùng chung nên có trên MỌI trang nhân viên (`staff-notifications.js` mới, tách khỏi `work-schedule.js` vốn thoát sớm khi không có lưới lịch) — **(D) trưởng khoa chỉ có nút "tự xếp lịch":** thêm `/head/clinic-roster` (bảng Bác sĩ × Thứ × Sáng/Chiều, lưu 1 lần) kiểm theo ĐỘ PHỦ CỦA KHOA thay vì bắt mỗi bác sĩ đủ 7 ngày, nhưng vẫn buộc mỗi bác sĩ ≥1 ca/tuần vì tuần trống hoàn toàn bị `findEffective` hiểu là "chưa đăng ký" và rơi về mẫu tuần cũ, cùng `POST /head/duty-roster/assign` phân công trực (dùng nguyên `validateShift`, ca vào thẳng APPROVED) — `BookingService(+Impl)`, `DoctorDashboardController`, `DoctorBookingManagerController`, `DoctorBookingRequestController`, `DoctorMedicalRecordController`, `doctor/booking-manager.html`, `doctor/booking-requests.html`, `LeaveService(+Impl)`, `LeaveRequestRepository`, `StaffScheduleService(+Impl)`, `HeadApprovalController`, `HeadRosterController` (mới), `head/leave-requests.html`, `head/duty-roster.html`, `head/clinic-roster.html` (mới), `head/dashboard.html`, `head/include/sidebar.html`, `Notification` + `NotificationRepository` + `NotificationService(+Impl)` (mới), `ClinicRosterRowDTO` (mới), `StaffScheduleApiController`, `doctor/include/header.html`, `staff/work-schedule.html`, `staff-notifications.js` (mới), `work-schedule.js` — booking-flow.md, supporting-subsystems.md, code-structure.md, coding-conventions.md, environment-setup.md
- 2026-07-29 — `fix:` 2 lỗ hổng còn lại của chatbot — **(1) phân quyền:** `/api/doctor/**` khai `permitAll` TRƯỚC `/api/doctor/chat/**`.hasRole("DOCTOR") mà `DoctorAiController` lại `@RequestMapping("/api/doctor/chat")`, nên Spring lấy luật khớp đầu tiên và toàn bộ trợ lý AI của bác sĩ MỞ CÔNG KHAI (người lạ đọc được tên bệnh nhân, chẩn đoán, lịch khám); thêm khối 0 khai các API bị khoá quyền lên trên mọi `permitAll`; đồng thời `/api/doctors/search` (khác segment với `/api/doctor/**`) rơi xuống `authenticated()` làm khách vãng lai không tra được tên bác sĩ — **(2) lưới khung giờ thứ 12:** `DoctorAiController` tự liệt kê danh sách giờ riêng còn sót ca tối 17:30–19:00 nên báo bác sĩ "rảnh lúc 18:30" (giờ bệnh nhân không đặt được), lại bỏ qua `Schedule` nên báo trống cả ngày bác sĩ nghỉ; nay đọc `TimeSlotService.allSlots()` + lọc qua `BookingService.slotsOutsideWorkingHours`, và bỏ qua booking có `appointmentTime` không parse được (trước đây 1 dòng dữ liệu lạ là 500 cả trợ lý) — `SecurityConfig`, `DoctorAiController`, `TimeSlotService` — authentication-and-roles.md, ai-assistant.md, sync-slot-grid/SKILL.md
- 2026-07-28 — `fix:` rà soát toàn diện chatbot, sửa 30 lỗi/điểm mơ hồ chia 4 nhóm — **(A) đặt sai giờ/ngày:** nhánh "khách nêu giờ" của `resolveBookingHandoff` thôi tự dựng khung giờ từ `booking_target` của model và thôi hỏi `booked-slots` (API này không biết lưới nên "10h20"/"7 giờ tối" đều thành "còn trống"), nay đi qua `/slot-alternatives`; `normalizeTimeHint` không còn bắt chữ "h" của "hôm/hoặc" ("đau 3 hôm nay" → 15:00), hiểu "8h kém 15"; `extractDateHint` xét tên thứ trước "hôm nay/ngày mai", sửa "thứ 2 tuần sau" dư 7 ngày, chỉ nhận dấu "/" và validate ngày (hết cảnh "sốt 39-40 độ" → ngày 39/40), thêm ngày mốt / cuối tuần / N ngày nữa / mùng 5 tháng 8 — **(B) nhầm bác sĩ & ý định:** `pickBestDoctorMatch` trùng tên thì HỎI LẠI thay vì chọn bừa, so khớp không dấu; `extractDoctorName` có stoplist ("bác sĩ nào cũng được"), sửa thứ tự cắt dấu câu; thêm `parseCancelIntent` (gõ "thôi không đặt nữa" không còn bị chốt lịch) và `looksLikeBookingRequest` ("phí đặt lịch bao nhiêu?" không kích hoạt luồng đặt); `resolveAlternativeChoice` hiểu "đổi lịch sang bác sĩ khác" là đổi BÁC SĨ — **(C) im lặng khi lỗi + UX:** thay mọi `return null` câm bằng `{error}` có câu giải thích, phân biệt lỗi mạng với "không tìm thấy bác sĩ", thu hẹp try quanh `JSON.parse` (hết cảnh đổ `reasoning`/`patient_summary` ra chat), guard `ai_reply` thiếu, `safeStorage` bọc sessionStorage, đếm ngược 5s + nút Huỷ thay redirect 900ms, cờ `isSending` + `isComposing` — **(D) backend:** 2 NPE làm khách kẹt "Hệ thống bận" vĩnh viễn, timeout RestTemplate + bỏ `@Transactional` khỏi hàm gọi mạng, bỏ model id không tồn tại, log lỗi OpenRouter (HTTP 200 kèm `error`), `max_tokens`, validate input `/ask`, chặn ngày quá khứ/quá xa, `resolveCanonicalSlot` theo khung chứa, soft-lock chỉ đặt khi chốt (`POST /hold-slot`) — `AiService`, `AiController`, `AiRequest`, `AiResponse`, `BookingHealthyApplication`, `ai-chat.js`, `meditrust-voice-call.js` — ai-assistant.md, booking-flow.md, coding-conventions.md, sync-slot-grid/SKILL.md
- 2026-07-27 — `fix:` role trưởng khoa "chỉ thấy giao diện, không dùng được": tài khoản chỉ có ROLE_HEAD_DOCTOR bị đăng nhập rơi về `/` như bệnh nhân, admin liệt kê thành "Bệnh nhân", và không có chỗ gán khoa phụ trách nên `/head` báo chưa được gán — `SecurityConfig` (successHandler thêm nhánh HEAD_DOCTOR → `/head/dashboard`, đặt SAU DOCTOR), `OAuth2LoginSuccessHandler` (nhánh tương tự), `admin/manage-user.html` (hiển thị đủ 5 vai trò), `AdminController` (`syncHeadOfDepartment` + dropdown khoa khi tick Trưởng khoa, gán/gỡ `StaffProfile.headOfDepartment`), `admin/user-form.html` (nhãn vai trò tiếng Việt, ô "Khoa phụ trách", script ẩn/hiện) — authentication-and-roles.md
- 2026-07-26 — `fix:` khách xin "đổi sang buổi chiều" lại được xếp buổi SÁNG ngày khác — `ai-chat.js` (`extractSessionHint` bỏ alias `sang`/`toi` trùng giới từ "sang" và đại từ "tôi", xét chiều/tối trước sáng; giờ & ngày lấy từ câu khách trước, `lastHandoffDate` sau, `booking_target` của model cuối cùng vì model bịa giờ "09:00 - 11:00" và bịa ngày; khách nêu BUỔI cũng là `suggested` nên thẻ ghi "em chọn giúp ... trong buổi sáng"), `AiService` (mục 5B cấm tường thuật "em kiểm tra khung giờ ..." và cấm nêu giờ/ngày cụ thể trong `ai_reply`) — ai-assistant.md
- 2026-07-26 — `fix:` trợ lý AI mời khung giờ bác sĩ đang nghỉ, và khi không đặt được thì âm thầm đổi sang giờ khác mà vẫn ghi "khung giờ anh/chị vừa chọn" — `AiController` (2 endpoint lọc theo `Schedule` qua `BookingService`, `slotBlockReason`/`DaySlots`, `reason`+`reasonText`+`requestedDoctorWorkingRanges`, tham số `session`, quét sang ngày làm việc kế tiếp, bỏ 2 injection chết), `ai-chat.js` (`extractDateHint` hiểu tên thứ/`tuần sau`, `extractSessionHint`, `isSlotBookable`, `makeHandoff`, dựng lại `resolveBookingHandoff` 3 nhánh, thẻ `suggested` nói thật, sửa khóa `meditrust_session_id` bị lọt khoảng trắng), `meditrust-voice-call.js` (đọc lý do, nhánh fallback luôn là câu hỏi mở), `AiService` (mục 1 + 5B, `buildTodayBlock`), `BookingServiceImpl.rescheduleByUser` (chặn ngoài ca khám) — ai-assistant.md, booking-flow.md, sync-slot-grid/SKILL.md
- 2026-07-25 — `docs:` thêm 2 skill quy trình cho các điểm hay vỡ vì lặp thủ công: `sync-slot-grid` (11 nơi khai báo lưới khung giờ) và `ai-schema-change` (9 keys JSON + 4 script frontend); sửa 2 chỗ tài liệu sai — `.claude/skills/sync-slot-grid/SKILL.md`, `.claude/skills/ai-schema-change/SKILL.md` — booking-flow.md (bổ sung `doctor-details.html`, `index.html` vào danh sách template), ai-assistant.md (frontend đọc key là 4 file JS chứ không phải template; 2 chỗ đếm key viết khác nhau)
- 2026-07-25 — `fix:` đăng ký ca khám chỉ ăn vào TUẦN SAU (trước đây lưu một lần là đổi cả tuần hiện tại lẫn quá khứ), khóa lịch tuần hiện tại và các tuần đã qua, chốt đăng ký lúc 22:00 CN — `Schedule.weekStart` (cột mới), `ScheduleRepository.findEffective`/`findEffectiveOn`, `LeavePolicy.weekStartOf`/`CLINIC_DEADLINE_*`, `StaffScheduleServiceImpl` (`replaceClinicSchedules` theo tuần, `autoFillFullWeek`, `getEvents` gắn `readOnly`), `BookingServiceImpl`, `ScheduleInfoController`, `DoctorController`, `DataInitializer`, bỏ `DoctorService.registerSchedule`/`deleteSchedule` + route `/doctor/schedule/delete/{id}`, `work-schedule.html|js`, `work-schedule.css` — supporting-subsystems.md, booking-flow.md, code-structure.md
- 2026-07-25 — `feat:` đăng ký ca khám cho TUẦN SAU (mỗi ngày tối thiểu 1 ca), nhắc bác sĩ vào sáng CN + tự động xếp lịch cả tuần tối CN nếu chưa đăng ký, trưởng khoa nhắc/chốt thủ công — `StaffProfile.clinicRegisteredForWeek`, `StaffScheduleServiceImpl` (validate mỗi ngày ≥1 ca, `nextWeekStart`/`sendNextWeekRegistrationReminders`/`autoRegisterUnregisteredDoctors`), `ClinicRegistrationTask` (cron CN), `StaffScheduleApiController` (nhắc ở chuông), `HeadApprovalController` (`/head/clinic/remind|finalize`), `work-schedule.html`, `head/dashboard.html` — supporting-subsystems.md
- 2026-07-25 — `fix:` đặt lịch không theo lịch làm việc (bác sĩ chỉ đăng ký ca sáng vẫn chọn được giờ chiều) + trang bác sĩ lúc ẩn lúc hiện (tự ẩn thẻ khi hết chỗ) — `BookingService.isSlotWithinWorkingHours`/`slotsOutsideWorkingHours`, `BookingApi.booked-slots`, `BookingController.processAppointment` (chặn server), `user/doctors.html` (không ẩn thẻ, báo "Hết lịch trống") — booking-flow.md
- 2026-07-24 — `feat:` đăng ký ca khám theo tuần (bảng Thứ×buổi, lưu 1 lần), lịch full-width co giãn + Lịch tháng dạng ô ngày, trực lặp nhiều tuần; sửa lỗi ca khám hiện chồng 2 khối (do lưu cả StaffShift lẫn Schedule) — `StaffScheduleServiceImpl` (`saveClinicTemplate`, bỏ `syncClinicSchedule`, `getEvents` bỏ qua clinic StaffShift), `StaffWorkScheduleController` (`/clinic/save`, `repeatWeeks`), `work-schedule.js|css` (lưới tháng + full-width), `work-schedule.html` — supporting-subsystems.md
- 2026-07-24 — `feat:` lịch làm việc, lịch trực & nghỉ phép cho bác sĩ + lễ tân (đăng ký ca khám / phiên trực đúng luật, nghỉ phép theo quota BLLĐ 2019 & Luật BHXH 2024, role trưởng khoa duyệt đơn, tìm người thay ca, giao diện lịch theo ảnh mẫu) — `LeavePolicy`, `StaffProfile`/`StaffShift`/`LeaveRequest`/`ShiftCoverRequest` + enum, `StaffScheduleService`/`LeaveService`/`ShiftCoverService`, `controller/staff|head`, `StaffScheduleApiController`, `templates/staff|head`, `work-schedule.css|js`, `DataInitializer.ensureStaffProfiles/ensureHeadDoctors`, `SecurityConfig` — supporting-subsystems.md, authentication-and-roles.md, code-structure.md, booking-flow.md, environment-setup.md
- 2026-07-24 — `fix:` bỏ khung giờ tối 17:30–20:30 khỏi đặt khám (ca khám chỉ trong giờ hành chính, ngoài giờ là phiên trực) — `BookingApi`/`AiController` (`ALL_SLOTS`), `TimeSlotService`, `ReceptionServiceImpl`, `DoctorServiceImpl.registerSchedule`, `AiService` (prompt giờ làm việc), `ai-chat.js`, 5 template đặt lịch — booking-flow.md, ai-assistant.md
- 2026-07-23 — `fix:` đồng bộ toàn bộ ảnh bác sĩ về một phong cách (chân dung studio, áo blouse trắng, nền xám) cho cả 132 bác sĩ — `uploads/doctor-*.jpg`, `uploads/bs-*.jpg` — environment-setup.md
- 2026-07-23 — `feat:` bổ sung 5 bác sĩ cho mỗi chuyên khoa (110 bác sĩ + lịch trực + ảnh đại diện) — `DoctorSeedData` (mới), `DataInitializer.ensureExtraDoctors`, `uploads/bs-*.jpg` — environment-setup.md, code-structure.md
- 2026-07-24 — `fix:` khách chọn "hướng 1" / "đổi bác sĩ" mà trợ lý vẫn hỏi lại (model không hề biết hệ thống vừa gợi ý gì) — `ai-chat.js` (`resolveAlternativeChoice`, `handoffFromAlternative`, `buildAlternativeContext`, `finishBookingHandoff`) — ai-assistant.md
- 2026-07-24 — `feat:` khung giờ kín thì báo ngay và gợi ý bác sĩ cùng khoa ít ca khám nhất quanh giờ đó (kèm lý do), bỏ tự nhảy trang khi lệch giờ — `AiController./slot-alternatives`, `ai-chat.js` (`buildSlotFullHtml`), `meditrust-voice-call.js` (`describeSlotFull`), `AiService` (mục 5B/5C: cấm nhận vơ đã giữ chỗ, chống hỏi lặp) — ai-assistant.md, booking-flow.md
- 2026-07-24 — `fix:` đặt nhầm khung giờ khi khách sửa giờ (xin 10h30 nhưng điều hướng ra 10h) — `ai-chat.js` (`slotStartTime`, `normalizeTimeHint`, `lastHandoffDate`, cờ `fallback`), `meditrust-voice-call.js` (đọc rõ khi phải đổi giờ), `AiService` (luật ghi lại `appointment_time`) — ai-assistant.md
- 2026-07-23 — `fix:` đặt nhầm bác sĩ khi khách yêu cầu đổi người, trợ lý hỏi lại dù khách đã nói rõ, xưng hô em/anh-chị, tăng tốc độ đọc và giảm thời gian chờ — `AiController` (tham số `doctorId`), `ai-chat.js` (`pickBestDoctorMatch`, `extractDoctorName`, `doctorNotFound`), `meditrust-voice-call.js` (`parseYesNo`), `AiService` (luật xưng hô + luật đổi bác sĩ) — ai-assistant.md
- 2026-07-23 — `feat:` giao tiếp bằng giọng nói cho trợ lý AI (nút mic/loa ở cả 3 khung chat + chế độ gọi rảnh tay cho bệnh nhân) — `meditrust-voice.js`, `meditrust-voice-call.js`, `ai-chat.js` (`window.MediTrustChat`), `AiService` (`speech_reply`), 3 template `ai-chat` — ai-assistant.md, coding-conventions.md, environment-setup.md
- 2026-07-23 — `docs:` thêm quy tắc bắt buộc ghi nhật ký tiến độ vào `.claude/rules/` — `.claude/rules/progress-log.md`, `CLAUDE.md` — progress-log.md (mới)
- 2026-07-23 — `feat:` thay đổi lịch khám phía client (bệnh nhân tự dời lịch tại `/user/booking/edit/{id}`) — `UserBookingEditController`, `BookingService.whyCannotReschedule` / `rescheduleByUser`, `templates/user/` — booking-flow.md
- 2026-07-23 — `feat:` role lễ tân: đặt lịch tại quầy, hủy/dời lịch, hàng đợi khám — `controller/receptionist/`, `ReceptionServiceImpl.sortByQueue`, `DataInitializer.ensureReceptionistAccount` — authentication-and-roles.md, booking-flow.md
- 2026-07-21 — `feat:` AI điều hướng tự động đặt lịch (`booking_intent` / `booking_target`) — `AiService`, `templates/*/include/ai-chat*.html` — ai-assistant.md
- 2026-07-21 — `fix:` lỗi hiển thị ảnh avatar và thanh tìm kiếm — `ImageService`, `WebConfig`, static assets — —
